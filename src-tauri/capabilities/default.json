{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Every window: file picking, settings storage and opening links externally. File reads deliberately stay in Rust, so no fs permissions are granted here.",
  "windows": [
    "main",
    "doc-*",
    "panel-*"
  ],
  "permissions": [
    "core:default",
    "core:webview:allow-internal-toggle-devtools",
    "dialog:allow-open",
    "store:default",
    "opener:allow-open-url"
  ]
}
