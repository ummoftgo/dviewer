# dviewer

[한국어](README.md) | [English](README.en.md)

마크다운·JSON·JSONC·JSONL·YAML·TOML·XML·CSV·TSV·텍스트/로그·SQLite·Excel·Parquet·ZIP을 읽기 위한 데스크톱 뷰어. Tauri v2 + Rust 백엔드 + Svelte 5 프론트엔드. 인터페이스는 한국어·영어·일본어·중국어(간체)를 지원합니다.

열네 가지 형식이지만 읽는 방식은 **다섯**입니다. 형식마다 화면을 만들면 열네 벌을 따로 손봐야 하고, 그중 열셋은 늘 뒤처집니다.

| 보기 | 형식 | 하는 일 |
| --- | --- | --- |
| 글 | 마크다운 | GitHub 수준 렌더링(표·체크박스·각주·알림 블록), 코드 구문 강조·언어 193개 선택(TypeScript·TSX·TOML·Dockerfile 포함), Mermaid, KaTeX. 전체·블록 복사(원문/HTML·스타일 포함 옵션), Mermaid SVG·PNG와 표시 수식 TeX·MathML·PNG 복사, 원문/렌더 토글, 클릭·스크롤에 따른 목차 현재 위치 표시·자동 따라가기, 렌더·원문 검색(도구 모음 버튼·Ctrl+F), 본문 너비 선택 메뉴와 설정 슬라이더, 표 열 너비 자동 추천·기본 채우기, 열 너비 끌기·내용 맞춤·스크롤/채우기 전환·머리글 고정 |
| 트리 | JSON · JSONC · YAML · TOML · XML | 접기/펼치기, 키·값·경로 검색, 깊이별 가이드선, 키/값 표, 경로 팝오버, 우클릭 복사. 배열·맵을 서브탭 표로 열기(XML 제외) |
| 표 | CSV · TSV · 텍스트/로그 · JSONL/NDJSON | 고정된 머리글과 행 번호, 끌어서 조절하는 열 너비, 칸 단위 검색과 복사, 머리글 우클릭으로 정렬·이 열만 필터·열 너비 맞추기(모든 격자, Parquet은 필터만). **로그는 앞 필드를 알아보고 열로 나눕니다** — 시각·수준·출처·메시지, 그리고 원하면 `키=값`까지 |
| 컬렉션 | SQLite · Excel(xlsx) · Parquet | 한 파일에 든 여럿 중 하나를 골라 같은 격자로 봅니다. SQLite 는 읽기 전용 연결과 DDL 보기, xlsx 는 시트 선택과 수식 보기, Parquet 은 스키마 보기 |
| 압축 | ZIP | 안에 든 문서의 목록. 고르면 **위의 네 가지 중 제 것으로** 새 탭에 열립니다 |

- 500MB급 JSON·CSV와 200MB급 로그를 통째로 메모리에 올리지 않고 다룹니다. 자세한 수치는 [검증과 성능](doc/verification.md)에 있습니다.
- 파일 선택, 드래그 앤 드롭, URL, 붙여넣기 네 경로로 열 수 있고 탭으로 여러 문서를 동시에 봅니다.
- 형식은 확장자로 정하고(판별되지 않으면 텍스트), 문자 인코딩(UTF-8 · CP949/EUC-KR · UTF-16 등)은 내용으로 판별합니다. 둘 다 도구 모음에서 언제든 바꿀 수 있습니다.
- **로그는 열로 나뉩니다.** 앞머리의 시각·수준·`[출처]` 를 알아보고 나머지는 메시지 한 열에 둡니다. 스택 트레이스처럼 시각으로 시작하지 않는 줄은 앞 레코드에 이어 붙습니다. `키=값` 쌍은 도구 모음에서 펼칠 수 있고, 언제든 한 줄짜리로 되돌릴 수 있습니다. ERROR·WARN 은 레벨 칸에만 은은한 색이 붙습니다. 추론이 확실하지 않으면 나누지 않습니다.
- **JSONL 은 표입니다.** 한 줄이 한 행, 키가 열입니다. 열은 앞부분 표본에서 알아내고, 없는 키는 빈 칸으로 둡니다 — 줄이 객체가 아니면 나누지 않습니다. 언제든 한 열짜리 원본 줄로 되돌릴 수 있고, 형식을 JSON 으로 바꾸면 같은 파일이 트리로 열립니다.
- **주석이 있는 JSON 은 JSONC 로 읽습니다.** `.jsonc` 는 `//`·`/* */` 주석과 후행 쉼표를 지나갑니다. `.json` 은 그대로 엄격합니다 — 대신 엄격한 읽기가 멈춘 자리가 JSONC 였다면 지나갔을 자리이면, 오류가 그 전환을 알려 줍니다.
- **JSONC 의 주석이 값 옆에 남습니다.** 값 **위**에 쓰인 설명과 값 **뒤**에 같은 줄로 쓰인 덧말이 그 행 끝에 흐리게 붙고, 고르면 키/값 표에 각각 전문이 나옵니다 — 저자가 그 자리에 쓴 이유가 거기서 읽히라는 것이니까요. 덧말은 그 줄에서 끝난 것에 붙으므로 `} // 여기까지` 는 그 블록의 말이 됩니다.
- **JSONPath 로 노드를 고를 수 있습니다** (트리 뷰). `$.items[0].name` · `$..title` 처럼 자리를 고르고, `$..book[?@.price < 10]` 처럼 **값으로 걸러**낼 수도 있습니다 — 슬라이스(`$.items[1:3]`)·합집합(`$.items[0,-1]`)·뒤에서 세는 인덱스(`$.items[-1]`)까지 RFC 9535 의 선택자 전부입니다. 글자를 찾는 것이 아니라 자리를 고르는 것이라, 3,840만 노드 파일에서 `$..notes` 가 0.06초입니다. 기존 경로 부분 일치 검색은 그대로 있습니다.
- **정규식으로 찾을 수 있습니다.** 검색창의 `.*` 를 켜면 질의를 정규식으로 읽습니다 — 트리에서는 키 하나·값 하나, 격자에서는 칸 하나 안에서 맞추므로 `^\d+$` 가 뜻대로 동작합니다. 끄면 지금까지와 똑같은 리터럴 검색입니다.
- **Parquet 은 여는 시간이 파일 크기를 따라가지 않습니다.** 끝에 붙은 색인만 읽고, 화면이 닿는 행 그룹만 풀어서 보여줍니다 — 52MB 0.19ms, 311MB 0.61ms 로, 자라는 것은 행 그룹 수뿐입니다. 그래서 다른 형식들이 가진 전체 상한이 없습니다.
- **Excel 통합 문서를 엽니다.** 시트를 골라 표로 봅니다. 열 이름은 `A`·`B`·`AA`, 1행은 1행 — 원본과 대조할 때 좌표가 어긋나지 않게. 날짜는 ISO 8601 로, 수식은 토글로 값 대신 볼 수 있습니다. 변환해서 읽으므로 64MB 상한이 있습니다.
- **열네 형식 전부가 압축 파일 안에서도, 주소로도 열립니다.** Excel · Parquet · SQLite 까지 모두 바이트를 받는 리더로 읽으므로 파일일 필요가 없습니다. 압축에서 꺼낸 데이터베이스는 파일을 질의하는 대신 그 **이미지를 제자리에서** 읽습니다 — 사본을 만들지 않고, 그래서 압축 항목의 512MB 상한이 그대로 상한입니다. Excel 의 64MB 도 그대로입니다.
- **SQLite 는 질의로 읽습니다.** 바이트를 훑지 않는 첫 형식입니다. 파일을 읽기 전용으로 열어 표와 뷰의 목록을 보여주고, 고른 것의 행을 CSV 와 **같은 격자**로 그립니다. 1,024행마다 위치를 적어 두므로 300만 번째 행으로도 곧장 갑니다.
- **압축 파일은 열어서 고릅니다.** `.zip` 을 열면 안에 든 것의 목록이 나오고, 하나를 고르면 새 탭에서 제 형식으로 열립니다 — 열네 번째 형식이라기보다 앞의 열세 개의 곱셈입니다. 여는 값은 파일 끝의 목차뿐이라 1GB 짜리도 0.1초입니다(푸는 것은 고른 항목 하나뿐). 안에 든 문서가 하나뿐이면 목록을 건너뛰고 바로 그것을 엽니다. **이름이 UTF-8 이 아닌 압축 파일도 읽습니다** — 압축 파일 하나의 이름을 전부 모아 한 번에 인코딩을 추측하고, 추측일 때는 상태줄이 그렇게 말합니다.
- **gzip 파일은 그냥 열면 됩니다.** `access.log.gz` 를 열면 압축을 풀어 안쪽 이름(`access.log`)으로 형식을 정합니다. 원문 보기도 풀린 내용을 보여줍니다.
- **탭은 이름의 끝을 지킵니다.** 긴 파일 이름은 뒤가 아니라 **가운데**가 줄어듭니다 — `2026-09-report-final.json` 이 `2026-09-report-f…inal.json` 이 되므로, 앞이 같고 끝만 다른 두 파일을 탭에서 구별할 수 있습니다. **이름이 같은 탭에는 폴더 이름이 흐리게 붙고**(`config.json · alpha`), 필요하면 한 단계 더 올라갑니다. 탭이 창보다 많아지면 스트립이 가로로 스크롤되고(양끝이 흐려집니다) **탭 목록 버튼**이 나타납니다.
- 다크/라이트(기본 자동), 인터페이스 배율, 인터페이스·본문 글자 크기, 시스템 글꼴 목록에서 고르는 본문·코드 글꼴을 지정할 수 있습니다.

- **배열·맵을 서브탭 표로 엽니다.** 트리의 비어 있지 않은 배열·객체를 우클릭하면 부모 메인 탭 아래에 표가 열립니다. 같은 위치는 기존 서브탭으로 돌아갑니다. 부모를 닫으면 표도 함께 닫히고, 표만 닫으면 부모가 활성화됩니다. 형식·인코딩을 바꾸면 옛 표는 흐린 라벨과 설명을 붙여 유지하며 새로 열면 새 세대 표가 생깁니다. 배열 행 번호는 `[n]` 경로와 같은 0부터입니다.
- **모든 격자에서 행을 필터합니다.** 대소문자를 구분하지 않는 부분 문자열로 모든 열 또는 지정 열의 값을 찾습니다. 머리글 우클릭으로 기본 순서·오름차순·내림차순을 고르며, 한 번 클릭 순환도 그대로입니다(Parquet은 필터만). 열 이름 칩은 입력 범위이고 결과 범위는 적용 성공 뒤 상태 줄에 표시됩니다. 칩의 ×는 모든 열로 돌리고, 필터 지우기와 입력의 `Esc`는 필터만 지웁니다. 취소는 원래 순서로 돌아갑니다. 복사와 행 번호는 원본을 유지하고 검색은 보이는 행에서 합니다. 메뉴와 경계 두 번 누르기는 해당 열만 표본과 실제 머리글에 맞춥니다.

세션 복원은 기본으로 켜져 있습니다. main 창의 로컬 파일·URL을 탭 순서·활성 문서·원문/렌더 방식과 함께 다시 열고, 시동 요청 문서는 그 뒤에 엽니다. 아카이브 항목은 로컬 루트 파일로 합쳐 다시 엽니다. 붙여넣기·파생 탭·다른 창·스크롤·선택은 복원하지 않으며 `--new`와 스모크는 세션 저장·복원을 건너뜁니다.

설치본은 Markdown·JSON·YAML·TOML·CSV/TSV·Parquet·SQLite의 지정 확장자에 연결 후보를 등록합니다. 기본 앱은 사용자가 선택합니다. Windows 포터블 exe는 등록하지 않고 AppImage는 별도 통합 도구가 필요합니다. macOS는 DMG와 포터블 ZIP의 `.app` 모두 파일 연결 선언을 포함합니다.

열린 로컬 파일의 저장을 감지해 자동으로 다시 읽습니다. 연속 쓰기도 쓰기가 끝날 때까지 갱신을 미루지 않습니다. 설정에서 끄면 변경 알림만 표시합니다. 다시 읽으면 파생 표 탭과 노드 패널은 닫히며 새 문서에서 다시 열 수 있습니다. URL·gzip·아카이브 안 항목은 감시하지 않습니다.

텍스트·로그도 툴바의 **표 / 원문**으로 전환할 수 있습니다. 원문은 가상 스크롤로 보이는 줄만 가져오며, 줄 번호·Unicode 대소문자 무시 찾기·앞뒤 순환·줄 번호 이동을 제공합니다. 긴 줄은 줄바꿈 없이 가로로 스크롤하며 줄 번호는 고정됩니다. 원문 오류는 그 보기에만 표시하므로 표로 돌아갈 수 있습니다. 마크다운 원문은 기존 동작을 유지합니다.

CSV·TSV·JSONL·텍스트·로그와 SQLite·Excel·Parquet·파생 표는 기본으로 남는 창 폭을 열 비율에 따라 채우며, 열 합이 창보다 넓으면 가로로 스크롤합니다. 툴바의 표 너비 드롭다운에서 채우기·가로 스크롤을 고르면 현재 탭에 즉시 적용하고 새 탭의 기본값도 저장합니다. 다른 열린 탭은 바꾸지 않으며 설정 창의 라디오는 새 탭 기본값만 바꿉니다. 원문 모드에서는 드롭다운을 숨깁니다. 자동 추정 상한은 채우기에서 420px, 스크롤에서 8000px입니다. 모드를 전환하면 현재 행 표본으로 기준 폭과 비율을 다시 계산하므로 이전 수동 열 조절은 초기화됩니다. 헤더 메뉴의 열 너비 맞추기·너비 추천은 8000px까지 쓰고, 너비 초기화는 현재 방식의 자동 추정으로 돌아갑니다. 열 구분선에 초점을 두고 좌우 화살표로 8px, Shift와 함께 24px씩 조절하며 Enter·더블클릭으로 내용에 맞춥니다. 채우기에서 맞춤은 이웃 열의 최소 64px를 지키며 폭을 나누고, 내용 폭 자체가 가용 폭보다 클 때는 다른 열의 현재 표시 폭을 유지한 채 가로 스크롤로 넘칩니다. 추정은 현재 행 표본의 1000자 셀 미리보기를 사용합니다. 잘린 셀은 점선 밑줄의 말줄임표와 종류별 툴팁으로 알리고, 선택하면 상태줄에 한도 배지를 표시합니다. 텍스트 셀은 1000자, 바이너리는 기존16바이트 미리보기이며 값 복사는 최대8MiB입니다. 원문 안내는 텍스트·로그에만 제공합니다. 모든 셀이 길 때 텍스트 미리보기 문자 예산은 이전의2배입니다. 시트·테이블을 전환하면 이전 열의 폭과 채우기 비율을 비우고 새 표본으로 다시 계산합니다.

## 필요 환경

- Node.js 20 이상 (개발 기준 24), npm
- Rust 1.88 이상 — calamine이 요구하는 하한입니다 (개발 기준 1.94)
- [Tauri v2 사전 요구사항](https://v2.tauri.app/start/prerequisites/) — Windows는 WebView2와 MSVC 빌드 도구, Linux는 `webkit2gtk`

## 실행

```bash
npm install && npm run tauri dev
```

배포본을 만들려면:

```bash
npm run tauri build
```

`src-tauri/target/release/` 에 실행 파일이, `bundle/msi`·`bundle/nsis` 에 설치본이 생성됩니다.

## 명령줄

```bash
dviewer report.md                      # 위치 인자
dviewer --open="C:/데이터/큰파일.json"
dviewer --open-url=https://example.com/data.json
dviewer --new --open=a.csv             # 탭이 아니라 새 창으로
```

두 번째 실행은 창을 만들지 않고 이미 돌고 있는 프로세스의 **현재 창에 탭으로** 붙습니다. `--new` 를 주면 그때만 창을 하나 더 만듭니다. 동작 원리는 [설계와 구조](doc/architecture.md#명령줄-그리고-이미-열려-있는-창)에 있습니다.

## 업데이트

시작 5분 뒤, 이후 1시간마다 새 안정 버전을 조용히 확인합니다. 시작 화면과 탭 바의 `↑ v…` 배지를 누르면 릴리스 노트와 업데이트 동작을 볼 수 있습니다. 자동으로 설치하지 않습니다. 설정에서 **업데이트 자동 확인**을 끄거나 **지금 확인**을 누를 수 있고, **이 버전 건너뛰기**는 해당 버전의 자동 알림만 숨깁니다.

| 배포 형태 | 새 버전으로 옮기기 |
| --- | --- |
| Windows x64 포터블 exe | 서명 검증 후 실행 파일 교체·재시작 |
| Windows x64 NSIS 설치본 | 서명 검증 후 설치 프로그램으로 갱신·재시작 |
| Windows MSI | 알림과 릴리스 페이지 링크 |
| macOS 앱(DMG·포터블 ZIP) | 알림과 릴리스 페이지 링크 |
| Linux AppImage·deb·rpm | 알림과 릴리스 페이지 링크 |

매니페스트와 설치 파일 모두 내장 공개키로 서명을 검사합니다. 다운로드는 디스크로 바로 쓰며 최대 256MiB이고 도중에 취소할 수 있습니다. 재시작하면 열려 있던 파일·URL 원본을 다시 엽니다. 붙여넣은 문서, 파생 탭, 창 배치와 선택은 복원되지 않습니다. 공개키가 없는 개발 빌드는 확인을 시작하지 않고 설정에서 그 이유를 알립니다.

## 단축키

| 키 | 동작 |
|---|---|
| `Ctrl O` | 파일 열기 |
| `Ctrl T` | 새 탭 (시작 화면) |
| `Ctrl W` | 탭 닫기 |
| `Ctrl Tab` / `Ctrl Shift Tab` | 메인 탭 순환 |
| `Ctrl PageDown` / `Ctrl PageUp` | 활성 문서의 서브탭 순환 |
| `Ctrl E` | 마크다운 원문/렌더 토글 |
| `Ctrl F` | 트리 검색 (전체 / 키 / 값 / 경로), 표 검색, 마크다운 렌더·원문 검색 |
| `Enter` / `Shift Enter` | 다음 / 이전 검색 결과 |
| 마크다운 검색창에서 `Esc` | 검색 닫기·강조 해제 |
| `Ctrl +` `Ctrl -` `Ctrl 0` | 인터페이스 배율 |
| 트리에서 `←` `→` `Enter` | 접기 / 펼치기 |
| 트리에서 `{ 3 }` `[ 3 ]` `< 3 >` 클릭 | 접기 / 펼치기 |
| 표에서 `←` `↑` `↓` `→` `PgUp` `PgDn` `Home` `End` | 칸 이동 |
| 트리·표에서 `Ctrl C` | 선택한 값 복사 |
| 트리에서 우클릭 | 경로 / 키 / 값 복사, 새 창으로 떼어내기 |
| 키/값 표에서 잘린 값 클릭 · `Enter` | 그 자리에서 전체 값 펼치기 |
| 표에서 우클릭 | 값 / 행 / 열 이름 복사 |
| 떼어낸 창에서 `Esc` | 이전 위치로 |
| 마우스 뒤로 / 앞으로 버튼 | 이전 / 다음 선택 노드 |

## 문서

기술 문서는 `doc/` 에 있습니다.

| 문서 | 내용 |
| --- | --- |
| [설계와 구조](doc/architecture.md) | 디렉터리 구조, 트리 엔진과 각 형식의 설계, 인코딩, 다국어, 가상 스크롤, 보안 경계 |
| [검증과 성능](doc/verification.md) | 테스트, 벤치마크 수치와 재는 방법, 픽스처 |
| [빌드와 배포](doc/release.md) | CI 구성, OS별 산출물, 서명 제약, 저장소 규칙 |
| [의존성](doc/dependencies.md) | 패키지 선택 이유와 취약점 점검 |

## 알려진 한계

- 텍스트·로그 원문 검색 질의는 UTF-8 기준 8MiB까지입니다. 검색 엔진의 컴파일 상한을 넘는 질의도 오류로 알립니다.
- 원문 조회가 용량 상한으로 거절되면 같은 범위 키는 다시 요청하지 않으며, 조회 범위가 바뀌면 다시 요청할 수 있습니다.

- 텍스트·로그 원문은 최대 `u32::MAX` 바이트(4GiB 미만)·5천만 줄이며, 한 번에 최대 2,000줄을 조회합니다. 반환 문자열 합계가 8MiB를 넘으면 해당 범위 조회를 거절하며, 줄바꿈 없는 매우 긴 줄도 이 상한을 따릅니다. 찾기는 줄 안의 Unicode 대소문자 무시 부분 일치이며 정규식을 받지 않습니다. 강조는 렌더된 줄마다 앞 2,000개까지입니다. 복사는 현재 렌더된 범위의 브라우저 선택만 지원합니다. 마지막 개행 뒤 빈 줄을 보존하며, CRLF는 LF로 표시·복사됩니다. 기존 인코딩 변환 상한은 그대로입니다.

- 마크다운 검색은 일치 2,000개·정규식 256자·Worker 실행 1초가 상한입니다. 빈 폭 일치는 제외합니다. 렌더 검색은 마크업·조작 버튼·숨긴 내용·수식의 중복 표현을 제외하며, 닫힌 details는 포함하고 이동할 때 엽니다. 문단·셀 경계는 개행으로 구분하고, 그 개행만 일치하면 인접 텍스트 위치로 이동합니다. 강조 API가 없는 환경에서는 상태 줄에 알리고 일치 위치로 이동만 합니다.

- 자기 갱신은 Windows x64 포터블 exe와 NSIS에서만 제공합니다. 업데이트 파일은 256MiB, 매니페스트는 64KiB까지입니다. NSIS는 설치 경로와 다시 열 문서의 명령줄 합계가 1,024 UTF-16 단위(NUL 포함)를 넘으면 설치 전에 거절합니다. 문서를 일부 닫고 다시 시도할 수 있습니다.

- 배열 표의 열은 앞 2,000개 원소 중 객체가 70% 이상이면 첫 등장 순으로 최대 64개를 모읍니다. 나머지는 값 한 열이며, 맵은 `key` 열을 더합니다. 빈 배열·맵과 XML은 표로 펼치지 않습니다. 파생 표의 형식·인코딩은 바꿀 수 없습니다.
- 정렬은 정수 정밀도를 지키며, 글은 값의 앞 128 UTF-8 바이트를 바이트 순으로 비교합니다. 같은 접두는 원본 순서이고 빈 칸·SQL NULL은 양방향 모두 끝입니다. 글 키 저장소는 256MiB 상한이며 넘으면 정렬을 거절합니다. 순열·임시 키·검색 역사상 메모리는 별도입니다. 필터는 칸마다 복사와 같은 8MiB 상한까지만 봅니다.
- Parquet은 머리글 정렬을 제공하지 않습니다. 311MiB 파일에서 정렬된 100행 조회가 500ms 기준을 넘었습니다. 행 필터는 사용할 수 있습니다.
- JSON 파일은 4GB까지입니다 (오프셋이 `u32`).
- 정규식은 **하나의 값 안에서** 맞춥니다 — 트리에서는 키 하나·값 하나, 격자에서는 칸 하나. 여러 노드나 여러 칸에 걸친 매치는 없습니다. `^`·`$` 가 뜻을 가지려면 그래야 합니다.
- 정규식 검색은 리터럴보다 느립니다. 값을 하나씩 보기 때문이고, 300MB CSV 에서 1.5초쯤입니다(리터럴은 0.05초). 도중에 멈출 수 있습니다.
- 붙지 않는 주석이 셋 있습니다: 여는 괄호 바로 뒤(`[ // 이 배열은`), 키와 값 사이(`"k": /* x */ 1`), 그리고 닫는 괄호 앞 자기 줄. 앞의 둘은 아직 끝난 값이 없고, 셋째는 뒤따르는 것을 설명하지도 않으면서 앞 블록의 설명으로 보기에는 저자의 뜻이 갈립니다 — 엉뚱한 것에 붙이느니 보여주지 않습니다.
- JSONPath 는 **선택자도 함수도 RFC 9535 그대로**입니다 — 선택자 전부에 더해 `length()` · `count()` · `match()` · `search()` · `value()` 를 필터 안에서 씁니다. 타입은 **쓰는 동안** 봅니다: `length(@.*)` 처럼 한 노드로 좁혀지지 않는 것에 길이를 물으면 문서를 열기도 전에 거절당합니다. 남은 것은 객체·배열을 통째로 비교하는 것(`?@.a == @.b` 에서 양쪽이 객체인 경우) 하나이고, 쓰면 무엇이 없는지 이름을 대는 오류가 납니다. 합집합 결과는 문서 순서이고 중복이 없습니다 — RFC 는 `[0,0]` 에 같은 노드를 두 번 주지만, 뷰어에서 한 노드를 두 번 하이라이트하는 것은 뜻이 없습니다.
- JSONPath 의 뒤쪽 인덱스와 슬라이스는 **앞에서부터 걷습니다.** 자식은 색인에서 이어 붙어 있어 n번째에 닿으려면 그 앞을 밟아야 하므로, `[-1]` 이나 `[1000000:]` 은 그 행까지 스크롤하는 것과 같은 값이 듭니다 — 240만 항목 배열에서 14ms 입니다. 슬라이스는 몇 개를 고르든 그 걷기를 한 번만 하고, 합집합은 조각마다 한 번씩 합니다.
- JSONPath 는 XML 에는 없습니다. XML 경로는 XPath 모양이라 표현식의 뜻이 달라집니다.
- 리터럴 검색의 대소문자 무시는 ASCII 범위에서만 동작합니다. 정규식 쪽은 크레이트가 유니코드까지 봅니다.
- 마크다운 렌더링은 16MB까지입니다. 그보다 큰 파일은 열리지 않습니다.
- 마크다운 HTML 복사는 일반 편집기에도 같은 HTML 소스를 붙이며, 원문 선택만 마크다운을 복사합니다. 정화 직후 표현을 사용하며 기본은 스타일 없음입니다. “HTML에 스타일 포함”을 켜면 현재 테마의 값을 인라인으로 넣습니다. 내보낸 루트의 글자색·배경색·본문 글꼴은 테마 토큰으로 항상 명시하며, UTF-8 4MiB를 넘으면 알림과 함께 스타일 없는 HTML을 복사합니다. 이 선택은 전체·블록 복사에 함께 적용됩니다. Mermaid와 표시 수식의 렌더 결과는 별도 블록 메뉴에서 복사합니다. 원시 HTML처럼 원문 위치를 확인할 수 없는 블록은 원문 복사를 비활성화합니다. 블록 단독 복사는 참조 링크 정의를 자동으로 덧붙이지 않습니다. 코드 언어 선택은 열린 탭에서 유지하고 재해석 때 초기화합니다. 재강조 입력 상한도 16MiB입니다. 표 너비 추천은 첫·끝을 포함한 최대 20행의 낱말 폭을 측정하며 표본 밖 낱말의 폭까지 보장하지 않습니다. 원문 범위를 자를 때 개행은 보존하지만 Windows 평문 클립보드는 LF를 CRLF로 바꿀 수 있습니다.
- Mermaid SVG는 문자열로, PNG는 투명 배경의 표시 크기 두 배로 복사합니다. 표시 수식 PNG는 번들 KaTeX 글꼴을 포함합니다. PNG의 긴 변은 최대 4096px이며 축소되면 알립니다. 외부 자원을 새로 다운로드하지 않고 SVG의 외부 참조는 제거합니다. Mermaid 글꼴은 시스템 대체 글꼴로 바뀔 수 있습니다. 이미지 클립보드 미지원·변환 실패는 오류로 알립니다. Word·웹메일의 HTML 해석과 글꼴은 붙여넣는 프로그램에 따라 달라질 수 있습니다.
- 마크다운 표의 너비 조절은 병합·중첩 없는 직사각형 표에만 적용됩니다. 기본은 문서 너비 채우기이며 저장된 스크롤 선택은 유지합니다. 스크롤 모드는 보이는 스크롤바를 유지하며 표 위 버튼으로 보기 방식과 너비 초기화를 고릅니다. 경계의 방향키로 조절하고 두 번 누르기·Enter로 내용에 맞춥니다. 채우기는 열마다 최소 3rem을 우선하므로 열이 많으면 가로 스크롤이 남습니다. 채우기에서 넘침이 없으면 원본 머리글을 고정하고, 스크롤 모드·넘침이 있으면 가로 이동과 열 너비에 맞춘 표시용 복제 머리글을 사용합니다. 복제는 검색·복사에서 제외하고 열 조절은 원본에서만 합니다. 표 상태는 열린 탭에서만 유지되고 형식·인코딩을 바꾸면 초기화됩니다. 설정의 기본 방식은 새로 여는 문서에 적용됩니다.
- 펼침 깊이 프리셋은 9까지이며 기본값도 9입니다. 그보다 깊은 단계는 개별 노드를 펼쳐서 봅니다.
- 로컬 파일은 64 MiB 이하이면 전체를 메모리에 복사하고 파일 핸들을 닫습니다. 편집기의 저장·삭제·이름 바꾸기를 막는 매핑을 남기지 않으며, 복사된 바이트에는 파일 축소로 인한 매핑 폴트가 없습니다. **64 MiB 초과 파일만 mmap**을 유지하므로 편집기의 공유 모드에 따라 저장이 막히거나 외부 저장 중 값이 섞이고 프로세스가 종료될 수 있습니다(Unix SIGBUS·Windows in-page error). 변경 감지·재열기는 시간 보장 없는 최선 노력이며 삭제·rename-out은 무시합니다.
- 마크다운의 원격 이미지를 허용합니다. `img-src` 에 `https:` 가 들어 있어 배지 같은 것이 그대로 보이는 대신, 문서를 열었다는 사실이 그 이미지를 주는 서버에 남습니다. 문서가 웹에서 보이는 그대로 보여야 한다고 판단한 결과입니다.
- SQLite 의 뷰와 WITHOUT ROWID 테이블은 rowid 가 없어 행 위치를 미리 적어 둘 수 없습니다. 앞쪽은 똑같이 빠르지만 뒤쪽으로 갈수록 한 화면을 얻는 데 오래 걸립니다. 일반 테이블은 크기와 무관하게 일정합니다.
- SQLite 데이터베이스는 훑기가 500만 행에서 멈춥니다. 그보다 큰 테이블은 앞 500만 행만 보이고 상태줄이 그렇게 알립니다.
- Parquet 의 행 그룹 하나가 400만 행을 넘으면 열지 않습니다. 행 그룹은 절반만 풀 수 없는 단위라, 그보다 크면 처음 닿는 순간 창이 몇 초씩 멈춥니다. 보통 10만~100만 행으로 쓰이므로 실제로 걸리는 파일은 드뭅니다.
- Excel 은 통합 문서를 메모리로 변환해 읽으므로 64MB 까지입니다. 값이 파일보다 커집니다 — 실측으로 9.7MB 파일이 51MB 가 됩니다. 시트의 수식 칸은 Excel 이 **마지막으로 계산해 둔 값**을 보여줍니다 — 다시 계산하지 않고 저장된 파일이라면 그 값이 낡았을 수 있습니다. 셀의 표시 서식은 재현하지 않습니다(날짜는 ISO 8601). `.xls`(옛 이진 형식)는 읽지 않습니다.
- SQLite 는 읽기 전용으로만 엽니다. **파일로 열 때**, 쓰다 만 트랜잭션이 남아 롤백 저널(`-journal`)이 붙어 있는 데이터베이스는, 읽기 전용으로는 그 롤백을 수행할 수 없어 첫 질의에서 오류가 납니다 — 그 데이터베이스를 만든 프로그램이 한 번 열어 정리한 뒤에 봐야 합니다. 압축 파일 안이나 주소에서 온 데이터베이스에는 곁파일 자체가 없으므로 마지막 체크포인트 상태로 보입니다 — 쓰기 앞서 기록하는 로그(WAL)가 있었더라도 그것은 압축되지 않았을 테니까요.
- 압축 파일은 zip 만 읽습니다. `tar.gz` 는 목차가 없어 목록을 만드는 일이 곧 전체를 푸는 일이라, 비용 모델이 다릅니다.
- 압축 파일 안의 항목은 512MB까지 풀립니다. 파일이 적어 둔 크기가 아니라 **실제로 나온 양**을 봅니다 — 적어 둔 숫자는 압축 파일을 만든 쪽이 마음대로 쓸 수 있으니까요.
- 암호가 걸린 항목은 목록에 자물쇠로 표시하고 열지 않습니다. 암호를 묻지도 풀지도 않습니다.
- 압축 파일 안의 마크다운은 상대 경로 이미지가 보이지 않습니다. 그 이미지가 있는 곳은 압축 파일 안이고, 웹뷰가 닿을 수 있는 자리가 아닙니다.
- 압축 파일은 세 겹까지 들어갑니다 (`a.zip → b.zip → c.zip` 안의 문서까지). 한 겹 들어갈 때마다 그 겹이 통째로 메모리에 남습니다.
- 같은 이름이 두 번 든 압축 파일은 마지막 항목만 보입니다. 목차를 이름으로 찾는 구조라 가려진 쪽에는 닿을 길이 없고, 두 행을 그리고 같은 바이트를 여는 것은 가진 것을 속이는 일이라 하나만 그립니다.
- 압축 파일의 항목은 최근 문서에 남지 않습니다. 최근 목록은 파일 경로로 다시 여는데, 항목에는 그런 경로가 없습니다.
- 압축 파일 안 이름의 인코딩은 추측입니다. 틀려도 여는 데에는 지장이 없습니다 — 항목은 이름이 아니라 목차의 번호로 찾습니다.
- 문서 편집·저장 기능은 없습니다. 읽기 전용 뷰어입니다.
