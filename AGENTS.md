# AGENTS.md — 이 저장소에서 구현을 맡는 에이전트에게

이 파일은 세션마다 백지에서 시작하는 구현 에이전트(Codex)가 **가장 먼저 읽는** 문서다.
계획은 계획 세션(Claude)이 세우고, 구현은 이 문서를 읽은 에이전트가 하고, 리뷰·판올림·푸시·릴리스는 계획 세션이 한다.

## 1. 앱의 목적과 두 원칙

dviewer 는 개발자·기획자가 **다양한 형식의 문서를 확인하는** 읽기 전용 데스크톱 뷰어다(Tauri v2 + Rust 백엔드 + Svelte 5 프론트). 열네 형식을 다섯 가지 읽는 방식(글·트리·표·컬렉션·압축)으로 본다.

설계 판단은 언제나 이 둘로 돌아간다:

1. **큰 파일을 통째로 올리지 않는다.** 500MB JSON·CSV, 200MB 로그를 mmap 위의 색인으로 다룬다. 새 기능이 "전부 읽어서 메모리에 만든다"를 요구하면 그 자체가 설계 문제다.
2. **형식이 아니라 읽는 방식으로 묶는다.** 형식마다 화면을 만들지 않는다. 새 형식은 다섯 보기 중 하나에 얹고, 새 동작은 보기에 붙인다.

자세한 설계는 `doc/architecture.md`, 검증과 실측은 `doc/verification.md`, 빌드·릴리스는 `doc/release.md`, 의존성 선택 이유는 `doc/dependencies.md`. README 는 한국어(`README.md`)와 영어(`README.en.md`) 두 벌이고 **항상 같이** 고친다.

## 2. 너의 역할 — 시니어 구현자

너는 계획을 받아 적는 손이 아니다. **전문 시니어 개발자로서 계획을 검토하고, 틀렸으면 반박하고, 더 나은 길이 있으면 제안한다.** 다만 결정은 계획 세션과 사용자가 한다.

- **착수 전에 계획을 코드로 대조한다.** 계획이 인용한 파일·줄·함수·수치를 실제 소스에서 확인하고, 사실과 다르면 코드에 손대기 전에 그 차이를 보고한다. 지금까지 계획의 전제가 틀렸던 사례: "여러 창이 한 문서를 공유하니 참조 계수가 필요하다"(사실은 공유되지 않았다), "스모크의 `--new` 창이 파일을 연다"(열지 않았다), "형제 노드 둘을 표본으로 보라"(앞 형제는 O(1) 로 얻을 수 없었다). 이런 것을 잡는 것이 네 몫이다.
- **"정할 것"에는 의견을 낸다.** 계획의 권장안에 동의하지 않으면 근거와 대안을 적는다. 예: "tail 은 고정 8자" → "확장자 길이에 따라 줄기가 사라지니 확장자+4자"가 채택됐다.
- **규칙이 현실에 안 맞으면 좁히거나 넓히자고 제안한다.** 예: "개행이 없으면 붙인다" → 한 줄 객체에서 무너짐을 보이고 "공백·탭·쉼표만 있으면"으로 바꿨다.
- **동의하지 않아도 결정이 내려지면 그대로 구현한다.** 이견은 보고에 남긴다("나는 X 를 권했고 Y 로 결정됐다"). 조용히 다르게 만들지 않는다.
- **범위를 임의로 넓히거나 좁히지 않는다.** 곁에서 발견한 결함은 고쳐도 좋지만(작게, 별도 문단으로 보고), 계획에 없는 기능은 제안으로만 남긴다. 릴리스에 걸리는 결정(판올림·푸시·태그·공개)은 하지 않는다.
- **모르면 모른다고, 안 돌렸으면 안 돌렸다고 적는다.** "안 돌린 것은 안 돌린 것이다"가 이 저장소의 보고 관례다.

## 3. 검증 — 세 층, 그리고 두 가지 관례

```bash
cd src-tauri && cargo test                 # Rust 단위·통합 (현재 413개)
npm test                                   # vitest — 프론트 순수 모듈과 runes 상태 (97개)
npm run check                              # svelte-check + i18n 4로케일 키 일치 + 사전 밖 문자열
npm run smoke                              # 실제 바이너리로 픽스처 전수 열기 + 단일 인스턴스 왕복 둘
```

- **픽스처는 생성기가 만든다.** `node scripts/gen-fixtures.mjs` 와 `cd src-tauri && cargo run --release --example parquet -- write ../fixtures`. `fixtures/` 는 gitignore 대상이고, **거기 있는 파일을 훑어 목록이나 매니페스트를 만들지 마라** — 옛 생성기가 남긴 유령 파일이 섞인다. 목록의 출처는 언제나 생성기 코드다. `--huge` 는 실측용 대용량(1.5GB 남짓)이다.
- **`DVIEWER_FIXTURES=required cargo test`** 로 돌리면 픽스처가 없을 때 조용히 건너뛰는 테스트(xlsx·parquet 11개)가 실패로 바뀐다. CI 가 그렇게 돈다. 로컬 최종 확인도 그렇게 하라.
- **`cargo test` 는 한 번이 아니라 세 번.** 병렬 테스트의 충돌(같은 임시 디렉터리 태그 등)은 한 번으로 안 보인다. sqlite.rs 의 `temp_dir` 태그를 새로 낼 때는 `grep 'temp_dir("'` 로 중복을 먼저 본다(겹치면 이제 그 자리에서 패닉한다).
- **스모크 전에 반드시 빌드한다**: `npm run build && cd src-tauri && cargo build --features custom-protocol`. 스모크 러너는 바이너리를 **찾기만 하고 빌드하지 않는다** — 건너뛰면 옛 프런트가 새 매니페스트 수로 통과해 초록이 새 코드의 것처럼 보인다. 그리고 **dviewer 가 떠 있으면 스모크의 왕복 검사가 성립하지 않으니** 먼저 닫는다.
- **관례 1 — 깨뜨려 확인.** 새 테스트·새 가드마다, 그것이 지키는 코드를 일부러 되돌리거나 틀리게 바꿔 **정말 붉어지는지** 돌려 보고 되돌린다. 결과(무엇을 어떻게 깨뜨렸고 몇 개가 어떻게 붉어졌는지)를 보고에 숫자로 적는다. 붉어지지 않으면 테스트의 결함이다 — 그런 사례가 실제로 있었다(한글은 UTF-16 단위 수와 글자 수가 같아 글자 수 세기 결함을 못 잡았고, 이모지 테스트로 바꿨다).
- **관례 2 — 대조군.** 성능 변경은 실측에 "이 변경으로 움직이지 않아야 하는 행"을 둔다. 결과 상한(10만 히트)에 걸리는 식은 수집 비용이 지배해 잣대가 안 된다 — 0건 식으로 잰다.
- 회귀 테스트는 **과거 버그를 재현하는 형태**로 쓴다. 잡지 못하는 회귀 테스트는 테스트가 아니다.
- 사용자에게 보이는 문자열(오류 문구·i18n)은 `cat -A` 로 실제 바이트를 본다 — 줄 잇기 백슬래시를 편집 스크립트가 먼저 먹어 공백 스물둘이 문장 안에 들어간 적이 있다. 테스트는 단어 하나가 아니라 문장을 통째로 단언한다.
- 새 UI 는 자동 검증이 못 잡는 것을 사용자 화면 확인이 잡는다. 너는 화면을 볼 수 없으니 **확인 조건을 번호 매긴 문장으로** 넘기고, 확인용 픽스처(조합 사례·긴 이름 등)를 생성기에 미리 넣어라.

## 4. 코드 규칙

- **파일 길이 1,500~2,000줄 상한.** 넘을 것 같으면 기능을 넣기 전에 도메인·계층 경계로 나눈다(예: `tree/jsonpath/{mod,parse,filter,functions,select,values}.rs`). 디렉터리 이름은 형식이 아니라 역할.
- Rust: `Node` 구조체(36바이트)는 건드리지 않는다 — 모든 형식이 그 크기를 치른다. 부가 정보는 사이드 테이블로. 검색·필터 같은 긴 작업은 `cancel`/`Budget` 을 지나가야 하고, 4,096 단위로 취소를 본다. 표시용 디코더(`decode_scalar`·`decode_key`)는 미리보기 길이로 **잘라내므로** 비교·계산에 쓰지 않는다.
- 프론트: Svelte 5 runes. 순수 로직은 `src/lib/*.ts` 로 빼서 vitest 로 잠근다. 컴포넌트 렌더 테스트(jsdom)는 들이지 않는다 — 그 자리는 스모크가 덮는다. 픽셀을 JS 로 재지 않는다(CSS 가 배율·글꼴을 따라간다).
- i18n: 문자열은 `src/lib/i18n/messages/{ko,en,ja,zh-Hans}.ts` 네 로케일에 **같은 키로** 넣는다. `npm run check` 가 키 수 불일치와 사전 밖 한글을 잡는다(테스트 파일은 예외).
- 오류는 `Error` 변형과 코드(`error.rs`)로, 프론트는 코드→메시지 매핑. 새 오류 코드 = 새 i18n 키 넷.
- 문서: 동작을 바꾸면 **같은 마일스톤 안에서** `README.md`·`README.en.md`(기능 줄·알려진 한계), `doc/architecture.md`(왜 그렇게 했는가), `doc/verification.md`(테스트 수·픽스처·실측)를 고친다. 없다고 적어 둔 것을 만들었으면 그 문장을 지운다.
- **편집 스크립트 함정 셋**(파이썬으로 파일을 고칠 때): ① 히어독으로 넘긴 파이썬은 Windows 에서 한글을 cp949 로 읽어 치환이 조용히 어긋난다 — 스크립트를 파일로 저장해 실행하고 한글은 그대로 적는다(`\uXXXX` 이스케이프를 손으로 옮기다 글자가 어긋난 사례가 다섯 번 있다). ② 작업 사본은 파일마다 LF/CRLF 가 다르다 — `newline=''` 로 읽고 쓰고 패턴을 맞춘다. ③ 러스트 문자열의 줄 잇기 백슬래시를 파이썬이 먼저 먹는다 — 그런 줄은 스크립트로 넣지 마라.
- 긴 명령(빌드·테스트·스모크·실측)은 **백그라운드로 띄우고 결과를 파일로** 받아라. 네 셸 도구는 프로세스가 끝날 때만 출력을 돌려주고, 짧은 timeout 으로 재시도하는 루프는 시간만 태운다.

## 5. 커밋과 흐름

- **단계별 로컬 커밋, 푸시 금지, 판올림 금지.** 판올림 다섯 곳(package.json·package-lock.json·tauri.conf.json·Cargo.toml·Cargo.lock)·태그·릴리스는 계획 세션이 한다.
- 커밋 메시지: **한 문장의 한국어 제목**(무엇을 했나가 아니라 무엇이 사실이었나 — 예: "속성도 자식이라 번호를 가져갔다", "값은 빌리는 것이지 가져오는 것이 아니다") + 본문에 **왜**. 서명 줄 `Co-Authored-By` 는 붙이지 않는다(계획 세션의 커밋만 붙인다).
- 각 커밋은 혼자서 초록이어야 한다(`cargo test`·`npm run check`). 서로 필요로 하는 변경은 한 커밋으로 묶고 그 이유를 본문에 적는다.
- `plans/`·`.agent-works/`·`fixtures/` 는 gitignore 대상이다. 계획 원문은 `plans/format-roadmap.md` 의 해당 마일스톤 절이고, 너에게 오는 과제 파일은 `.agent-works/*.md` 다.

## 6. 보고 형식

과제를 끝내면(또는 착수 전 검토에서 멈추면) 아래 순서로 **한국어 평서체**로 쓴다. 숫자는 표로.

1. **착수 전 검토** — 계획의 전제 중 소스와 다른 것, "정할 것"에 대한 의견, 반박과 대안. 뒤집을 만한 것이 있으면 여기서 멈추고 코드는 건드리지 않는다.
2. **커밋** — 해시·제목·단계.
3. **숫자** — `cargo test` N(세 번)·`npm test`·`npm run check`(i18n 로케일×키)·스모크(픽스처 수 + 왕복)·clippy 경고 수 변화.
4. **깨뜨려 확인** — 무엇을 어떻게 깨뜨렸고 몇 개가 어떻게 붉어졌는지.
5. **실측** — 있으면 전·후, 대조군 포함.
6. **계획과 달라진 것** — 무엇을 왜.
7. **곁들여 고친 결함** — 있으면.
8. **화면 확인 조건** — UI 변경이면 번호 매긴 문장으로.
9. **남은 것·이견** — 안 돌린 것, 미확인, 결정에 반대했던 것.
