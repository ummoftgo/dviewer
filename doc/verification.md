# 검증과 성능

← [README](../README.md)

## M49 — 마크다운 표 채우기의 긴 토큰 넘침

자동 채우기의 추천 결과를 `fillWidths`로 제한한다. 기존 고정 표 레이아웃·줄바꿈 CSS와 수동 폭은 유지한다. 단위 검사는 추천 최소 폭이 문서 폭을 넘는 경우, UI 최소 폭 합 바로 아래·같음·바로 위, 이미 맞는 추천 비율의 보존을 검사한다. 프런트 검증은 vitest 394개·check 오류/경고 0(4개 로케일×483키)이 통과했다.

생성기에서 `markdown-table-fit.md`의 긴 URL·인라인 코드 표와 별도 `markdownTableFit` 단계를 추가했다. 설정·탭·표 상태를 명시적으로 fill로 지정하고 finally에서 복원한다. `data-fitted` 뒤 표와 `.table-viewport`의 scrollWidth/clientWidth를 비교하고, 텍스트의 실제 줄바꿈 기하·수동 조절·추천 복원·scroll 왕복·복사 결과도 확인한다. 생성기 기준 스모크 매니페스트는 50개다.

사용자의 앱 종료·cargo 사용 승인 뒤, 각 스모크 직전 `tasklist`로 dviewer 미실행을 확인했다. `DVIEWER_FIXTURES=required cargo test` 555개가 한 번에 통과했다. 추천 폭 제한만 제거한 변형을 새 프런트·debug 바이너리로 빌드하자 `markdownTableFit` 1개가 실패했고 다른 픽스처 49개와 왕복 2개는 통과했다. 실패 시 표 scrollWidth는 17,849px, 뷰포트 clientWidth는 1,072px였다. 원본 SHA-256 일치로 바이트 복원을 확인한 뒤 다시 빌드한 정상 debug는 픽스처 50개와 왕복 2개가 통과했다. 정상 fill의 표·뷰포트·스크롤 폭은 모두 1,072px였고 scroll 모드의 넘침은 유지됐다.

개발 incremental과 dviewer dev 산출물을 정리하고 main `0d67961`에 rebase했다(이미 같은 기준이어서 코드 변경 없음). release 스모크·Linux/macOS·clippy·사용자 화면 재확인은 실행하지 않았다. 위 치수는 기능 검증이며 성능 실측은 없다.

## M45 — 라벨 책갈피

2026-09-17 `m45-current`는 main `5f49700`에서 사용자 화면 결함을 재현했다. 기존 스모크는 `pendingAnchor`로 이동한 뒤 `bookmarkHeading` 갱신까지 기다렸지만, 실제 추가 함수는 다음 애니메이션 프레임 이전의 제목을 읽을 수 있었다. 두 번째 절 중간으로 `scroller.scrollTop`을 직접 바꾸고 scroll 이벤트 직후 툴바 버튼을 클릭하도록 바꾸자 수정 전 debug에서 책갈피 기본 라벨 검사가 실패했다(픽스처 1개 실패·48개 통과, 왕복 2개 통과). 절 중간의 `activeHeading` 계산 자체는 단위 검사에서 정상이다. 추가 대상은 패널을 열기 전에 계산하므로 패널 열림 이후의 effect 정리를 이 재현의 원인으로 보지 않았다.

수정 후에는 `headingPositions`·`prosePosition`으로 추가 시점에 직접 절을 찾는다. 툴바 버튼에 이어 패널이 열린 상태에서 세 번째 절로 스크롤하고 Ctrl+D로 추가한 anchor도 검사한다. 준비되지 않은 좌표를 첫 제목으로 바꾸지 않으며, 제목 없음·첫 제목 앞·숨은 제목·추적 상태의 이전 id/빈 값/null·HTML의 기존 준비 조건을 단위 검사로 덮는다. vitest 392개, check 오류/경고 0(4×483키), `DVIEWER_FIXTURES=required cargo test` 555개가 통과했다. 변경한 Svelte 컴포넌트 3개의 autofixer issues는 비어 있다. 새 프런트와 debug 바이너리를 빌드한 뒤 Windows 스모크 픽스처 49개와 왕복 2개가 통과했다. 각 스모크 직전 `tasklist`로 dviewer 미실행을 확인했다.

직접 좌표 읽기를 다시 `currentAnchor(tab.toc, tab.bookmarkHeading ?? '')`로 바꾼 변형은 상태 테스트 1개 실패·7개 통과였다. 기대한 현재 절 대신 첫 제목이 나오는 것을 잡았고, 파일 SHA-256 일치로 원본 바이트 복원 후 8개 모두 통과했다. 이번 수정의 release·Linux/macOS 스모크·clippy·사용자 화면 재확인은 실행하지 않았다. 성능 변경과 실측은 없다. 아래 기록은 이전 M45 구현 당시의 검증이다.

같은 날 main `8580b71`의 PDF 진단을 추가로 반영했다. PDF 단계 상태·제한된 오류 상세와 책갈피 메시지를 모두 유지했고, vitest 380개·check 오류/경고 0(4×482키)을 확인했다. 이 재검증에서도 cargo·스모크는 실행하지 않았다.

2026-09-17 main `a4eada5`의 M43 PDF 위로 rebase했다. PDF 페이지 복원·프레임 URL·메시지·i18n과 책갈피 동작을 함께 유지했고, 공용 에이전트의 PDF 분기에서는 HTML 제목 추적을 등록하지 않으면서 책갈피 패널 단축키만 전달하는 회귀 검사를 추가했다. 통합 프런트는 vitest 375개·check 오류/경고 0(4×482키)로 확인했다. 이 rebase에서는 cargo·스모크를 실행하지 않았다. 아래 Rust·스모크 수치는 M43 통합 전 M45의 검증 기록이다.

프런트 모델·상태 테스트는 손상 항목 건너뛰기, 모르는 필드 제거, 중복 id 제외, 로드 중·읽기 실패 후 쓰기 차단, 동일 라벨의 독립 추가, 이름 변경·삭제·재지정·전체 삭제, 문서 내 순서·전체 최근순·필터, 기본 라벨·맨 위·id/텍스트 재탐색, 이전 이동 응답 무시를 검사한다. 실제 HTML 에이전트를 VM에서 실행해 제목 보고·맨 위/존재/없는 목적지 응답과 단축키 전달도 확인한다.

새 `bookmarks` 스모크는 자체 저장 목록으로 두 번째 제목의 기본 라벨 추가·다른 탭에서 이동·닫힌 문서 다시 열기·삭제를 수행한다. `pendingAnchor === null`·렌더 준비·제목과 스크롤 기하를 기다리며 고정 프레임 수를 사용하지 않는다. 시작 전 store 값을 보관하고 `finally`에서 복원한다. 생성기 기준 매니페스트는 48개다.

anchor.id의 타입 검사만 제거한 변형은 vitest 1개 실패·3개 통과였다. 숫자 id인 손상 항목이 정상 항목을 밀어내는 것을 잡았으며 원본 바이트 복원 뒤 해당 4개가 통과했다. 이동의 pendingAnchor 대입을 scrollTop=0으로 바꾼 변형은 실제 debug 스모크에서 책갈피 1개 실패·나머지 47개와 왕복 2개 통과였다. 목표 제목 이동이 끝나지 않아 실패했고, 원본 SHA-256 일치를 확인해 복원한 뒤 release 스모크가 통과했다.

2026-09-15 프런트 최종 검사에서 `npm test` 366개와 `npm run check`(오류·경고 0, 4개 로케일×478개 키)가 통과했다. 변경한 Svelte 컴포넌트 7개의 autofixer issues도 비어 있다. 이 결과는 실제 바이너리·WebView 검증을 대신하지 않는다.

required Rust 테스트는 548개가 한 번에 통과했다. 1단계 커밋의 프런트는 363개·4×467키였고, 2단계는 Rust 소스와 포함된 HTML 에이전트 바이트가 동일하므로 이 Rust 결과를 재사용했다. 최종 새 debug·release 빌드에서 각각 픽스처 48개와 왕복 2개가 통과했다.

첫 debug 실행에서는 새 검사가 기존 제목의 scroll-margin-top:1rem을 무시하여 책갈피 1개만 실패했다. 계산된 CSS 여백을 검사에 반영한 뒤 재실행은 통과했으며, 실제 제목 간격 15.84375px와 CSS 여백 16px를 기록했다. 책갈피의 탭 왕복·닫힌 문서 재열기는 정상 debug에서 118ms, release에서 112ms였다. 이는 검사 실행 시간이며 기능의 전후 성능 비교가 아니다.

화면 확인용 생성기 픽스처는 `bookmarks.md`(여러 절·긴 제목), `bookmarks-top.md`(제목 없음), `bookmarks-a-very-long-document-name-for-filter-and-layout.md`(긴 문서명), `bookmarks.html`(HTML 절·프레임 내부 Ctrl+D)이다. Windows 로컬 자동 검증은 완료했으며 Linux/macOS warm 잡과 사용자 화면 확인은 아직 남아 있다. clippy는 실행하지 않았다.

## M41-mac — 즉시 앵커 이동 후보

`pendingAnchor`로 문서를 여는 경로만 `behavior: 'instant'`로 이동한다. 목차와 문서 내부 앵커 클릭의 기본값은 smooth다. macOS의 relativeLinks 생략을 제거했으며 원래 인코딩 경로·앵커·없는 파일 검사를 유지한다. 이 변경은 고침 후보이며 macOS WebKit의 애니메이션 중 언마운트가 원인인지는 리허설로 확인해야 한다.

| 검사 | 결과 |
|---|---|
| vitest | 346통과·32파일 |
| check | 오류0·경고0·4로케일×451키 |
| MarkdownView Svelte 분석 | issues 없음 |
| Darwin 매니페스트 대조 | relativeLinks 활성·원래 앵커 링크·46개 유지 |
| 프런트·debug 빌드 | 통과 |
| Windows debug 스모크 | 1회, 46개·왕복2 통과, 앱 sweep 19,979ms |
| macOS 재현 | 미실행, 세 OS 확인 필요 |

## 컬렉션 탭 전환 경합 (2026-09-15)

Linux warm의 report.json.gz 실패는 이전 컬렉션의 select가 격자 refresh 뒤 다음 placeholder를 읽어 sqlite_schema에 음수 ID를 보내는 경로와 부합했다. CollectionView는 마운트 시 문서 참조·ID·generation·형식을 고정하고 해제 시 무효화한다. 목록·선택·수식 전환의 await 이후와 오류·종료 처리에서 문서 수명과 선택 요청을 검사하며, 격자 refresh 직후에도 다시 검사한다. IPC와 결과 기록은 고정한 문서로만 수행한다. 열기 성공 시 오류를 덮어 지우는 처리는 추가하지 않았다.

| 검사 | 결과 |
|---|---|
| vitest | main 2704f19 rebase 후 355통과·34파일 |
| check | 오류0·경고0·4로케일×455키 |
| Svelte 분석 | CollectionView issues 0 |
| generation만 비교하는 변형 | 새 회귀1개 실패, 원본 바이트 복원 후 재통과 |
| Windows debug 스모크 | rebase 전 741bcd7의 새 빌드로 1회, 46개·왕복2 통과 |
| 정리 | dev incremental 제거, cargo clean 322파일·6.3 GiB, 종료0 |

새 순수 검사는 같은 generation의 placeholder로 교체된 참조와 ID·세대·준비 상태·해제 여부를 검사한다. 실제 Svelte 비동기 재개 순서를 재현하는 DOM 검사는 아니다. Windows 스모크에서 sample.parquet→report.json.gz가 모두 ready로 통과했고 앱 sweep은 19,542ms였다. 그 뒤 main 2704f19로 rebase하면서 M44의 저장된 컬렉션 이름 우선 선택과 이번 요청 수명 검사를 함께 보존했다. 요청 순서대로 rebase 후에는 vitest·check·Svelte 분석만 재실행했다. rebase 이후 바이너리 스모크와 Linux/macOS warm 확인은 남아 있다. cargo test·release 스모크는 실행하지 않았으며 스모크의 완료 기준이나 대기 시간은 바꾸지 않았다.

## M46b — 집중 모드의 입구와 출구

상단 툴바의 집중 모드 버튼과 집중 중 오른쪽 위 종료 알약을 추가했다. 기존 sample.md 집중 스모크에 버튼 클릭→body[data-focus]·알약 표시→알약 초점·클릭→해제·초점 복원을 추가했다. CSS 표시와 동기 속성을 확인하며 고정 프레임 대기나 저장 설정을 사용하지 않는다. 기존 키보드·검색 Esc·메뉴 복원 검사도 유지한다.

| 검사 | 결과 |
|---|---|
| vitest | 346통과·32파일 |
| check | 오류0·경고0·4로케일×451키 |
| Svelte 분석 | App·Toolbar·Icon issues 없음 |
| 빌드·debug 스모크 | 새 프런트·custom-protocol debug 빌드 성공, 46개·왕복2 통과 |
| Rust·clippy | 이번 후속 변경에서 미실행, Rust 변경 없음 |
| release 스모크 | 병합 뒤 계획 세션이 실행 |

debug 스모크는 승인 뒤 한 번 실행했으며 앱 sweep은 18,553ms였다. 결과 JSONL은 `.agent-works/m46b-debug-{sweep,handoff,new}.jsonl`에 보존했다. dev incremental 제거와 `cargo clean -p dviewer --profile dev`로 정리하고 기존 release 산출물은 보존했다.

추가 변형·성능 실측은 이 후속 과제에 지정되지 않았다. 화면에서는 모든 문서 보기의 툴바 아이콘과 툴팁, 집중 모드의 알약 가독성·본문 가림 정도·Tab 접근·클릭 종료를 확인한다. M46의 실제 키보드 동작은 사용자 화면 확인을 통과했다. M46b의 새 버튼은 별도 화면 확인이 남아 있다.

## M46 — 집중 모드

| 최초 구현 검증 | 결과 |
|---|---|
| Rust fixtures required | 538통과·실패0·건너뜀0 |
| vitest | 343통과·31파일(기존338 + 신규5) |
| check | 오류0·경고0·4로케일×449키 |
| 프런트·debug·release 빌드 | 통과 |
| Esc 우선순위 반전 | 1실패·3통과, 원본 바이트 복원 후 전체 통과 |
| 실제 스모크 | debug·release 각각 46개·왕복2 통과 |

main `9166082`의 프레임 진단 변경을 rebase하면서 FrameView의 진단 상태와 집중 모드 표식을 모두 보존했다. 이후 vitest 345개, check 오류0·경고0·4로케일×449키, 프런트·debug·release 빌드가 통과했다. 이 시점의 Rust 소스는 최초 검증과 동일했다.

Windows 실제 스모크의 앱 sweep은 debug 18,312ms·release 17,423ms다. 최초 debug와 진단 재현에서는 sample.md의 메뉴 복원 단언 1개가 실패했다. window 자체에 합성 Esc를 보내면 모드 종료 뒤 복원된 메뉴까지 닫혔으므로 실제 초점 요소에서 키를 발생시키도록 스모크를 고쳤다. 새 debug 빌드의 재검증과 release 빌드의 첫 스모크가 모두 통과했다.

스모크 뒤 main `09992c2`를 충돌 없이 rebase했다. 새 agentStart 진단 메시지를 단축키 횟수에 포함했던 테스트 1개를 시작/키 입력 구간으로 분리한 뒤 vitest 346개·32파일과 check 오류0·경고0·4로케일×449키가 통과했다. FrameView의 Svelte 분석도 issues 없음이다. 최종 rebase 후 cargo·스모크는 재실행하지 않았으므로 위 Rust·스모크 수치는 마지막 진단 커밋 반영 전 결과다. M46 자체의 Rust 변경은 없다. dev incremental 제거와 `cargo clean -p dviewer --profile dev`를 완료했고 release 산출물은 보존했다.

순수 상태 전이는 보기에서 소비한 Esc와 다음 미처리 Esc를 구분한다. 메시지 파서는 focus·escape만 추가 허용하며, 실제 agent.js를 VM에서 실행해 두 키 전송·반복 입력·소비된 키·기존 find/raw·Ctrl+Tab 미전달을 검사한다. 새 안내 문장은 전체 문자열 단언과 cat -A 바이트 확인을 거쳤다.

기존 sample.md 스모크에 body[data-focus] 진입·검색창의 첫 Esc 우선·다음 Esc 종료·본문 DOM 보존·초점 복원을 추가했다. 상단 메뉴를 연 채 진입했을 때 숨긴 메뉴가 Esc를 가로채지 않는지도 확인한다. 고정 프레임 수 대신 동기 속성과 검색 DOM을 사용하고 원래 창 상태를 복원한다. 이 검사는 합성 KeyboardEvent이므로 네이티브 F11 선점과 실제 사용자 키 입력을 입증하지 않는다. 그 확인은 사용자 화면 조건으로 남긴다. 추가 픽스처 없이 기존46검사를 유지한다.

## M47 — 표 열 숨김·순서·고정

| 첫 단계 검증 | 결과 |
|---|---|
| Rust fixtures required | 531통과·실패0·건너뜀0, Rust 변경 없음 |
| vitest | 325통과·28파일 |
| check | 오류0·경고0·4로케일×440키 |
| Svelte 자동 분석 | DataGrid·GridControls·TableView·CollectionView issues 0 |
| 복사 원본순서 변형 | 4실패·3통과, 원복 완료 |
| 재색인 초기화 제거 변형 | 1실패·51필터 제외, 원복 완료 |
| 실제 스모크 | 사용자 앱 종료 후 아래 최종 고정 빌드로 수행 |

원본 열 번호와 표시 순서의 분리, 숨김·복원·마지막 열 보호, 채우기 이웃 보정·넘침 맞춤의 원본 폭 반영, 숨긴 열을 제외한 추천, 기본 행 복사 보존·표시 순서 TSV·빈 값·인코딩 상한·늦은 응답 취소를 검사한다. 기본 구성 복원과 재색인 초기화를 실제 DocTab 테스트에서 확인한다.

기존 컬렉션 스모크는 케밥으로 열을 숨기고 data-visible-columns 감소와 data-fitted 완료를 기다린 뒤, Shift+F10 메뉴로 기본 구성을 복원한다. DOM과 초점 복귀도 확인하며 검사 전 상태를 저장·복원한다. 별도 화면 확인용 columns.csv는 긴 열·중복 이름·탭·따옴표와 세로 스크롤 행을 생성한다. 새 픽스처를 스모크 목록에 추가하지 않아 매니페스트 검사 수는45로 유지한다.

| 고정 포함 최종 검증 | 결과 |
|---|---|
| vitest | 327통과·28파일 |
| check | 오류0·경고0·4로케일×442키 |
| 새 debug 스모크 | 45검사+왕복2 통과(픽스처21초) |
| 새 release 스모크 | 45검사+왕복2 통과(픽스처18초) |
| Rust | 첫 단계531통과 재사용, Rust 변경 없음 |
| clippy | 이번 과제에서는 실행하지 않음 |

고정 offset은 표시 폭과 행 번호 폭을 누적하는 순수 테스트로 검증했다. 고정 열 숨김·개수 감소·해제·재색인 초기화도 검사한다. 컬렉션 스모크는 이전 구성의 초기화, 고정 메뉴와 data-frozen-count·헤더/본문 표시, 기본 구성 복원을 확인한다. 실제 가로 스크롤의 겹침·강조·다른 OS 화면 확인은 사용자에게 남긴다. 새 한국어 상태줄은 전체 문장을 단언했고 cat -A로 실제 바이트도 확인했다.

## M41 — 마크다운 상대 문서 링크

상대 링크 해석은 URL 구분자를 분리한 뒤 디코딩하며, 드라이브·UNC 공유·POSIX 루트를 보존한다. 파일 구분자 비교, URL 중복 탭, 중첩 압축의 직접 부모, 원래 탭의 읽기 상태, 오류 탭 보존·재시도·닫힌 요청의 늦은 실패, 마크다운 앵커 전달을 검사한다. 절대 링크·file 스킴은 제외하고 Rust는 수정하지 않았다.

| 검증 | 결과 |
|---|---|
| Rust fixtures required | 531통과·실패0·건너뜀0 |
| vitest | 313통과·27파일(기존275 + 신규38) |
| check | 오류0·경고0·4로케일×432키 |
| 프런트 빌드 | 통과 |
| 새 debug 스모크 | 45검사 + 단일 인스턴스 전달·새 창 왕복2 통과(픽스처21초) |
| 새 release 스모크 | 45검사 + 단일 인스턴스 전달·새 창 왕복2 통과(픽스처18초) |
| clippy all-target | 고유 경고 위치21개(lib18 + 테스트3), Rust 변경 없음·별도 이전판 실행 없음 |
| Svelte 자동 분석 | App·MarkdownView issues 0 |

`markdown-links.md`를 생성기와 스모크에 등록했다. 실제 DOM 클릭으로 JSON 새 탭의 ready와 트리 준비, 같은 링크의 탭 재사용, 한글·공백 인코딩 경로의 Markdown 앵커 소비와 스크롤, 없는 파일의 새 error 탭 및 오류 DOM을 확인한다. 고정 프레임 수를 완료 신호로 사용하지 않는다. 생성기에는 긴 대상 문서와 돌아오는 링크, archive.zip의 일반·중첩 형제 링크도 있다. 직접 부모 압축 탭이 없거나 대상이 목록에 없을 때에는 기존 지원 불가 알림을 사용한다.

깨뜨려 확인은 `..` 처리 제거에서10실패·20통과, 새 탭 생성을 현재 탭 교체로 바꾼 상태에서 링크 정책7실패·나머지44필터 제외였다. 원본 바이트로 복원 후 전체313개가 통과했다. 성능 변경이 아니므로 별도 실측은 없고, 실제 명세 폴더 화면과 다른 OS 확인은 사용자에게 남긴다.

## M38 — 채우기 넘침과 원문 검색 슬롯

A는 맞춤 때문에 채우기가 넘칠 때 모든 열의 현재 표시 폭을 기준으로 확정하고 대상 열만 바꾼다. 이전 기준 배열과 표시 폭 배열을 변형하지 않는지 함께 검사한다.

| A 검증 | 결과 |
|---|---|
| vitest | 275통과·26파일 |
| check | 오류0·경고0·4로케일×432키 |
| 지정 변형 | 표시 폭 복사를 제거하여2실패·18통과, 원복 후 전체 통과 |
| Rust | Rust 소스 불변, 기존529통과 결과 재사용 |

B는 같은 문서 ID의 표/트리 검색과 원문 찾기를 서로 다른 슬롯에 둔다. 새 요청이 같은 종류의 이전 토큰만 취소하는지, 표 검색 명시 취소가 원문에 영향을 주지 않는지, 문서 취소가 모든 슬롯을 비우고 다른 문서에는 영향을 주지 않는지 검사한다. Document.line_cancel의 조회/색인 취소도 함께 확인한다.

| B·최종 검증 | 결과 |
|---|---|
| Rust fixtures required | 531통과·실패0·건너뜀0 |
| 지정 변형 | 원문 찾기를 search 슬롯에 넣어2실패·529필터 제외, 원복 후 전체 통과 |
| 프런트 | 프런트 소스 불변, A 결과 재사용 |
| 새 debug 스모크 | 44개+왕복2통과(21초) |
| 새 release 스모크 | 44개+왕복2통과(18초) |

새 스모크 검사는 없다. 사용자 결정대로 별도 화면 확인은 요구하지 않으며 clippy·다른 OS는 이번 검증에 포함하지 않았다.

## M35 — 텍스트·로그 가상 원문

생성기의 big.log는 정확히 17MiB·139,264줄이고 마지막 개행 없이 끝납니다. M34를 포함한 최종 매니페스트는 44검사·고유 파일 43개입니다. `textRawVirtual`은 표로 열린 것을 확인하고 실제 툴바로 원문 전환, 전체 줄 수·끝 스크롤·맨 위 복귀·마지막 내용 줄 검색과 강조·표 복귀를 검사합니다. 기존 reading.txt 검사는 CRLF를 정규화한 줄 내용과 줄 번호를 가상 원문 DOM에 대조합니다. 마크다운 스모크는 그대로입니다.

| 검증 | 현재 결과 |
| --- | --- |
| vitest | rebase 후 266개(25파일) 통과 |
| 지정 변형: overscan 제거 | 가시 범위 테스트 1개 실패·4개 통과, 원복 완료 |
| Rust | fixtures required 524개 통과; clippy 수정 후 관련 16개 통과 |
| 지정 변형: 마지막 LF 뒤 빈 줄 누락 | 지정 Rust 테스트 1개 실패, 원복 후 관련 16개 통과 |
| check | rebase 후 오류0·경고0, 4×426키 |
| 실제 debug 스모크 | rebase 후 44검사+왕복2 통과, 픽스처 검사 20초 |
| release 스모크 | rebase 후 44검사+왕복2 통과, 픽스처 검사 17초 |
| clippy | 중복 제외 기존 위치 21개, 신규 경고0; 변경 전 별도 실행은 하지 않음 |

Rust 경계 검사는 LF·CRLF·끝 개행·빈 파일·BOM·UTF-16·조회 범위·반환 문자열 상한·Unicode 앞뒤 순환·취소·세대 폐기를 포함합니다. Svelte 자동 분석은 TextRawView의 issues가 없었습니다. 최초 sandbox 스모크는 창과 첫 결과를 만들지 못해 종료했고 같은 바이너리를 sandbox 밖에서 실행해 통과했습니다. 제품 코드는 바꾸지 않았습니다.

main의 M34(b6bba80) 위로 rebase하면서 문서와 스모크 진입점의 양쪽 변경을 보존했습니다. Rust 소스는 앞선 검증 때와 같아 전체 테스트와 clippy 결과를 재사용했고, 바뀐 프런트는 테스트·check·빌드 및 새 debug/release 스모크로 다시 확인했습니다.

release Rust 연산을 같은 바이트에 7회 반복한 중앙값입니다. 기존 원문 열은 doc_source_text와 같은 상한 확인·UTF-8 문자열 변환 뒤 줄 분할 연산이며 IPC·DOM 시간은 포함하지 않습니다. 표 색인은 같은 Records::Lines 코드를 앞뒤에서 실행한 대조군입니다. rebase 전 실제 WebView 전체 검사 시간은 reading.txt 111ms, big.log 497ms였으며 개별 검색 지연을 뜻하지 않습니다.

| 입력 | 기존 원문 연산 ms | 새 줄 색인 ms | 끝 범위 조회 ms | 0건 찾기 ms | 표 대조군 전/후 ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| reading.txt, 677바이트 | 0.0006 | 0.0001 | 0.0006 | 0.1229 | 0.0007 / 0.0009 |
| big.log, 17MiB | 16MiB 상한으로 거절 | 0.7123 | 0.0106 | 6.2454 | 17.0515 / 17.7976 |

큰 로그의 논리적 오프셋 크기는 557,056바이트이며 Vec의 실제 용량·전체 프로세스 메모리는 별도입니다. 끝 100줄의 반환 문자열은 12,701바이트입니다. 실제 화면 확인은 긴 줄 가로 이동과 줄 번호 고정, 찾기·줄 이동, 렌더 범위 선택 복사와 마크다운 원문 유지입니다.

### M35 재검증 — 거절된 범위의 반복 요청

TooLarge로 거절된 범위는 rows가 비어도 범위 키가 바뀌기 전까지 재요청하지 않습니다. 회귀 vitest 하나가 거절 뒤 finally로 pending이 비워진 상태에서 같은 키의 미세 스크롤을 반복하고, 다른 키의 요청·옛 응답 무시·일시적 오류 재시도까지 확인합니다. 위의 스모크 결과는 최초 구현의 기록이며 이번 수정에서는 사용자 지시대로 vitest와 check만 실행했습니다.

| 재검증 | 결과 |
| --- | --- |
| vitest | 267통과·25파일, 회귀1개 추가 |
| check | 오류0·경고0·4로케일×426키 |
| cargo·스모크 | 미실행 |

## M37 — 셀 미리보기 한도와 잘림 표시

Rust는 표·JSONL·파생 표·Excel·Parquet의999/1000/1001 Unicode 문자 경계, 트리의500자 유지, SQLite·Parquet 바이너리16바이트 메타데이터를 검증한다. 프런트는 종류별 툴팁 전문·배지 조건·화면 밖 선택 보존과 새 페이지 갱신을 검사한다. 지정 변형은 빈 선택 검사를 유지하면서 배지의 잘림 조건만 반전한다. reading.txt는 생성기에서 가장 긴 줄을1293자로 늘리고 기존textReading에서 잘린 셀의title·선택배지·완전한셀선택시배지제거를 검사한다. 검사 수는 늘리지 않는다.

| 검증 | 결과 |
|---|---|
| Rust fixtures required | 529통과·실패0·건너뜀0 |
| vitest | 274통과·26파일 |
| check | 오류0·경고0·4로케일×432키 |
| 지정 변형 | 배지 조건 반전2실패·1통과, 원복 후 전체 통과 |
| 새 debug 스모크 | 44개+왕복2통과(21초) |
| 새 release 스모크 | 44개+왕복2통과(18초) |

Svelte 자동 분석은4파일에서 issues가 없었다. 처음 프런트 테스트의 언어 설정 속성 오류를 고친 뒤 지정 변형과 전체 검사를 다시 확인했다. 사용자 허용 신호 뒤 새 프런트·native 빌드와 스모크를 순차 실행했다.

## M36 — 툴바 표 너비와 즉시 적용

기존 collectionWidths 검사에서 실제 툴바 드롭다운의 스크롤·채우기를 선택하고 체크 표시·현재 탭·설정 기본값·저장 호출을 확인한다. 현재 data-width-mode와 data-fitted 뒤 스크롤은 자연 폭, 채우기는 뷰포트 폭인지1px 허용으로 비교한다. 수동 조절을 모사한 폭·비율이 전환 시 재측정되는지도 확인한다. 저장 메서드만 일시 대체하여 사용자 스토어에 쓰지 않고 finally에서 원래 메서드와 상태를 복원한다. 기존 textReading에는 표에서 메뉴 표시·원문에서 숨김 단언을 추가한다. 검사 수는 늘리지 않는다.

vitest는 현재 탭 갱신·다른 탭 불변·설정 저장 경계와 자동 상한의 scroll→fill→scroll 왕복을 검사한다. 지정 변형은 적용 함수의 save 호출만 제거하여 저장 회귀가 실패하는지 확인한다. 키 조절 회귀는8px·Shift24px·fill 이웃 흡수·scroll 기준 폭 변경·최소64px를 검사한다. 맞춤 회귀는 fill 유지·이웃 최소 폭·내용이 가용폭 초과할 때 overflow·scroll 기준 폭 갱신·한 열 채우기를 확인한다. 컬렉션 스모크에서도 실제 그립의 초점·화살표·Enter와 더블클릭의 동일 맞춤을 확인한다.

| 검증 | 결과 |
|---|---|
| vitest | 271통과·25파일 |
| check | 오류0·경고0·4로케일×427키 |
| 지정 변형 | 적용 함수의 save 호출 제거 시 저장 회귀1실패·16통과, 원복 후 전체 통과 |
| Rust 테스트 | 프런트 전용 과제 결정에 따라 생략 |
| 새 debug 스모크 | 첫 실행44개 중1실패·왕복2통과 → 하네스 보완 후44개+왕복2통과(20초) |
| 새 release 스모크 | 44개+왕복2통과(18초) |

그립의 separator 경고는 기존 Splitter와 같은 초점 가능한 값 있는 구분선 패턴으로 설명·예외 처리했다. Svelte 자동 분석도 최종 변경 파일에 issues가 없었다. 테스트 작성 중 row 보조 함수 범위와 입력값에 기대값 matcher를 넣은 오류를 고친 뒤 전체 검사를 통과했다. 사용자 허용 신호 뒤 native 빌드·스모크를 순차 실행했다. 첫 debug의 markdownReading은 HTML 수신 확인 뒤 비동기 표 후처리 전에 래퍼를 읽어 실패했다. checkStickyTables 시작에 래퍼·복제 머리글 완료 대기를 넣고 재빌드하여 다시 검증했다. 마크다운 제품 코드는 바꾸지 않았다.

## M34 — 컬렉션 격자 너비

sample.xlsx의 매출·비고 시트를 새 collectionWidths 검사 행에서 재사용한다. 매니페스트는43개 검사이며 고유 파일을 추가한 것은 아니다. 설정·탭 너비 방식·글꼴 크기·UI 배율을 명시하고 finally에서 복원한다. 실제 시트 선택기 전환 직후 기준 폭과 비율이 비워졌는지 확인하고, 선택한 시트의 통계·표본 폭·DOM 열 수·행·data-fitted 완료 뒤 머리글 폭과 뷰포트를1px 허용으로 대조한다. 각 시트의 헤더 메뉴에서 맞춤·추천·초기화가 있는지도 확인한다.

컬렉션 네 종류의 회귀는 설정 적재 전과 후의 값을 다르게 하여 공통 열기 경로가 적재 후의 너비 방식을 스냅샷으로 남기는지 검사한다. 별도 순수 회귀는 다음 시트의 페이지가 도착하기 전에 기준 폭과 비율을 함께 비우는지 확인한다. 지정 변형은 컬렉션에 설정 스냅샷을 적용하지 않도록 바꾸어 회귀가 실패하는지 검사한다.

| 검증 | 결과 |
|---|---|
| Rust | fixtures required 514통과·실패0·건너뜀0 |
| vitest | 261통과·24파일 |
| check | 오류0·경고0·4로케일×421키 |
| 지정 변형 | 컬렉션 스냅샷을 누락시켜4실패·40통과, 원복 후 전체 통과 |
| 새 debug | 43검사+왕복2 통과(20초) |
| 새 release | 43검사+왕복2 통과(18초) |

사용자의 병렬 작업 허용 신호 뒤 cargo와 스모크를 순서대로 실행했다. Svelte 자동 분석은3파일에서 issues가 없었다. clippy·Linux·macOS·사용자 화면 확인은 이번 검증에 포함하지 않았다.

## M33 — 텍스트 원문과 표 너비

| A 검증 | 결과 |
|---|---|
| vitest | 248개(24파일) 통과 |
| check | 오류0/경고0 · 4×415키 |
| 지정 변형② | supportsRaw를 prose 전용으로 바꿔1개 실패·1개 통과, 원복 후 전체 통과 |
| Rust·새 debug | fixtures required 514개 통과 · 새 debug42개+왕복2 통과(20초) |

reading.txt는 생성기가 만드는 단일 열 텍스트이며 빈 줄·긴 줄·CRLF/LF·마지막 개행을 포함한다. M33에서는 실제 툴바 버튼으로 원문을 열어 전체 문자열과 split(\n) 기준 줄 번호를 대조했다. M35에서는 같은 검사를 가상 원문 줄 DOM 대조로 바꿨다. 원문 오류는 공용 문서 오류와 분리했다.

| B 검증 | 결과 |
|---|---|
| vitest | 256개(24파일) 통과 |
| check | 오류0/경고0 · 4×420키 |
| 지정 변형① | 남는 폭을 첫 열에 몰아주어3개 실패·12개 통과, 원복 후 전체 통과 |
| Rust | Rust 무변경으로 A의 fixtures required 514개 결과 재사용 |
| 새 debug | 42개+왕복2 통과(20초) |
| 새 release | 42개+왕복2 통과(17초) |

B의 같은 TXT 검사는 저장 설정과 탭 상태를 명시하고 finally에서 복원한다. 현재 배치의 data-fitted 완료를 기다려 머리글 폭과 clientWidth를 1px 허용으로 비교한다. 실제 헤더 메뉴의 맞춤·추천이 자동420px 상한을 넘고4000px 이내인지, 초기화 뒤 자동 상한과 창 채우기가 복원되는지도 검사한다. 순수 회귀는 거터 제외·여러 열 비율·창 축소·overflow·드래그 이웃 보정·설정 적재 후 탭별 스냅샷을 검사한다. 최초 추가 테스트의 짧은 머리글 예상 폭을 잘못 정한 한 곳을 고친 뒤 지정 변형과 전체 검사를 다시 통과했다. Svelte 자동 분석은 A3파일·B5파일에서 issues가 없었으며 clippy는 이번에 실행하지 않았다. Linux·macOS와 실제 사용자 화면 확인은 별도다.

## M32 — 세션 복원·파일 연결·파일 변경 감지

A의 세션 검사는 준비된 file/url만 저장하고 아카이브 항목을 로컬 루트 파일로 합치는지, text/treeSlice/로드 중 탭을 제외하는지, 실패한 항목 뒤에서도 저장된 활성 문서를 찾는지 확인한다. 복원 중에는 저장하지 않고 시동·외부 요청을 그 뒤에 직렬 처리한다. Rust 큐는 수신 준비 전 요청 누적과 빈 --new의 복원 제외를 검사한다.

| A 검증 | 결과 |
|---|---|
| vitest | 233개(22파일) 통과 |
| check | 오류0/경고0 · 4로케일×412키 |
| 지정 변형① | text 저장 제외를 해제하여 세션 회귀1개 실패·4개 통과, 원복 후 전체 통과 |
| Rust·debug 스모크 | fixtures required 504개 통과 · 새 debug 41개+왕복2 통과(20초) |

Svelte 자동 분석은 App·SettingsPanel·Settings에 문제를 보고하지 않았으나 Session 모듈에서는 도구 내부 오류가 발생했다. 세션 모듈은 실제 Svelte 검사와 vitest로 검증했다. 최종 release 자동 검증은 아래 C 결과에 기록하고, 화면 확인은 계획 세션에 남긴다.

B는 연결 선언의 확장자가 source.rs 지원표에 포함되는지와 중복·Viewer 역할·MIME 존재를 검사하고, URL 변환은 공백·한글·#이 포함된 file URL의 경로 복원과 원격 URL 보존을 검사했다. macOS Finder 전달과 설치본의 연결 후보 등록은 이 Windows 검증에 포함되지 않는다.

| B 검증 | 결과 |
|---|---|
| Rust | fixtures required 506개 통과 |
| check·debug | 오류0/경고0 · 4×412키 · 새 debug 41개+왕복2(20초) |
| vitest | 프런트 동작 변경이 없어 A의233개 통과 결과 재사용 |
| 지정 변형② | xls 연결 추가 시 대조1개 실패(unsupported association: xls), 원복 후 전체 통과 |

## C — 파일 변경 감지와 세대 교체

실제 임시 파일 쓰기에서 등록한 문서·창으로 이벤트가 오는지, 부모 감시를 공유하고 마지막 해제 때 비우는지, rename 목적지만 통과하는지 검사했다. 원자적 파일 교체를 다시 읽어 형식을 재판별하고 사용자가 고른 인코딩을 유지한다. 실제로 만든 이전 TreeDoc/TableDoc 결과는 재열기 뒤 등록이 거절된다. 프런트는 닫힌 탭·응답 순서 역전·연속 저장·파생 탭과 모든 보기 캐시 초기화·자동 다시 읽기 해제 알림을 검사한다.

| C 검증 | 결과 |
|---|---|
| Rust | fixtures required 511개 통과, 전체 1회 |
| vitest | 243개(23파일) 통과 |
| check | 오류0/경고0 · 4로케일×414키 |
| Svelte 분석 | 변경8파일 issues 없음; 비동기 effect와 비반응 Map 권고는 확인 후 유지 |
| 지정 변형③ | generation 증가 제거 시 이전 결과 등록 거절·세대 증가 회귀2개 실패, 원복 후 전체 통과 |
| 새 debug | 41개+왕복2 통과(20초), 단계별 1회 |
| 마지막 release | 새 release 41개+왕복2 통과(18초), 마일스톤 끝 1회 |
| clippy --all-targets | 고유21개(lib18+test3), 기존 문서 기록과 같음; 신규0개 |

clippy는 처음 -D warnings로 실행해 기존21개와 새 테스트 경고1개 때문에 실패했다. 새 경고를 고친 뒤 일반 clippy는 종료0이고 기존21개를 그대로 보고했다. 로드맵의 경고0과 실제 기준이 달라 범위 밖 정리는 하지 않았다. 기준 커밋을 이번에 다시 실행한 수치는 아니다. check에서 새 테스트의 Promise 타입 누락1개를 발견해 수정했다. notify 다운로드의 첫 시도는 Windows schannel 자격증명 오류로 실패했고 실행환경을 바꾼 재시도에서 성공했다.

화면 확인은 계획 세션이 사용자에게 요청한다: main 탭의 순서·활성·mode 복원과 추가 시동 요청 활성, 설치 후 사용자가 연결 앱을 선택했을 때의 전달, 편집기 제자리 저장·rename 교체 뒤 갱신과 설정 해제 시 알림만 표시. 저장 뒤 곧 갱신하는 최선 노력이며 1초 보장과 mmap 안전성 검증이 아니다. Windows 스모크가 Linux/macOS 실행·설치본 검증을 대신하지 않는다.

## M32 재검증 수정

연속 쓰기의 디바운스 기한 연장, 세대 충돌 취소 뒤 재열기 유실, 옛 트리 비동기 응답의 오류 덮어쓰기, UI 스레드의 동기 감시 등록을 수정했다. 첫 기한 유지와 전달 후 새 기한·등록 해제를 Rust 순수 테스트로 검사하고, 형식 전환 도중 Cancelled가 와도 새 저장 이벤트 없이 다시 읽는지와 닫힌 탭에는 재시도하지 않는지를 vitest로 검사한다.

| 검증 | 결과 |
|---|---|
| Rust, fixtures required | 512개 통과, 전체1회 |
| vitest | 245개(23파일) 통과 |
| check | 오류0·경고0, 4×414키 |
| 새 debug 스모크 | 41개+왕복2 통과·20초·1회 |
| 새 release 스모크 | 41개+왕복2 통과·17초·1회 |
| Svelte 분석 | TreeView·Workspace issues 없음, 기존 비동기 effect·비반응 Map 권고 유지 |
| clippy·추가 변형 | 이번 수정에서는 미실행. 이전 고유21개 기록은 재측정하지 않음 |

TreeView는 시작 세대를 포착하고 행 조회·토글의 성공과 실패, 색인 시작 실패, 노드 위치·패널 오류 응답을 그 세대에서만 반영한다. 컴포넌트 DOM 테스트는 추가하지 않았으며, 기존 스모크 통과가 해당 지연 응답 레이스의 직접 재현은 아니다. watch_doc의 블로킹 작업 이동은 구조 변경으로 별도 단위 테스트 없이 Rust 전체와 새 바이너리 스모크로 확인했다. 두 스모크 전 dviewer.exe를 종료했고 cargo는 순서대로 실행했다. Windows 화면·다른 OS·CI 검증은 계획 세션에 남긴다.

## M32 작은 파일 메모리 적재 — 허용된 선행 검증

로컬 파일을 여는 공통 DocBytes::map_file에서 64 MiB 이하를 Owned로 읽고 초과는 Mapped로 유지한다. 경계 함수와 작은 파일·빈 파일의 Owned 결과를 검사했다. SQLite의 Mapped 보존 테스트는 작은 파일도 항상 매핑된다는 준비 코드만 직접 Mmap 생성으로 바꿨다. 재열기·감시 제품 경로는 변경하지 않았다.

| 검증 | 결과 |
|---|---|
| 지정 변형 | <=를 <로 변경하자 정확히 67108864바이트 경계1개 실패, 원복 |
| Rust, fixtures required | 514개 통과·전체1회 |
| vitest | 245개 통과·23파일 |
| check | 오류0·경고0, 4×414키 |
| debug/release 스모크·release 빌드·실측 | 사용자 화면 확인 중이므로 명시적 신호까지 보류 |

이 결과는 실제 release 앱에서 편집기 저장·Rename-Item·Remove-Item이 동작한다는 검증이 아니다. 계획 세션 신호 뒤 50 MB JSON의 열기 시간 전후와 큰 파일 대조군, 저장·이름 변경·삭제의 실제 결과를 기록한다. 변경 전 Rename/Remove 실패는 공유 모드에 따라 보장하지 않으며 관측대로 보고한다. 사용자 신호 뒤 이어지는 release 실측·스모크 결과는 작업 보고서의 작은 파일 메모리 적재 절에 기록한다.

## v0.18.0 Linux 검색 스모크 수정 — 엔진에 독립적인 종료 계약

805c568 뒤 태그 빌드에서 Linux만 M26의 `pathological regex did not time out with the UI responsive`로 실패했다는 사용자 보고를 받았다. 기존 단언은 timeout과 프레임 수 초과를 한 문장으로 묶어 실패 경로를 구분하지 못했고, `^(a+)+$`가 모든 엔진에서 반드시 시간 초과한다는 가정도 부적절했다. Linux의 실제 완료 시간이나 실패한 조건은 로컬에서 확인하지 않았다.

하네스만 수정했다. 검색 대기 중 rAF와 합성 input 이벤트 처리를 확인하고, 종료 상태는 정상 완료(0건) 또는 timeout만 허용한다. 두 경로 모두 running=false·searched=true·hits=0·current=-1·capped=false·빈 detail이어야 하며 다른 오류는 실패한다. 후속 일반 검색의 정상 결과도 확인한다. 세대 검사는 실제 Worker의 응답 콜백을 보존해 교체 질의 대기 중과 완료 후에 이전 세대의 결과를 직접 전달한다. 워커 종료나 디바운스 취소만으로 늦은 응답 검사가 통과하지 않도록 했다. Worker 생성자 대체는 finally에서 복원한다.

| 검증 | 결과 |
|---|---|
| vitest·check | 226개(21파일) 통과 · 오류 0/경고 0 · 4로케일×410키 |
| Windows 새 debug 스모크 | 1회 41개+왕복2 통과(20초), 실행 전후 dviewer 종료 |
| 생략 | 제품 코드 변경이 없어 release 스모크 생략. Linux/WebKit·Rust 테스트·clippy 미실행 |

## v0.18.0 WebKit CI 실패 수정 — 내보내기 루트 테마 계약

사용자가 전달한 run 34613132672에서 Linux·macOS release는 `styled HTML lost the inherited theme`로 실패했고 Windows는 통과했다. 기존 코드는 루트까지 계산값과 브라우저 기본값을 비교해 같으면 생략하므로 글자색·배경색·본문 글꼴의 명시를 보장하지 못했다. 다만 측정 루트에 이미 `background-color: var(--bg)`를 지정하고 있었으며 기존 메시지는 누락된 속성을 구분하지 않았다. 따라서 WebKit에서 실제로 어느 속성이 어떤 값이었는지와 투명 배경 가설은 미확인이다. WebKit은 로컬 실행하지 않았다.

제품 계약에 따라 documentElement의 계산된 --text·--bg·--font-body를 내보내기 루트의 color·background-color·font-family에 항상 명시한다. 사용자 글꼴처럼 다른 변수를 참조하는 토큰도 [CSS 변수의 계산값 규칙](https://www.w3.org/TR/css-variables-1/#defining-variables)에 따라 해소된 값을 읽는다. 후손 비교 전에 비교 루트에도 같은 인라인 값을 적용하며 후손의 기본값 생략은 유지한다. 스모크는 빠진 속성 이름을 오류에 포함하고, 세 값이 실제 토큰과 일치하는지도 확인한다.

| 검증 | 결과 |
|---|---|
| 회귀 | 측정값이 기본값과 같아 모두 생략되는 입력에서도 루트 토큰 세 값은 명시, 후손은 계속 생략 |
| vitest·check | 226개(21파일) 통과 · 오류 0/경고 0 · 4로케일×410키 |
| Windows 새 debug·release 스모크 | 각 1회 41개+왕복2 통과(debug 20초 / release 17초), 각 실행 전 dviewer 종료 |
| 미실행 | WebKit·CI 재빌드·Rust 테스트·clippy·변형 시험(이번 지시에 없음) |

## v0.18.0 릴리스 전 스모크 수정 — HTML 복사 설정 독립성

판올림 f8d6ec0에서 기본 HTML 검사가 사용자의 저장된 markdownCopyStyled를 통제하지 않는 하네스 결함을 재현했다. 같은 새 debug 빌드와 sample.md에서 스토어 true는 `HTML clipboard payload changed`로 실패하고 false는 통과했다. 생성기 재실행 전후 sample.md SHA256은 C2C368EA21A1FB67562D6F1CD057EE7D0628BE08976E051BA07C26AEA6B3F909로 같았다.

workspace는 설정 로드를 기다린 뒤 문서를 연다. 기대값은 htmlForCopy의 스타일 없는 문자열인데 제품 copyMarkdown은 저장값 true에 따라 스타일 포함 HTML을 생성한다. 양쪽 모두 tab.html을 사용하므로 복제 머리글 생성 순서는 관여하지 않는다. getType의 완료를 await하므로 미완료 페이로드를 먼저 읽은 문제도 아니다. 제품 코드는 유지하고 기본 HTML 검사에서 false를 명시한 뒤 finally에서 이전 값을 복원한다. 스타일 검사의 true/false 명시와 복원도 유지한다. 앞선 M28 최종 통과는 당시 설정 조건의 결과이며 지속 설정 독립성까지 증명하지 못했다.

| 검증 | 결과 |
|---|---|
| 수정 전 집중 재현 | 저장 true: sample.md 1개 실패 / 저장 false: 같은 파일 통과 |
| 수정 후 vitest·check | 225개 통과 · 오류 0/경고 0 · 4로케일×410키 |
| 수정 후 전체 스모크 | 새 debug 저장 true 1회: 41+왕복2 통과(20초) · 새 release 저장 false 1회: 41+왕복2 통과(17초); 각각 끔/켬을 모두 검사 |
| 스토어 보존 | 종료 후 원문 바이트 복원, 백업과 SHA256 일치 |

## M28 — 이미지 복사·스타일 HTML·표 머리글

2026-09-11 Windows에서 단계별로 새 debug 바이너리를 빌드해 확인했다. 기존 생성기 목록의 sample.md와 markdown-reading.md에 이미지·스타일 HTML·고정 머리글 검사 세 종류를 추가했으므로 픽스처 수는 41+왕복2를 유지한다. 백엔드·의존성·판번호는 변경하지 않았다.

| 검사 | 결과 |
|---|---|
| required cargo test | 502개, ①에서 1회 통과; 이후 Rust/검사 입력 불변으로 재사용 |
| vitest | 225개 통과, 기존 213개에서 12개 추가 |
| check | 오류 0·경고 0, i18n 4×410 |
| 새 debug 스모크 | 기능 단계 ①~⑤ 각각 최종 41+2 통과 |
| 새 release 스모크 | 41개+왕복2 통과, 17초; 마일스톤 끝에 1회 |
| 깨뜨려 확인: 기본값 생략 제거 | vitest 2개 실패·3개 통과, 복원 후 전체 통과 |
| 깨뜨려 확인: translateX 부호 반전 | vitest 1개 실패, 복원 후 전체 통과 |
| clippy | 이번에는 미실행; Rust 코드 불변, 이전 21개 기록을 재측정하지 않음 |

이미지 검사는 실제 복사 경로의 ClipboardItem을 가로채 SVG XML·표시 크기·font-ui 제거, PNG 시그니처/디코드, 수식 원문 TeX와 MathML annotation 일치 및 인라인 수식 제외를 확인한다. 폰트 준비·Image.decode·Blob 완료 Promise를 기다린다. 스타일 검사는 양 MIME 동일, class/data 제거, 제목·표·코드·테마 속성, 블록 선택, 실제 4MiB 초과 대체와 안내 토스트, 끈 상태의 기존 HTML 및 측정 DOM 회수를 확인한다.

머리글 검사는 MutationObserver/ResizeObserver/scroll과 실제 좌표 조건으로 고정·표 끝 숨김, 자연 폭·열 조절·초기화·가로 이동·복사 및 검색 제외를 확인한다. ⑤ 첫 실행은 신규 검사에서 복제 자체를 검색 루트로 넘겨 TreeWalker의 루트 제외 검사를 건너뛴 하네스 오류로 1픽스처가 실패했다(왕복2 통과). 실제 제품처럼 article을 색인해 복제 Text 노드가 없는지 확인하도록 고친 뒤 새 debug41+2가 통과했다. 제품 검색 코드는 바꾸지 않았다.

| 실측 | 결과 |
|---|---|
| 수식 PNG 착수 게이트 | 3수식×라이트/다크×15/22.5px=12개, 글꼴 woff2 20개, 880×180~352px, 11,176~26,635B, 가장자리 잘림 없음 |
| 실제 메뉴의 PNG | Mermaid 45,885B·수식 13,898B, 투명 여백 및 생성 이미지 시각 확인 |
| 실제 메뉴의 스타일 HTML | text/html·text/plain 각각 81,121B, 체크박스 끄기로 원래 선택 복원 |
| 실제 포인터 열 끌기 | reading 표 첫 열 41.5521→73.5521px(+32), 복제도 73.5521px |
| 가로 이동 및 고정 위치 | scrollLeft120→translateX(-120px), 복제 top=scroller top=76.6979px |
| 수식 PNG 지연 묶음 | 374.22kB(gzip265.14kB), 복사할 때만 로드 |

위 메뉴 확인은 OS 클립보드를 덮어쓰지 않고 native ClipboardItem 페이로드를 확인한 것이다. Word·그림판·웹메일 실제 붙여넣기, macOS/Linux WebKit과 CI는 이번에 실행하지 않았다. 사용자 화면에서는 이미지 붙여넣기와 두 테마의 대비, 스타일 HTML의 제목·표·코드 표현, 두 표 모드의 머리글 및 포인터 조절을 확인한다. 성능 전후 비교는 하지 않았으며 기존 읽기·복사·검색 회귀 스모크를 대조군으로 유지했다.

## v0.17.0 CI 실패 수정 — 배분 완료·목차 반올림·부분 JSONL

2026-09-11, [실패한 CI run 34485778701](https://github.com/ummoftgo/dviewer/actions/runs/34485778701)의 중단된 조사를 재개했다. 검증 규칙 d02001e에 따라 cargo와 빌드를 순차 실행하고 각 최종 검사는 한 번 수행했다. Linux WebKit 재현은 추가하지 않았다.

**표는 하네스의 초기 배분 완료 대기가 빠진 문제로 분류했다.** `enhanceTables`는 `observer.observe(viewport)` 뒤 최초 `layout()`을 호출한다. 최초 폭이 없거나 표시되지 않으면 측정을 보류하며, 초기 ResizeObserver 통지가 `schedule(layout)`으로 후속 프레임의 배분을 예약한다. 자동 fill은 매번 intrinsic 측정과 `recommendWidths`로 들어가고 자연 폭을 `fillRatios`에 저장하는 경로가 없다. 기존 하네스는 반환 직후 observer의 첫 통지보다 먼저 너비를 읽고 실패하면 handle까지 폐기했다. 반환은 초기화 완료이지 모든 배분의 정착 완료가 아니었다.

검사는 폰트 준비 후 측정하고, 제품 observer보다 늦게 등록한 검사 observer의 양수 폭 통지와 그 뒤 예약된 프레임을 기다린다. 먼저 등록된 제품 observer가 예약한 배분이 완료된 뒤 실제/기대 너비를 한 번 비교한다. 올바른 너비가 나올 때까지 단언을 재시도하지 않는다. 같은 검사에서 최초 폭을 0으로 두었다가 정상 폭으로 바꾸어, 하네스의 refresh 없이 제품이 추천을 적용하는 경로도 확인한다. 제품의 표 배분 코드는 변경하지 않았다. 실패 메시지에는 실제/기대 너비와 적용된 col 너비를 남긴다.

CI 원본에는 실제 열 너비나 observer 통지 시각이 없으므로 그 실행에서 미준비를 유발한 환경 요인까지 재현했다고 주장하지 않는다. 이번 분류의 근거는 코드의 완료 계약과 지연 초기화 경로 검증이다. 이전 Windows CPU 제한 관측에서도 최초 적용 뒤 추천이 유지됐다.

**목차는 제품의 소수점 경계 결함이었다.** 이전 WebView2 CPU 제한 관측에서 스크롤이 정착한 뒤에도 대상 제목이 inset보다 조금 아래에 남아 이전 항목이 활성화됐다. 비교에 1 CSSpx를 허용하고 관측 좌표를 회귀 검사에 넣었다. 캐시 갱신이나 스크롤 프레임의 DOM 측정은 늘리지 않았다. 스모크도 클릭 중 잠깐 해당 항목을 통과한 상태로 종료하지 않고 scrollTop 정착 후 활성 항목을 다시 단언한다.

**JSONL은 하네스가 부분 줄을 파싱한 문제였다.** CI의 `waitForListening`은 실제로 `{"ok` 조각을 읽었고, 같은 분할 쓰기로 기존 파서의 SyntaxError를 재현했다. 이제 폴링 중에만 마지막 개행 뒤 조각을 보류한다. 종료 후 읽기와 완성된 잘못된 JSON은 계속 실패한다. Node 표준 테스트는 모든 분할 지점과 부분 summary, 손상된 완료 줄을 검사하며 CI의 세 OS에서 실행한다.

| 근거·검증 | 결과 |
|---|---|
| 목차 재현의 실제 제목 top / inset | 16.052078px / 16px, 정착 후에도 이전 항목 |
| 기존 목차 회귀 기록 | 수정 전 1 실패·7 통과 → 수정 후 8 통과 |
| 기존 JSONL 가드 변형 기록 | 2 실패·1 통과, 복원 완료 |
| 이번 required cargo test | 502개 통과, 1회 |
| 이번 vitest | 213개 통과, 1회 |
| 이번 check | 오류 0 / 경고 0, i18n 4×398, 1회 |
| 이번 Node JSONL 검사 | 3개 통과, 1회 |
| 새 debug / 새 release 스모크 | 각각 41개 + 왕복 2개 통과, 각각 1회 |

이전의 변형 검증 기록을 재사용했고 이번에는 추가 스모크 변형이나 성능 실측·clippy를 실행하지 않았다. 프런트와 두 프로필 바이너리를 새로 만들어 임시 진단 게이트를 제거한 소스를 검증했으며, 각 스모크 전에 dviewer를 닫았다. 재현·변형 원시는 `.agent-works/m26-ci-*`, 이번 최종 로그는 `.agent-works/m26-resume-final-*`에 보존했다. 로컬 release 통과가 CI 재실행을 대신하지는 않는다. 화면에서는 목차 클릭 후 강조가 이전 항목으로 돌아가지 않는지 확인한다.


## M26 보완 — 도구 모음 검색

렌더·원문 공통 도구 모음의 복사 버튼 옆 검색 아이콘이 Ctrl+F와 같은 검색창·포커스 경로를 사용한다. 두 보기의 스모크에서 닫힌 상태로 시작하여 버튼 클릭 후 열림·입력 포커스·aria-pressed와 전체 툴팁/접근성 이름을 확인한다. 이어 포커스를 버튼으로 옮긴 뒤 Ctrl+F를 검사해 기존 단축키 검증도 유지한다.

| 보완 재검증 | 결과 |
|---|---|
| npm test | 212개 통과 |
| npm run check | 오류 0 / 경고 0, i18n 4×398, 사전 밖 문자열 통과 |
| 새 debug 빌드 스모크 | 41개 + 왕복 2개 통과 |
| App의 onSearch를 빈 함수로 바꾼 변형 | long-markdown.md 1개 실패, 왕복 2개 통과; 복원 후 전수 통과 |

실행 중이던 작업 폴더의 debug dviewer.exe를 종료한 뒤 프런트와 custom-protocol 바이너리를 빌드하고 루트에서 스모크를 실행했다. 로그는 `.agent-works/m26-toolbar-{red,final}-*`에 보존했다. Rust·release·clippy·실측은 이번 보완에서 재실행하지 않았으며 아래 수치는 보완 전 기록이다. 화면에서는 두 보기의 아이콘 위치·툴팁·눌림 표시와 Esc 해제, 좁은 창·높은 배율의 구별성을 확인한다.

## M26 최종 통합 검증과 실측

2026-09-10 Windows에서 프런트와 각 custom-protocol 바이너리를 새로 빌드하고, 앱을 닫은 뒤 루트에서 스모크를 실행했다. 생성기 명시 목록에 `markdown-reading.md`(32행 표·긴 목차·TS/TSX/TOML·details·수식·긴 코드 줄)와 `markdown-search-large.md`(정확히 5,242,880B)를 추가했다. 대용량 문서는 제목과 plain-text 코드 블록을 하나씩 갖고 마지막 위치에 고유 검색어를 둔다.

| 최종 검사 | 결과 |
|---|---|
| `DVIEWER_FIXTURES=required cargo test` | 502개, 세 번 모두 통과 |
| `npm test` | 212개 통과 |
| `npm run check` | 오류 0 / 경고 0, i18n 4×397, 사전 밖 문자열 통과 |
| 새 debug / 새 release 스모크 | 각각 41개 + 단일 인스턴스 왕복 2개 통과 |
| `cargo clippy --all-targets` | 서로 다른 경고 21개: 라이브러리 18 + 테스트 추가 3, 기존 문서 기록과 같음 |
| 개행 위치 수정 전 회귀 검사 | long-markdown.md 1개 실패, 왕복 2개 통과 |
| 스크롤마다 목차 캐시 무효화 변형 | 제목 rect 재조회 11,800회로 long-markdown.md 1개 실패, 왕복 2개 통과 |

개행만 검색하면 텍스트 노드가 없어 결과를 버리는 결함을 최종 통합 검사에서 재현했다. 이제 인접 텍스트 또는 텍스트가 없는 루트에 접힌 Range를 두어 건수와 이동 위치를 유지한다. 변형은 모두 복원했고 최종 검증은 복원한 코드로 수행했다. clippy의 전 값은 기존 기록이며 이번에 기준 커밋을 다시 실행한 수치는 아니다.

기존 release 바이너리와 최종 release 바이너리에서 동일 바이트 문서를 사용했다. 후처리·파싱은 워밍업 뒤 6회 중앙값이며 기존 M25 벤치마크의 `after` 모드끼리 비교했다. 파싱이 대조군이다. 이 표에서 성능 악화는 관측되지 않았으나 작은 차이를 개선율로 일반화하지 않는다.

| 문서 | 후처리 전 → 후 ms | DOM 파싱 대조 전 → 후 ms |
|---|---:|---:|
| long-markdown: 제목 200 / 코드 100 / 표 20 | 21.35 → 18.40 | 1.55 → 1.20 |
| 5MiB: 제목 1 / 코드 1 / 표 0 | 313.35 → 310.00 | 12.00 → 10.60 |

새 검색은 전 버전에 없으므로 검색 자체의 전후 개선율은 없다. 아래는 워밍업 뒤 6회 중앙값이다. 찾기 시간은 결과 수집 상한의 영향을 피한 0건 literal·regex 질의이고, 강조는 별도의 상한 미만 질의다. Worker 왕복은 시작·문자열 전달·응답을 포함하지만 UI 디바운스는 제외한다. 강조 시간은 Range 생성과 Highlight 등록이며 실제 페인트와 이동 완료 시간은 아니다.

| 문서·보기 | 텍스트 노드 | 수집 ms | literal Worker ms | regex Worker ms | literal 왕복 ms | 강조 건수 | Range·등록 ms |
|---|---:|---:|---:|---:|---:|---:|---:|
| long 렌더 | 1,874 | 2.75 | 0.15 | 0.10 | 4.05 | 200 | 0.20 |
| 5MiB 렌더 | 4 | 1.30 | 0.30 | 2.05 | 7.30 | 1 | 0.00 |
| 5MiB 원문 | 1 | 0.00 | 0.30 | 2.00 | 5.80 | 1 | 0.05 |

0.00은 타이머 분해능보다 작게 관측된 값이며 무비용이라는 뜻이 아니다. 대용량 표본은 큰 코드 내용에 대한 검사이고 같은 크기의 복잡한 다중 블록 문서 전체를 대표하지 않는다. 문서 색인 수집과 Range·페인트는 메인 스레드에 남는다.

| 제목 200개 목차 | rAF 간격 중앙값 ms | 최댓값 ms | 프레임 수 |
|---|---:|---:|---:|
| 정지 대조군 | 5.60 | 5.80 | 60 |
| 본문 스크롤 | 5.55 | 5.70 | 60 |

스크롤 중 제목 사각형 재조회는 0회였고 끝의 활성 제목도 확인했다. 측정 구간에서 추가 프레임 지연은 관측되지 않았다. 최종 release exe는 27,786,240B로 기준보다 1,006,592B(+0.960MiB) 크다. 이는 언어 확장뿐 아니라 검색·UI·네이티브 스모크를 포함한 전체 변경이며 앱 시작 시간이나 RSS 측정은 아니다.

재현 경로는 `src/lib/components/markdown/searchSmoke.ts`의 `measureSearch`·`measureLargeSearch`·`measureTocScroll`, 기존 `smoke.ts`의 `measureMarkdown`이다. 로컬 원시 로그는 `.agent-works/m26-6-final-*`·`m26-6-clippy.log`, 전후 JSONL은 `.agent-works/m26-benchmark/{before,before-large,after}.jsonl`에 보존했다. 사용자의 색상·배율·조작감 확인과 macOS/Linux 실물 실행은 남아 있다.

## M26 원문 검색

같은 `## Section` 질의가 Ctrl+E 렌더→원문→렌더 전환에서 0→200→0건으로 다시 계산되는지 실제 창에서 검사한다. 원문 source는 텍스트 노드 하나로 유지하고, 줄 번호의 line-height와 일치 건수를 대조한다. 숫자 `200`의 원문 일치 수와 DOM 검색 결과가 같아야 하므로 거터를 잘못 포함하면 실패한다. Ctrl+F 포커스, Shift+Enter로 마지막 일치 이동, 전환 뒤 이전 Range 해제도 확인한다.

| 변형 | 실제 실패 |
|---|---:|
| 원문 준비 연결 제거 | 새 debug long-markdown.md 1개 |
| 색인 대상을 거터를 포함한 scroller로 변경 | 새 debug long-markdown.md 1개 |
| App의 원문 Ctrl+F 바인딩 제거 | 새 debug long-markdown.md 1개 |

변형 복원 뒤 required Rust 502개·vitest 212개·check 오류/경고 0(4×397)·새 debug 39+2가 통과했다. 추가 검색 엔진이나 백엔드 명령은 만들지 않았다.

## M26 렌더 문서 검색

순수 검색·오프셋 테스트 21개와 검색 상태의 뷰 전환/재해석 회귀 1개를 추가했다. `long-markdown.md`의 실제 검색창에서 Ctrl+F, literal 200건·regex 2건, 대소문자, Enter/Shift+Enter 왕복, Esc 해제와 WebView2 Highlight 등록을 확인한다. 별도 네이티브 DOM 검사는 문단·셀 경계, 조작 요소·숨김·수식 중복 제외, 이모지와 인라인 노드 연결, 닫힌 details와 표 레이아웃 확정 뒤 중앙 이동, 긴 코드 줄의 가로 이동을 검사한다.

Worker 검사는 `^(a+)+$`와 32개의 a 뒤 느낌표를 실제 Worker에서 실행하여 1초 종료와 메인 프레임 응답을 확인한다. 종료 뒤 재검색, DOM 교체의 자동 색인 재생성, 도구 버튼 변경의 캐시 보존, 강조 없는 폴백, 질의 교체·해제도 검사한다. 오류는 단어가 아니라 전체 번역 문장을 단언한다.

| 제거·변경한 가드 | 실제 실패 |
|---|---:|
| 결과 상한 제거 | vitest 2개 |
| 정규식 Unicode 문자 수를 UTF-16 길이로 변경 | vitest 1개 |
| 빈 폭 일치 제외 제거 | vitest 3개 |
| 텍스트 노드 경계 `<=`를 `<`로 변경 | vitest 3개 |
| 재해석 시 질의 초기화 제거 | vitest 1개 |
| 조작 요소·수식 중복 제외 제거 | 새 debug long-markdown.md 1개 |
| 문단·셀 구분 문자 제거 | 새 debug long-markdown.md 1개 |
| details 레이아웃 안정 대기 제거 | 새 debug long-markdown.md 1개 |
| 코드의 가로 스크롤 대상 제거 | 새 debug long-markdown.md 1개 |
| Worker 제한 시간을 10초로 늘림 | 새 debug long-markdown.md 1개 |
| 본문 교체 시 색인 무효화 제거 | 새 debug long-markdown.md 1개 |
| Highlight 등록 제거 | 새 debug long-markdown.md 1개 |

변형 복원 뒤 최종 required Rust 502개·vitest 212개·check 오류/경고 0(4×397)·새 debug 39+2가 통과했다. 모든 네이티브 변형의 왕복 2개는 통과했다. 실제 화면에서 라이트·다크 강조의 가독성, 긴 결과의 가로 이동, details 개방 뒤 위치는 별도로 확인한다.

## M26 목차 현재 위치

제목 200개인 `long-markdown.md`에서 처음·중간·바닥의 aria-current, 목차 자체 스크롤, hover 억제·해제 후 재개, 키보드 포커스 억제, 항목 클릭과 테마 변경 뒤 현재 위치를 검사한다. 순수 activeHeading 테스트는 빈 목록·맨 위·중간·바닥·정확한 경계·같은 좌표·단일 제목을 다루는 7개다. 최종 required Rust 502개·vitest 190개·check 오류/경고 0(4×388)·새 debug 39+2가 통과했다. 테마 변경은 기존 후처리 수명주기가 추적기를 재생성함을 실제 창에서 확인했으므로 별도 갱신 조건을 추가하지 않았다.

| 변형 | 실제 실패 |
|---|---:|
| 이진 탐색의 `<=`를 `<`로 변경 | vitest 2개 |
| hover·포커스 억제 제거 | 새 debug 스모크 long-markdown.md 1개 |

첫 전체 스모크에서 기존 복사 버튼 호버 검사 하나가 실패했다. 진단용 포인터 이벤트 기록을 추가한 새 빌드에서는 재현되지 않았고 39+2가 통과했다. 원인은 확인하지 못했으며 제품 호버 동작은 바꾸지 않았다. 실패 시 이벤트 종류·실제/합성 여부·대상·시각이 오류에 남는다.

## M26 표 너비 추천

낱말 최소 폭·sqrt 배분·포화·균등 잉여·부족한 본문 폭·단일 열·열 순서를 순수 함수로 검사한다. 네이티브 스모크는 첫·끝 포함 20행 표본, 정상 낱말 줄바꿈, 원본 행 유지, 기본 추천, 수동 해제와 스크롤 폭 보존을 확인한다. 별도 표에서 본문 폭·글꼴을 바꿔 추천의 재계산도 검사한다. 완료 대기는 모든 열의 적용 상태를 본다. 첫 열만 기다리면 이미 포화된 열이 그대로인 정상 배분을 완료로 인식하지 못한다.

| 깨뜨려 확인 | 결과 |
| --- | --- |
| sqrt를 선형으로 변경 | vitest 3개 실패 |
| 표본의 낱말 최소 폭 무시 | vitest 4개 실패 |
| max-content 포화 처리 제거 | vitest 2개 실패 |
| 전부 포화된 뒤 균등 잉여 배분 제거 | vitest 2개 실패 |
| 20행 상한 제거 | 새 debug 스모크 sample.md 1개 실패 |
| 측정 셀에 break-all 적용 | 새 debug 스모크 sample.md 1개 실패 |
| 기본 채우기를 이전 자연 폭 비례로 복원 | 새 debug 스모크 sample.md 1개 실패 |
| 추천 버튼이 스크롤 수동 폭까지 삭제 | 새 debug 스모크 sample.md 1개 실패 |

모든 변형을 복원한 최종 결과는 required Rust 502개·vitest 183개·check 오류/경고 0(4×387)·새 debug 스모크 39+2 통과다. 네이티브 변형에서도 왕복 2개는 통과했다. 표본 밖 낱말까지 최소 폭을 보장하지 않는다. 실제 화면에서는 긴 문장과 짧은 열의 배분, 끌어서 바꾼 뒤 추천으로 복원, 본문 너비와 글꼴 변경 후 배분을 확인한다.

## M26 언어 확장 — 기존 문법 우선

기존 문법 75개의 이름·토큰이 기존 세트를 유지하고 추가 세트를 로드하지 않는지, 확장 전 Rust·JS·Python·JSON·Bash·XML·HTML·Markdown의 HTML 8종이 완전히 같은지 검사한다. 새 토큰 5종, 새 문법의 HTML 이스케이프, 첫 줄 추측, 중복·숨김 제외와 이름 왕복도 검사한다. 선택 목록은 기존 68개에서 193개로 늘어난다.

| 착수 전 실측 | 릴리스 exe 크기 | 로드 중앙값(새 프로세스 11회) |
| --- | --- | --- |
| 기존 syntect | 26,779,648B | 0.498ms |
| two-face + 기존 우선 | +0.949MiB | 두 세트 1.655ms, 확장만 1.220ms |
| 문법 파일 5개 직접 포함 | +0.032MiB | 0.416ms |

관리할 문법 파일을 늘리지 않는 two-face를 선택하되 기존 세트를 보존한다. 단순 교체는 기존 문법 48개의 출력과 대표 HTML 8종 중 5종을 바꿨다. 실측은 격리한 같은 소스·프로필 비교이며 최종 UI 전체의 시작 시간 측정은 아니다.

| 깨뜨려 확인 | 결과 |
| --- | --- |
| 기본 세트를 two-face로 교체 | Rust 4개 실패, 골든 HTML 불일치 포함 |
| 확장 문법 선택 제거 | Rust 4개 실패 |
| 목록의 중복 제외 제거 | Rust 2개 실패 |
| TSX 빠른 목록 제외 | vitest 1개 실패 |
| 드롭다운도 펜스 판별기로 해석 | Rust 2개 실패 |
| 기존 이름 선택 우선순위 제거 | Rust 1개 실패 |
| 펜스의 기존 이름·토큰 우선순위 제거 | Rust 6개 실패, fish 펜스·골든 포함 |
| 요청한 이름을 실제 적용 이름 대신 라벨에 사용 | 새 debug 스모크에서 sample.md 1개 실패, 왕복 2개 통과 |

이름 왕복 검사에서 Fish·Sass가 기존 토큰과 충돌하는 문제가 드러나 추가 승인을 받았다. 펜스는 기존 우선·지연 로드, 드롭다운은 정확한 이름 우선으로 구분하고, 라벨은 재강조 응답의 실제 이름을 사용한다. fish/sass 펜스 보존·Fish/Sass 선택·기존 75개 이름 선택 검사를 추가했다. 첫 네이티브 검사의 HTML 비교는 브라우저의 직렬화 차이를 포함했으므로, 기존 검사와 같이 DOM으로 정규화한 두 표현을 비교하도록 바로잡았다.

수정본 최종 검증은 required Rust 502개·vitest 173개·check 오류/경고 0(4×386)·새 debug 스모크 39+2 통과다. UI 확인: ts·tsx·toml·dockerfile 펜스와 빠른 언어 목록, Fish·Sass의 정확한 선택과 라벨을 확인하고, 기존 언어의 라이트·다크 강조가 유지되는지 확인한다.

## M26 선행 수정 — 첫 제목 호버의 레이아웃 보존

복사 버튼을 article 끝에 한 번만 붙이고 블록 좌표로 배치한다. 생성 전후와 다른 블록 → 첫 제목 → 다른 블록 이동에서 첫 자식·제목 위치를 검사하며, 레이아웃 변화 후 버튼 위치도 확인한다. 기존 호버 연결·클릭·숨김·해제와 키보드 선택 검사는 유지한다.

| 검사 | 결과 |
| --- | --- |
| required cargo test | 492개 통과 |
| vitest / check | 172개 / 오류·경고 0, 4로케일 × 386키 |
| 새 debug 빌드 스모크 | 픽스처 39개 + 왕복 2개 통과 |
| insertBefore 구현 복원 | 새 debug 빌드에서 sample.md의 첫 제목 위치 검사 1개 실패, 왕복 2개 통과 |

관찰자와 이미지·details·글꼴 이벤트를 프레임 단위로 모으고 해제 때 모두 회수한다. 첫 제목과 다른 블록 사이를 왕복해도 흔들리지 않는지, 글꼴·너비 변경 뒤 버튼이 선택한 블록을 따라가는지는 사용자 화면 확인 조건이다. 다른 OS와 릴리스 스모크는 M26 최종 검증에서 구분해 기록한다.

## v0.16.0 릴리스 스모크 — 닫힌 details 표의 완료 대기

[CI run 34371597314](https://github.com/ummoftgo/dviewer/actions/runs/34371597314)의 Windows·macOS는 `sample.md: markdownTables opened details table has invalid widths`로 실패했고 Linux는 통과했다. 제품의 조기 측정이 아니라 스모크의 조기 검증을 Windows WebView2에서 재현했다. 제품은 닫힌 표의 측정을 건너뛰고, `toggle`·`ResizeObserver` 뒤 예약한 프레임에서 너비를 적용한다. 스모크의 두 번째 프레임 콜백이 그 적용보다 먼저 실행될 수 있었다.

네이티브 추적에서 첫 프레임 1.0ms → ResizeObserver 1.4ms → toggle 5.1ms → 두 번째 프레임·검증 5.2ms 순서였고, 검증 시 두 열의 inline width는 빈 문자열이었다. 이후 표본에서는 둘 다 135.667px였다. 이는 적용 시각의 실측이 아니라 검증 전후 상태의 관측이다. [`toggle`은 비동기 작업](https://html.spec.whatwg.org/multipage/interactive-elements.html#the-details-element)이고 [ResizeObserver는 레이아웃 뒤 전달](https://www.w3.org/TR/resize-observer/#html-processing-model-event-loop)되므로 두 프레임 자체가 제품 콜백의 완료 신호는 아니다.

기존 스모크는 기본값이 `scroll`이면 연 뒤 `fill` 전환으로 동기 레이아웃을 실행해 경쟁을 가릴 수 있었다. 이제 닫힌 상태에서 `fill`로 맞춘 뒤 열고, 모든 열에 최초 너비가 적용됐을 때 기존 최솟값 검사를 한 번 수행한다. 완료 대기는 기존 제한 시간을 사용하며 올바른 너비가 나올 때까지 단언을 재시도하지 않는다. 제품 코드·사용자 동작은 바꾸지 않았다.

Linux CI는 Xvfb·D-Bus 안의 WebKitGTK이고 Windows는 WebView2, macOS는 WKWebView다. 같은 검사가 통과한 것은 다른 실행 순서에서 경쟁이 드러나지 않은 것으로 추정한다. CI 로그에는 프레임·toggle·observer 순서가 없어 Linux에서 어떤 콜백이 먼저였는지, 느림·포커스 중 무엇이 영향을 줬는지는 확정하지 않았다. Linux·macOS 재실행은 이번 로컬 검증에 포함하지 않았다.

| 검사 | 결과 |
| --- | --- |
| Windows release 빌드·스모크 | `npm run build` → `cargo build --release --features custom-protocol` → 루트에서 `npm run smoke -- --release`, 픽스처 39개 + 왕복 2개 통과 |
| 완료 대기를 두 프레임으로 되돌린 변형 | 닫힌 상태의 fill 고정은 유지, 새 release 빌드에서 sample.md만 실패(39개 중 1개), 왕복 2개 통과 |
| 제품의 details 열 너비를 0px로 만든 변형 | 완료 대기는 수정본 유지, 새 release 빌드에서 같은 최솟값 검사 1개 실패, 왕복 2개 통과 |
| required cargo test | 492개 × 3회 통과 |
| vitest | 172개 통과 |
| 최종 check | 오류·경고 0, 4로케일 × 386키 |
| clippy·성능 실측 | 재실행하지 않음(제품 코드 변경 없음) |

각 스모크 전에 저장소의 dviewer 프로세스가 종료됐음을 확인했다. 두 변형을 복원한 최종 소스로 다시 빌드해 통과를 확인했다. 변경 파일은 스모크와 이 검증 기록뿐이며 사용자 UI 확인 조건은 추가되지 않았다.

## M25 화면 확인 보완 — HTML 소스와 복사 버튼 호버

HTML 복사는 전체·블록 모두 `text/html`과 `text/plain`에 같은 정화 HTML 문자열을 넣는다. 일반 편집기에서도 HTML 소스가 붙고, 원문 선택만 마크다운을 넣는다. 완료 문구는 각각 “HTML 복사됨”과 “원문 복사됨”이다. 이 정책이 아래 최초 M25 검증 당시의 HTML+원문 동반 정책을 대체한다.

버튼의 투명한 CSS 영역이 본문까지의 틈을 연결한다. 양쪽을 벗어날 때만 짧게 기다려 숨기며 재진입하면 취소한다. 키보드 포커스는 유지하고 해제 시 타이머도 정리한다.

| 검사 | 결과 |
| --- | --- |
| required cargo test | 492개 × 3회 통과 |
| vitest | 172개 통과, 페이로드·완료 문구 신규 7개 |
| check | 오류·경고 0, 4로케일 × 386키 |
| 새 빌드 스모크 | 픽스처 39개 + 왕복 2개 통과 |
| 네이티브 추가 단계 | 전체/블록의 두 MIME 일치·원문 분리, 블록→틈→버튼 유지·클릭·이탈 숨김·해제 |
| 숨김 지연 / 실제 마우스 체류 확인 | 180ms / 틈·버튼 각각 260ms 뒤 opacity 1, 이탈 후 0 |
| OS 클립보드 | HTML Format·UnicodeText 존재, 평문이 고정 시험용 HTML 접두어와 일치(내용 저장 없음) |
| 페이로드를 마크다운으로 바꾼 변형 | 신규 검사 7개 중 3개 실패 |
| 즉시 숨김 / CSS 연결 영역 제거 변형 | 각각 실제 WebView 호버 검사 1개 실패, 원본 통과 |
| clippy | 이번에는 재실행하지 않음(Rust 변경 없음) |

실제 마우스로 버튼을 눌러 메뉴를 열고 HTML 복사 완료 문구를 확인했다. `.svelte` 파일 변경은 없으며 이벤트 로직과 CSS에만 호버 수정을 적용했다. 사용자는 일반 편집기의 HTML 소스 붙여넣기와 느린 블록→버튼 이동을 다시 확인해야 한다. 다른 OS·WebKit·외부 편집기의 붙여넣기는 이번에도 직접 시험하지 않았다.


## M25 — 마크다운 복사·코드 언어·표 기본값

| 검사 | 결과 |
| --- | --- |
| Rust (`DVIEWER_FIXTURES=required`) | 492개, 최종 세 번 모두 통과(기준 479 + 신규 13) |
| vitest | 165개 통과(기준 144 + 신규 21) |
| svelte-check / i18n | 오류·경고 0 / 4 × 386키 |
| Svelte autofixer | 수정한 컴포넌트·상태 모듈 issues 0 |
| 새 custom-protocol 빌드 스모크 | 생성기 픽스처 39개 + 단일 인스턴스 전달·새 창 왕복 2개 통과 |
| clippy all-target 고유 경고 | 21 → 21 |
| 새 고정 픽스처 | `long-markdown.md`: 제목 200·코드 100·표 20, `markdown-copy.md`: CRLF·이모지·원시 HTML·지원/미지원 코드·수식·Mermaid |

스모크는 위치 속성의 블록/인라인 경계, 블록·코드 후처리의 멱등성, HTML 정리의 소유 표식·표 래퍼·원래 이미지 경로, 두 ClipboardItem MIME과 실패 전파를 검사한다. 실제 강조 IPC 결과를 늦춰 이전 선택이 새 선택을 덮지 못하고 재해석 세대를 넘지 못하는지도 확인한다. Tauri의 읽기 전용 invoke를 교체하려던 첫 시험은 거절되어 작은 함수 전달 지점으로 바꿨다. 이후 직접 HTML 문자열 비교가 브라우저의 `&quot;` 직렬화 차이로 실패했으며 기대값도 DOM을 거쳐 비교하도록 바로잡았다. 제품 언어 상태와 코드 내용은 그때도 정상이었다.

| 깨뜨린 것 | 붉어진 결과 |
| --- | --- |
| 제목 절 경계 `<=` → `<` | 순수 검사 18개 중 3개 실패 |
| 정화기의 블록 `data-sourcepos` 허용 제거 | Rust 마크다운 검사 11개 중 4개 실패, 복원 후 통과 |
| HTML 정리의 소유 버튼 제거 생략 | 같은 실제 WebView DOM 검사에서 1개 실패, 원본 통과 |
| 재강조 요청 순서 가드 제거 | 실제 IPC를 지연한 WebView 검사에서 JSON 선택 뒤 Rust 응답이 반영되어 실패 |

### 실측

같은 정화 HTML과 표 기본값을 사용하고 새 복사·언어 컨트롤만 켜고 끈다. 워밍업을 제외한 교대 순서의 표본이며 HTML 파싱은 변하지 않아야 할 대조군이다. 짝수 표본의 중앙값은 가운데 두 값의 평균이다. 이번 표본에서는 추가 컨트롤의 후처리 중앙값이 늘었다. 파싱 대조군에도 변동이 있어 다른 환경으로 일반화하지 않는다. 버튼 이동은 이벤트 처리와 강제 레이아웃까지의 동기 시간을 잰 것으로 화면 합성 전체의 보장은 아니다.

| 긴 문서, Windows debug WebView2 | 기존 후처리 | 새 컨트롤 포함 |
| --- | ---: | ---: |
| 후처리 중앙값, 각 6회 | 19.85ms | 23.35ms |
| HTML 파싱 중앙값(대조군) | 1.7ms | 1.45ms |
| 블록 이동, 360회 | 해당 없음 | 중앙값 0.2ms / 최대 0.9ms |

| 스크롤바·실제 UI | 관찰 |
| --- | --- |
| WebView2 버전 | 152 |
| 밝음/어두움 × UI 100/125% | 표준 두 속성 auto, 스크롤바 공간 10px |
| 마우스가 떠난 뒤 유휴 2.5초 | 트랙·thumb가 화면에 남음 |
| 실제 마우스 드래그 | scrollLeft 0 → 1928 → 3519.33, 최대 3520 |
| 넘침 없는 표 / CSS 뷰포트 700px | 모두 공간 10px, 좁은 화면 버튼 x=8..34.4·본문 x=40 |
| 기존 thin/color 재적용 대조 | 이 환경에서는 공간 10px 유지; 과거 사라짐의 원인은 재현하지 못함 |
| 전체 HTML OS 클립보드 | HTML Format·UnicodeText 존재, CRLF·이모지를 포함한 원문 596 UTF-16 단위와 정확히 일치, 위치/조작 속성·SVG·KaTeX 없음 |
| 제목 절 OS 클립보드 | 다음 같은 급 제목 제외·끝 빈 줄 보존, LF 13개가 CRLF로 바뀌어 163 → 176 UTF-16 단위, 그 변환 외에는 정확히 일치 |
| 언어 UI | 미지원 foo → Rust에서 그 code만 변경, 원문 왕복 뒤 유지, 검색으로 Python 선택 |

CSS가 상시 조작 가능한 스크롤바 요구를 충족해 페이드·새 패키지는 추가하지 않았다. HTML 내보내기는 정화 직후 표현이고 앱 스타일·Mermaid SVG·KaTeX 결과는 보류했다. WebKit·다른 OS·릴리스 빌드·메모리 피크는 시험하지 않았다. Word/메일 등 외부 앱의 붙여넣기 표현은 사용자 화면 확인 대상으로 남긴다.


## M24 — 마크다운 본문 너비

| 검사 | 결과 |
| --- | --- |
| Rust (`DVIEWER_FIXTURES=required`) | 479개, 세 번 모두 통과 |
| vitest | 144개(기존 139 + 메뉴·표시·저장값 검사 5), 순환 시험 2개를 메뉴 구성 시험으로 교체 |
| svelte-check / i18n | 오류·경고 0 / 4 × 367키 |
| Svelte autofixer | 초기 4파일·이번 보완 2파일, issues 0 |
| 새 빌드 스모크 | 기존 픽스처 37개 + 왕복 2개 통과 |
| clippy all-target 고유 경고 | 21 → 21 |
| 메뉴 사용자 지정 행 / 현재 체크를 제거한 변이 | 각각 2개 실패, 복원 뒤 전체 통과 |

초기 순환 버전의 Windows debug WebView에서 72rem→전체→44rem→52rem 순환을 확인했다. 본문 안쪽과 12열 채우기 표의 폭은 각각 1072→1370→624→752px로 일치했고 목차는 240px를 유지했다. 표 DOM은 교체되지 않았다. 슬라이더 60rem에서 버튼을 누르면 72rem이 되고, 슬라이더 끝은 전체에 대응했다. 84rem 저장 후 앱을 재시작해 복원되는 것을 확인한 뒤 원래 52rem으로 되돌리고 저장 파일도 확인했다. 640px 창에서 넓게를 골라도 본문은 가용 폭 390px를 넘지 않았다. 고정 프레임 수만 기다리던 화면 검사는 재배분 전 값을 읽어 실패했으므로 새 설정값과 표 기하 반영을 함께 기다리도록 고쳤다. 성능 벤치마크·다른 OS·릴리스 빌드는 시험하지 않았다.

보완에서는 순환 함수와 시험을 제거하고 기존 ContextMenu로 직접 선택하게 했다. 실제 WebView에서 버튼 아래 위치, 프리셋 네 항목과 단일 체크, 사용자 지정 60rem의 다섯째 항목이 활성·checked이고 선택해도 값이 유지됨을 확인했다. 프리셋을 고르면 사용자 지정 행은 사라졌고 선택·Esc는 버튼으로 포커스를 돌려놓았으며 원문 전환은 메뉴를 닫았다. 시험 전 너비 44rem으로 복원해 저장 파일을 확인하고 테스트 앱을 종료했다.

## M23 — 마크다운 표의 열 너비와 보기 방식

| 검사 | 결과 |
| --- | --- |
| Rust (`DVIEWER_FIXTURES=required`) | 479개, 최종 세 번 모두 통과, Rust 변경 없음 |
| vitest | 139개 통과(기존 116 + 표 순수 계산 16 + 설정·탭 상태 7) |
| svelte-check / i18n | 오류·경고 0 / 4 × 360키 |
| Svelte autofixer | 변경한 컴포넌트·모듈 5개, issues 0 |
| 새 빌드 스모크 | 픽스처 37개 + 단일 인스턴스 전달·새 창 왕복 2개 |
| clippy all-target 고유 경고 | 21 → 21 |

`sample.md` 생성기에 12열 표, 본문 60행 표, 단일 열, 병합·중첩·불규칙 HTML 표를 넣었다. 기존 닫힌 details의 표도 검사한다. 마크다운 스모크는 HTML 문자열 준비 뒤 실제 DOM까지 기다리고 래퍼 중복·colgroup·손잡이 수·지원 제외·방식 전환·키보드 조절·최소 폭·이웃 보정·맞춤·초기화·모드별 값 보존·숨은 표 펼침·글꼴 변경·원문 왕복을 검사한다. 조절 가능한 표는 5개이며 스모크 매니페스트의 문서 수는 바뀌지 않았다.

| 깨뜨린 것 | 실패한 검사 수 |
| --- | ---: |
| 채우기 최소 폭 배분 가드 | 3 |
| 채우기 이웃 열 차감 | 3 |
| 표 상태 초기화 / 로드한 기본값 적용 / 설정 로드 캐시 / 저장값 검증 | 각각 1 |
| DOM 중복 후처리 가드 | 스모크 37개 중 1 |
| 초기화의 스크롤 수동 너비 제거 | 스모크 37개 중 1 |

변이마다 복원했고 마지막에는 프런트와 바이너리를 다시 빌드한 뒤 스모크를 통과했다. DOM 변이는 각각 `table enhancement was not idempotent`, `reset changed the mode or kept manual widths`로 실패했다. 단위 시험은 미구현 상태에서 신규 13개 실패·3개 통과로 시작했다.

Windows debug WebView에서 실제 포인터를 40px 움직였을 때 스크롤 표는 전체 폭이 40px 늘고 다른 열은 유지됐다. 채우기 표는 이웃 열이 40px 줄고 전체 752px를 유지했다. 글꼴 15→20px 변경은 DOM과 열 비율을 보존했고, 테마 재렌더는 DOM을 바꾸면서 조절값을 복원했다. 640px 화면에서 본문 표 영역은 310px, 12개 열은 각각 48px였으며 도구가 본문 안·표 위에 남았다. 성능 전후 벤치마크는 하지 않았다. macOS·Linux·릴리스 빌드·실제 화면 읽기 프로그램·인쇄·모든 배율 조합은 시험하지 않았다. 사용자 화면 확인 조건은 `.agent-works/20260907-report-m23.md`에 있다.

## M22 — 업데이트 확인과 자기 갱신

| 검사 | 결과 |
| --- | --- |
| Rust (`DVIEWER_FIXTURES=required`) | 479개, 세 번 모두 통과(기준 445 + 신규 34) |
| vitest | 116개 통과(기준 113 + 신규 3) |
| svelte-check / i18n | 오류·경고 0 / 4 × 353키 |
| 새 빌드 스모크 | 픽스처 37개 + 단일 인스턴스 전달·새 창 왕복 2개 |
| clippy all-target 고유 경고 | 21 → 21, 업데이트 신규 0 |
| 실제 Windows 갱신 | 포터블·NSIS 모두 0.13.0 → 0.14.0, 파일·URL 원본 2개 재열림 |

매니페스트·버전·호스트·서명·실제 수신량·취소·형태·파일 잠금·백업·상태 역전과 중복 작업을 검사했다. 실제 Tauri CLI 서명의 정상 파일 수용과 변조 거절도 통과했다. MSI·macOS·Linux 설치 시험은 하지 않았으며 이 형태의 설치 코드는 없다. Windows 시험은 별도 제품명·식별자·시험키를 쓴 개발 번들이다. 공개 릴리스·실제 GitHub Actions 실행·배포 키 등록은 하지 않았다.

| 깨뜨린 것 | 실패한 검사 수 |
| --- | ---: |
| URL 호스트 가드 | 3 |
| 서명 finalize 생략 | 3 |
| 선언 길이·실제 수신량 상한 제거 | 2 |
| 원본 인자·PE 형태·정리 범위 | 3 |
| 상태 중복·키 없음·노트 길이·변경 번호 | 4 |
| 프런트 응답 역전·늦은 구독 해제·건너뛰기 표시 | 3 |
| CI 자산 크기 상한 | 1 |
| NSIS 전체 인용·명령줄 상한 | 2 |

모두 복원 후 통과했다. self-replace가 없는 후보를 복사하려다 기존 exe 경로를 잃는 실패도 별도 복사 바이너리에서 재현한 뒤 백업 복원으로 고쳤다. 실제 NSIS 첫 시험에서는 버전만 바뀌고 원본 탭이 사라졌다. 재실행 명령줄과 NSIS `GetOptions`를 대조해 바깥 따옴표 제거를 확인했고 수정 뒤 두 원본의 탭과 본문이 다시 열렸다. 실제 앱에서 변조 매니페스트는 `updateBadSignature`로 거절됐고, 528,384바이트를 받은 중간 취소는 버전을 유지한 채 대기로 돌아왔다.

Windows debug, 정적 loopback 매니페스트·실제 minisign 서명, 조건별 세 번의 중앙값이다. 파일 크기만 바꾸고 **파일을 받지 않는 확인**을 대조군으로 두었다. 메모리는 프로브 프로세스의 PeakWorkingSet을 10ms 간격으로 읽었다. 짧은 확인도 고수위값을 읽을 수 있도록 예제가 측정 종료 뒤 100ms 기다리며 표의 시간에는 넣지 않는다.

| 자산 크기 | 작업 | 시간 중앙값(ms) | 메모리 중앙값(MiB) | 최대(MiB) |
| --- | --- | ---: | ---: | ---: |
| 1MiB | 확인만(대조군) | 10.93 | 17.84 | 17.84 |
| 1MiB | 확인·다운로드·검증 | 37.77 | 17.06 | 17.89 |
| 100MiB | 확인만(대조군) | 10.89 | 16.80 | 16.95 |
| 100MiB | 확인·다운로드·검증 | 2226.05 | 17.05 | 17.05 |

전체 최댓값은 17.89MiB이며 100MiB 전문을 모으지 않는다. 웹뷰를 포함한 앱 전체 메모리나 인터넷 다운로드 속도를 뜻하지는 않는다. 초기에 서버가 요청마다 서명하던 측정은 서명 CLI 비용이 섞여 제외했고, 정적 파일을 제공하는 서버로 다시 측정했다.

### 업데이트 끝까지 시험

배포키와 격리된 시험이다. 긴 명령은 백그라운드로 실행하고 결과를 파일에 남긴다. 먼저 실제 dviewer를 닫고 다음 스크립트로 두 버전의 개발 포터블·NSIS를 만든다. 버전·공개키·제품명·식별자는 설정 오버레이로만 주입한다.

```powershell
node scripts/test-updater-windows.mjs 0.13.0 0.14.0
node scripts/serve-updater-test.mjs
```

산출물은 `.agent-works/m22-e2e`이고 서버 주소는 그곳의 `server.json`에 기록된다. 시험키는 OS 임시 디렉터리에 있으며 내용은 출력하지 않는다. 서버는 별도 터미널/백그라운드로 유지하고 앱을 띄우는 셸에서 다음 환경을 설정한다.

```powershell
$probe = Join-Path $PWD '.agent-works/m22-e2e'
$server = Get-Content "$probe/server.json" | ConvertFrom-Json
$env:DVIEWER_UPDATE_MANIFEST = "$($server.origin)/latest.json"
$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS = '--remote-debugging-port=9231'
```

1. 포터블 0.13.0 exe를 별도 디렉터리의 `dviewer.exe`로 복사해 실행한다. 설정의 배포 형태가 포터블인지, 시작 화면의 배지가 나오는지 확인한다.
2. 생성된 `원본 문서.txt` 파일과 서버의 같은 문서 URL을 연다. 지금 확인→배지→지금 업데이트를 누른 뒤 앱이 0.14.0으로 다시 시작하고 두 원본의 탭·본문이 나타나는지 확인한다. 교체된 exe의 SHA-256은 서명한 `portable-0.14.0.exe`와 같아야 한다.
3. 포터블 시험 앱을 닫고 `setup-0.13.0.exe /S`로 별도 제품 **dviewer M22 test**를 설치한다. 기본 설치 경로는 `$env:LOCALAPPDATA/dviewer M22 test`이다. 그 앱에서 같은 두 원본을 열어 같은 갱신을 반복하고 NSIS 형태·버전·재열림을 확인한다. `/D`를 쓰면 Windows 역슬래시 경로로 전달하고 실제 설치 경로를 확인한다.
4. 자동화할 때는 `agent-browser --session m22 --cdp 9231 snapshot -i`로 실제 버튼을 찾는다. `eval 'window.__TAURI_INTERNALS__.invoke("plugin:app|version")'`는 실행 중인 앱의 버전을 읽는다. PowerShell에서는 참조를 `'@e2'`처럼 인용한다. `scenario.json`의 `tamper: true`는 서명한 매니페스트를 변조하고 `slow: true`는 취소 시험을 위해 파일 전송을 늦춘다. 시험 뒤 `{}`로 되돌린다.
5. 서버를 유지한 채 `./scripts/measure-updater.ps1`을 실행하면 1MiB·100MiB 및 확인 대조군을 각 세 번 측정해 `perf-results.json`에 남긴다. 이 예제의 로컬 URL은 debug에서만 허용된다.
6. 시험 앱과 서버를 닫고 시험 제품의 `uninstall.exe /S`로 제거한다. `node scripts/test-updater-windows.mjs --cleanup-keys`로 시험키를 제거한다. 시험 설정은 `com.xenia.dviewer.m22test` 식별자만 정리하며 일반 dviewer 설정은 건드리지 않는다. 개발용 환경변수를 지우고 일반 프런트·`cargo build --features custom-protocol`을 다시 빌드한 뒤 스모크를 돌린다.

## M20 — 서브탭과 머리글 메뉴

| 검사 | 결과 |
| --- | --- |
| Rust (`DVIEWER_FIXTURES=required`) | 445개, 세 번 모두 통과 |
| vitest | 113개 통과 |
| svelte-check | 오류 0, 경고 0 |
| i18n | 4 × 323키 일치, 사전 밖 문자열 검사 통과 |
| 빌드 후 스모크 | 픽스처 37개 + 단일 인스턴스 전달·새 창 왕복 2개 |
| clippy | lib 18 / all-target 고유 21, 이전과 같음 |

가족·고아·순환·노드 라벨, 자식 먼저 닫기와 열리는 자식 회수, 활성·비활성 자식 닫기의 부모 복귀를 확인했다. 열 필터는 다른 열의 값 제외·정렬 열 합집합·범위 밖 열 거절을 Rust에서 검사하고, sample.csv 스모크에서도 지정 열에 따른 결과와 복사·검색 좌표를 확인한다. 같은 행 수의 정렬을 시작한 뒤 탭을 왕복하는 테스트는 성공한 상태와 재조회 revision을 검증한다. 실제 DOM 갱신과 입력 칩·키·메뉴·세대 스타일은 사용자 화면 확인 대상이다.

깨뜨려 확인은 가족 처리 초기 누락에서 4개, 자식 제거 누락에서 2개, 필터 열 무시·검증 제거에서 3개가 실패했다. 상태·너비 복합 변이는 4개, 개별 revision 제거는 1개, 늦은 응답 가드 제거는 1개, 정렬 상태 대입 제거는 2개, 배열 라벨 변이는 3개가 실패했다. 비활성 자식 닫기는 기존 구현에서 1개가 실패한 뒤 수정했다. 전부 복원·수정 후 전체 검사와 새 빌드 스모크를 통과했다.

grid-cases.json 생성기에 점·대괄호·따옴표 키, 빈 키, 배열 안 컨테이너, 다른 열에 같은 값이 있는 필터 조합을 추가했다. 별도 성능 실측은 하지 않았으며 M19의 메모리 상한·순차 처리·Parquet 정렬 제한을 유지했다.

```bash
cd src-tauri && cargo test          # 479개: 문서 엔진과 서명·업데이트 상태·설치 경계
npm test                            # 144개: 프론트의 순수 모듈과 상태 로직
npm run smoke                       # 진짜 창에서 픽스처 전수 (아래)
```

## 프론트 테스트

`npm test` 는 vitest 를 돌립니다. 테스트는 검사하는 코드 **옆에** 삽니다(`src/**/*.test.ts`) — Rust 쪽 479개가 소스 옆 `#[cfg(test)]` 모듈에 있는 것과 같은 방식입니다.

**러너를 vitest 로 고른 이유는 하나뿐입니다: 이 저장소의 `vite.config.ts` 를 그대로 쓴다는 것.** 그래서 테스트가 앱과 **같은 방식으로 컴파일**되고, `.svelte.ts` 의 runes 가 추가 설정 없이 돕니다. 그것이 상태 로직을 테스트할 수 있게 하는 전부입니다. 설정은 `vitest.config.ts` 로 분리했습니다 — `vite.config.ts` 는 Tauri 빌드가 읽는 파일이라 테스트 전용 설정으로 불리지 않는 편이 안전합니다.

무엇을 덮는가:

- **순수 모듈**: `virtual.ts`(압축 스크롤 기하 — 상한 양쪽, 왕복, 0행·1행·높이 0), `source.ts`(탭이 같은 문서를 가리키는가), `archive/tree.ts`(목록 모양과 거르기), `grid/columns.ts`(문자 폭), `format.ts`, `keys.ts`.
- **상태 로직**: `docs.svelte.ts` 의 중복 방지 셋(파일·투명 해제·항목 재클릭), 여는 중 닫힌 탭의 문서 회수, `invalidate()` 의 리셋 목록, 검색 세대(seq) 가드, 빈 탭 규칙. `history.svelte.ts` 의 브라우저 의미.

**전부 과거에 실제로 났던 버그를 재현하는 형태입니다.** 그리고 각 항목이 정말 회귀를 잡는지는 그 자리를 일부러 깨뜨려 확인했습니다 — 아홉 군데(여는 중 닫힘 가드, 투명 해제 절, 번호 대신 이름 비교, 선택 노드 리셋, seq 증가, 거르기의 접힌 폴더 열기, 스페이서 상한, 넓은 문자 범위, 방문 중복 가드) 전부 테스트가 잡았습니다. 잡지 못하는 회귀 테스트는 테스트가 아닙니다.

**IPC 는 `vi.mock("../ipc")` 로 가립니다.** Tauri 의 `mockIPC` 는 `window.__TAURI_INTERNALS__` 를 갈아 끼우는 구현이라 DOM 환경이 필요한데, 그 의존을 목킹 편의로 들일 값이 아닙니다. 그리고 `ipc.ts` 가 이미 "백엔드와의 계약을 한 파일에 모은다"는 목적으로 존재하므로, 테스트가 가리는 경계도 거기가 맞습니다.

**컴포넌트 렌더 테스트(jsdom)는 들이지 않았습니다.** 가짜 DOM 에서 컴포넌트를 조립하는 것보다, 이 앱의 결합 구조(IPC·가상 스크롤)에는 진짜 창에서 진짜 IPC 로 도는 검증이 맞습니다. `test/setup.ts` 의 `localStorage` 스텁 열두 줄이 그 결정을 지키는 값입니다 — 여기에 두 번째 스텁이 필요해지면, 그건 그 상태 모듈이 상태이기를 그만두고 뷰가 되었다는 신호입니다.

## 스모크 하네스

`npm run smoke` 는 **앱을 실제로 띄워** 픽스처를 전부 열고, 어디까지 갔는지 기록합니다. 드라이버도 WebDriver 도 쓰지 않습니다 — 하네스가 곧 앱입니다(`--smoke` 플래그).

### 고전적 E2E 를 쓰지 않은 이유

이 저장소에서 **유닛 층을 빠져나간 결함의 실제 이력**을 보면 답이 나옵니다: 릴리스 빌드 한정 크래시(로그 빈 필드), 이벤트 루프 자기-블록 둘(`--new` 빈 창, 패널 검은 화면), capability 배선(opener 링크 전체 불능). 넷 다 **"띄우고 열면 터지는"** 계급이고 복잡한 상호작용이 필요 없었습니다. 반대로 사람 화면 확인 수십 항목이 잡은 상호작용 결함은 0건입니다 — 그 로직은 리뷰와 유닛 층이 먼저 잡았습니다.

WebDriver E2E 는 vitest 2층과 겹치는 것을 비싸게 다시 사면서(드라이버 드리프트·macOS 미지원·Linux webview ≠ WebView2·플레이크 유지비) 정작 위 계급을 더 잘 겨냥하지도 못합니다. 상호작용 회귀가 **실제로** 유닛 층을 빠져나가기 시작하면 그때 재론합니다.

### 모양

- **계획은 픽스처 옆에.** `gen-fixtures.mjs` 가 `fixtures/smoke.json` 을 함께 씁니다 — 무엇을 왜 만들었는지 아는 것은 생성기뿐이고, 기대값 없이 추가된 픽스처는 그 자리에서 눈에 띕니다(생성기가 없는 파일을 가리키면 오류로 알립니다).
- **`expect` 는 뷰 이름 한 칸**이거나 `error` 입니다. 그 한 칸이 아카이브의 두 동작을 가릅니다 — `single.zip` 은 문서 하나라 투명 해제되어야 하므로 `tree`, `single-locked.zip` 은 그 하나가 거절되어 목록으로 후퇴해야 하므로 `archive`.
- **결과는 JSON Lines, 한 줄씩 flush.** 끝에 한 번 쓰는 단일 문서는 이 하네스가 겨냥하는 바로 그 결함에서 빈 파일이 됩니다. 프로세스가 죽어도 **마지막 줄이 범인을 가리킵니다.** 요약 줄이 완주 표시이고, 그 부재가 곧 미완주 판정입니다.
- **시간 상한은 바깥에서.** 겨냥한 결함 셋 중 둘이 이벤트 루프가 안 도는 것이고, 안에 둔 타이머는 그 루프와 함께 멈춥니다.
- **종료 코드**: 0 전부 기대대로 · 1 하나 이상 어긋남 · 2 하네스가 시작하지 못함(매니페스트를 못 읽음, 결과를 못 씀).

### 무엇을 지나가는가

매니페스트의 37개를 **정상 열기 파이프라인 그대로** — `workspace.openPath` 를 부르고, 진짜 뷰가 마운트되어 스스로 로드하기를 기다립니다. 그 위에 파일 하나로는 닿지 않는 단계:

- `archive.zip` → `open_entry` 커맨드로 항목 열기 (클릭이 아니라 그 클릭이 부르는 것)
- `sample.csv` → 머리글 토글 뒤 검색 상태 초기화, 숫자 내림차순, 다른 열에서는 일치하지 않는 지정 열 필터, 원본 좌표 복사·표시 좌표 검색. 토글의 IPC와 상태 초기화를 하네스에서 호출하며 버튼 클릭 자체를 검증하지는 않습니다.
- `small.json` → `items`를 표로 펼쳐 컬렉션 뷰·열·값·0번 행을 확인하고, 재클릭 중복 방지와 부모를 닫은 뒤 자식 `grid_rows`의 `noSuchDoc` 거절을 검증합니다.
- `grid-cases.json` → 배열·맵·혼합·빈 값·긴 키·긴 값 조합의 화면 확인용 픽스처입니다.

그중 둘은 압축 **안**의 형식을 겨냥합니다. `workbook.zip` 과 `columnar.zip` 은 각각 xlsx 하나·parquet 하나만 든 zip 이라 투명 해제되고, 따라서 탭에 떠야 하는 것은 압축 목록이 아니라 시트 목록과 컬렉션입니다. 한때 `NeedsFile` 로 거절하던 자리이므로, 기대값 `collection` 이 곧 그 거절이 사라졌다는 진술입니다.

그리고 **단일 인스턴스 왕복** — 두 번째 프로세스를 실제로 띄워 ① 스스로 종료하는가 ② 첫 프로세스에 인자가 도착했는가, 그리고 `--new` 로 한 번 더 ③ 새 창이 정말 부팅해서 준 파일을 열었는가 ④ **그 창을 닫으면 그 창의 문서가 회수되는가.** 세 번째가 `--new` 빈 창 결함의 직접 재현이고, 네 번째는 단위 테스트가 닿을 수 없는 자리입니다 — 창 수명은 Tauri 이벤트 처리기 안이라 창이 실제로 파괴되어야 돌아갑니다. 그래서 그 창은 `smoke_done` 으로 나가지 않고 **스스로 닫고**, 처리기가 회수 결과를 한 줄 남기며 실행을 끝냅니다.

### 잡는지 확인했습니다

vitest 때와 같은 절차로, 두 가지를 일부러 깨뜨렸습니다: 매니페스트의 기대값을 틀리게 바꾸자 `sample.md: ready (prose 로 열림, tree 를 기대)`, `detect_kind` 에서 `md` 를 빼자 `sample.md: ready (table 로 열림, prose 를 기대)`. 둘 다 어느 픽스처가 어떻게 어긋났는지 이름을 댑니다.

### 프로세스 위생

스모크 프로세스는 플러그인 둘을 다르게 답니다. `window-state` 는 **양쪽 모드 모두 빼고** — 안 그러면 스모크가 사용자의 창 위치를 덮어씁니다. `single-instance` 는 **모드마다 반대로**입니다: 전수 열기는 빼야 하고(dviewer 가 떠 있으면 스모크가 그 창에 픽스처를 배달하고 죽습니다), 듣는 쪽은 있어야 합니다(단일 인스턴스가 되는 것이 시험 대상이므로). 그래서 듣는 쪽은 dviewer 가 이미 떠 있으면 성립하지 않고, 러너가 그 경우를 알아보고 그렇게 말합니다.

`--smoke` 는 **배포 바이너리에 남습니다.** 빌드 기능으로 가르면 배포된 것과 시험한 것이 다른 바이너리가 되어 이 하네스의 논거 절반이 사라집니다. 대신 `--smoke` 와 `--smoke-out` 둘 다 명시해야만 동작하고 기본값이 없어, 실수로 켜질 일도 임의 경로에 파일이 생길 일도 없습니다.

### CI

**태그 빌드(번들 잡)에서 릴리스 바이너리로 세 OS 전부** 돕니다. 검사 잡이 아닌 이유는 둘입니다 — 검사 잡은 앱 바이너리를 아예 만들지 않아 통째로 빌드가 하나 들어가고, 그건 디버그 빌드라 **이 하네스가 겨냥하는 "릴리스 빌드에서만 나는 크래시" 계급은 어차피 못 잡습니다.** Linux 러너는 화면이 없어 `xvfb-run` 으로 감쌉니다. 태그까지의 공백은 릴리스 절차의 "판올림 전 로컬 `npm run smoke` 1회"가 메웁니다. 검사 잡 편입은 디버그 빌드 시간을 CI 에서 실측한 뒤 결정합니다.

**검사 잡은 `cargo test` 앞에 픽스처를 만듭니다.** 그전까지는 만들지 않았고, 그래서 `xlsx` · `parquet` 의 **열한 개가 세 러너 모두에서 한 번도 돌지 않았습니다.** 그 테스트들은 픽스처가 없으면 `return` 으로 빠져나가 — 막 클론한 사람이 Node 를 깔기 전에도 `cargo test` 를 할 수 있게 하려는 거래입니다 — 아무것도 단언하지 않은 채 성공을 보고했습니다. 픽스처가 없으면 **통과하는** 테스트는 CI 에서는 테스트가 아닙니다.

두 읽기 다 필요하므로 어느 쪽인지를 짐작하지 않고 말하게 합니다. **`DVIEWER_FIXTURES=required`** 가 설정되어 있으면 픽스처 부재가 패닉이고(`src-tauri/src/testing.rs`), 없으면 지금처럼 조용히 비킵니다 — 로컬 동작은 그대로입니다. 검사 잡의 `cargo test` 에만 그 변수를 겁니다. Parquet 픽스처를 쓰는 예제는 번들 잡과 달리 `--release` 없이 돌립니다 — 이 잡에는 재사용할 릴리스 빌드가 없고, dev 프로필이면 바로 뒤 `cargo test` 가 컴파일할 것을 나눠 씁니다. 그리고 Node 가 세 러너 모두에 있어야 합니다 — 생성기가 `node:sqlite` 를 쓰므로 22.5+ 가 필요하고 러너 기본은 20 입니다.

잡는지도 두 방향으로 확인했습니다. `fixtures` 를 지우고 `DVIEWER_FIXTURES=required` 로 돌리면 **12개**가 붉어집니다 — 그 열한 개와, 같은 정책을 따르게 만든 헬퍼 자신의 테스트 하나. 변수 없이 같은 상태로 돌리면 전부 통과합니다.

`scripts/check-i18n.ts` 는 스크립트로 남습니다. 단언이 아니라 저장소 전체를 훑는 린트라 러너에 넣을 이유가 없습니다. 다만 사전 밖 문자열 스캔에서 `*.test.ts` 는 제외합니다 — 테스트 안의 한글은 화면 문자열이 아니라 **시험 대상 픽스처**(CP949 아카이브 이름, 한글로 재는 열 폭)이고, 사전 키를 요구하면 인코딩 경로를 영어로 시험하게 됩니다.

성능은 창 없이 직접 잽니다:

```bash
node scripts/gen-fixtures.mjs --huge
cd src-tauri
cargo run --release --example scan  -- ../fixtures/huge.json "needle:"
cargo run --release --example scan  -- ../fixtures/huge.xml  "항목 1499999"
cargo run --release --example jsonpath -- ../fixtures/huge.json
cargo run --release --example table -- ../fixtures/huge.csv  "항목 3999999"
cargo run --release --example table -- ../fixtures/huge.log  "id=1999999"
cargo run --release --example table -- ../fixtures/huge.jsonl "항목 1999999"
cargo run --release --example sqlite -- ../fixtures/huge.sqlite events
cargo run --release --example parquet -- read ../fixtures/huge.parquet "항목 1999999"
cargo run --release --example xlsx -- ../fixtures/huge.xlsx "9928527"
cargo run --release --example archive -- ../fixtures/archive.zip
```

이 저장소를 만든 기계(Windows 11)에서:

| 항목 | JSON 526MB | XML 184MB | CSV 298MB | 로그 208MB | JSONL 438MB |
|---|---|---|---|---|---|
| 규모 | 3,840만 노드 | 1,050만 노드 | 400만 행 × 12열 | 200만 행 × 4열 | 200만 행 × 7열 |
| 인덱싱 | 1.26초 (418 MB/s) | 0.97초 (190 MB/s) | 0.36초 (818 MB/s) | 0.21초 (1,004 MB/s) | 0.36초 (1,234 MB/s) |
| 인덱스 메모리 | 1.3 GB | 360 MB | 16 MB | 8 MB | 8 MB |
| 전체 펼치기 | 29ms | 7ms | — | — | — |
| 100행 조회 | 0.0ms | 0.0ms | 0.1ms | 0.0ms | 0.1ms |
| 본문 검색 | 1.16초 (48,000건) | 0.02초 | 0.05초 | 0.09초 | 0.02초 |
| 경로 검색 | 1.68초 | 0.51초 | — | — | — |
| 정규식 검색 | 1.73초 | — | 1.48초 | — | — |
| JSONPath (`$..notes`) | 0.06초 | — | — | — | — |

JSONPath 가 리터럴 검색보다 빠른 것이 이상해 보이지만 당연합니다 — 표현식은 단계마다 좁혀지므로 파일이 아니라 고른 것의 크기에 비례하고, 바이트를 훑지 않습니다. 이 값은 `..` 와 뒤따르는 단계를 하나로 읽은 뒤의 것으로, 그러기 전에는 2.26초였습니다(서브트리 전체를 모았다가 거르는 형태였습니다).

**선택자마다 값이 다릅니다.** 같은 파일(527MB·3,840만 노드)에서 `cargo run --release --example jsonpath -- ../fixtures/huge.json`, 세 번 돌려 안정된 값:

| 표현식 | 값 | 무엇을 치르는가 |
|---|---|---|
| `$.items[0]` | 16~21µs | 첫 자식 |
| `$.items[1000000]` | 5.7~6.0ms | 그 앞 백만 자식을 밟는다 |
| `$.items[-1]` | 14.0~14.8ms | 240만 자식 전부 |
| `$.items[1000000:1000010]` | 5.6~5.9ms | 열 개를 골라도 걷기는 한 번 |
| `$.items[0,1000000,-1]` | 19.3~19.8ms | 조각마다 한 번씩 |
| `$.items[?@.id > 2399990]` | **272ms** (M18 전 366) | 240만 후보에 질문 |
| `$..[?@.id > 2399990]` | **1.87초** (M18 전 2.62) | 3,840만 후보에 같은 질문 |
| `$..notes` | 65ms | 필터가 없어 M18 이 건드릴 것이 없다 |
| `$.items[?match(@.slug, 'item-23999[0-9]')]` | **322ms** (M18 전 531) | 240만 후보에 정규식 |
| `$.items[?length(@.slug) > 12]` | **310ms** (M18 전 458) | 정규식 없이 문자열만 — 0건이라 수집이 섞이지 않는다 |
| `$.items[?count(@.tags[*]) == 3]` | **386ms** (M18 전 522) | 후보마다 질의를 하나 더 |
| 취소 반응 | 244~275µs | 멈추라는 말에서 멈춤까지 |

읽히는 것 셋입니다. **뒤쪽에 닿는 것은 걷기**입니다 — 자식이 색인에서 이어 붙어 있어 n번째에 가려면 그 앞을 밟고, 그 행까지 스크롤하는 것과 같은 값입니다. **슬라이스는 그 걷기를 한 번만** 합니다(`[1000000:1000010]` 이 `[1000000]` 과 같은 값입니다). 그리고 **필터는 답이 아니라 파일을 따라갑니다** — 같은 아홉 건을 `$.items` 아래에서 0.35초, `$..` 아래에서 2.5초에 찾습니다. 그것이 이 한 단계에만 취소가 붙어 있는 이유이고, 4,096 노드마다 깃발을 보는 값이 0.3ms 입니다. **함수는 그 위에 얹힙니다** — 같은 240만 후보에 값 하나를 읽는 비교가 0.27초, 정규식이 0.32초, 후보마다 질의를 하나 더 푸는 `count()` 가 0.39초입니다. 정규식은 미리 컴파일해 두므로 여기서 재는 것은 컴파일이 아니라 맞춰 보는 값입니다.

**M18 이 후보당 할당을 걷어냈습니다**(괄호 안이 그 전 값입니다). 둘이었습니다 — 함수가 값을 `'static` 으로 돌려주려고 **문자열을 후보마다 복사**한 것, 그리고 `select_from` 이 단계마다 노드 id 목록을 **새로 만든** 것. 앞의 것은 수명을 꿰뚫어 없앴고(`length` 458 → 399ms), 뒤의 것은 버퍼를 돌려 쓰게 했습니다(기준 366 → 272ms).

**어느 행이 얼마나 줄었는지가 무엇을 고쳤는지 말해 줍니다.** `$..` 아래 필터가 가장 크게(-29%) 줄어든 것은 같은 질문을 3,840만 후보에 하므로 후보당 할당을 가장 여러 번 치르기 때문이고, `$..notes` 가 그대로인 것은 필터가 없어 후보당 반복 자체가 없기 때문입니다. 움직이지 않아야 할 행이 움직이지 않은 것이, 움직인 행의 숫자를 믿을 수 있게 합니다.

숫자들이 서로 다른 이유가 설계를 설명합니다. 로그와 CSV가 가장 빠르고 가벼운 건 레코드 시작 위치만 기록하기 때문이고(행당 4바이트), 로그가 그중에서도 빠른 건 따옴표 상태를 따라갈 필요가 없어 개행만 세면 되기 때문입니다. 로그의 열은 색인을 키우지 않습니다 — 배치는 앞부분 표본에서 한 번 정해지고, 칸 나누기는 화면에 보이는 행에서만 일어납니다, XML이 바이트당 가장 느린 건 같은 바이트 수에 노드가 훨씬 빽빽하기 때문입니다(마크업 18바이트당 노드 하나 대 JSON의 14바이트). 트리 인덱스 메모리는 파일 크기가 아니라 **노드 수**에 비례합니다(노드당 36바이트).

XML 경로의 위치 술어 `[n]` 은 따로 재야 합니다. 같은 이름 형제를 세는 일이고 그것은 선형인데, `tree_path` 가 **호버 팝오버**라 포인터가 지나는 행마다 다시 불립니다. `huge.xml`(184MB, `<catalog>` 아래 `<item>` 150만)의 마지막 자식에서:

| 방법 | 값 |
|---|---|
| 지름길(형제 번호를 순번으로) | 16~18µs |
| 걷기, 키를 `decode_key` 로 비교 | 60~62ms |
| 걷기, 바이트로 비교 | 11.2~11.4ms |

11ms 면 복사에는 안 보이지만 호버에는 보입니다 — 목록 위로 커서를 움직이면 스무 행에 220ms 입니다. 그래서 자식이 4,096을 넘으면 지름길을 쓰고, 그 지름길이 가정하는 것(앞선 속성 수·이름이 하나인가)만 싸게 확인합니다. 자세한 것은 [설계](architecture.md)의 XML 경로 절에 있습니다.

SQLite 는 이 표에 들어가지 않습니다. 바이트를 훑지 않으니 인덱싱 속도라는 항목 자체가 없고, 재는 것이 다릅니다 — 336MB·300만 행 `events` 테이블에서:

| 항목 | 값 |
|---|---|
| 연결 | 0.7ms |
| 컬렉션 훑기 (300만 행) | 0.33초 |
| 체크포인트 색인 | 22 KB |
| 60행 조회 (0행 / 150만 / 300만) | 0.16 / 0.16 / 0.15ms |
| 같은 조회, 색인 없이 `OFFSET` | 271ms |

마지막 두 줄이 체크포인트가 산 것입니다. SQLite 는 rowid 로는 즉시 찾아가지만 "300만 번째 행"이라는 개념이 없어서, 색인이 없으면 `OFFSET 2999940` 이 앞의 300만 행을 전부 밟고 지나갑니다 — **1,800배**. 1,024행마다 rowid 하나를 적어 두면 그것이 탐색 한 번과 1,023걸음 이내로 바뀌고, 300만 행에 22KB 를 씁니다.

rowid 가 없는 것 — 뷰와 WITHOUT ROWID 테이블 — 은 적어 둘 것이 없어 저 271ms 쪽 경로를 탑니다. 앞쪽은 똑같이 빠르고 뒤로 갈수록 느려집니다.

이미지로 읽을 때는 숫자가 달라집니다. 압축 항목과 내려받기가 그 길이고, 같은 파일을 `--bytes` 로 재서 나란히 놓으면(같은 기계·같은 예제이고, 파일 열은 다시 쟰 값이라 위 표와 오차 범위 안에서 같습니다):

| 항목 | 파일 | 이미지 |
|---|---|---|
| 연결 | 0.45ms | 0.34~0.52ms |
| 300만 행 훑기 | 332~339ms | 60~67ms |
| 색인 없이 끝 60행 | 286ms | 29ms |
| 60행 조회 | 160~183µs | 78~152µs |
| 전체 검색 | 1.0초 | 0.69~0.70초 |

**연결이 같은 급인 것이 요점입니다.** `sqlite3_deserialize` 는 `SQLITE_DESERIALIZE_READONLY` 로 버퍼를 제자리에서 읽으므로 여는 순간에 350MB 를 복사하지 않고, 복사했다면 여기서 드러났을 것입니다. 그 뒤가 더 빠른 것은 이미지 쪽에 파일 입출력이 아예 없기 때문이고, 그 대가는 **버퍼를 얻는 값** — 350MB 를 OS 캐시에서 읽는 데 70ms — 인데, 그것은 압축 파일이 이미 치른 값이므로 위 표에는 들어 있지 않습니다(예제가 그 읽기를 타이밍 밖에 둡니다). 이미지 전체가 메모리에 있다는 점은 바뀌지 않으므로, 압축 항목의 512MB 상한이 그대로 상한입니다.

JSONL 이 바이트당 가장 빠른 이유가 이 설계의 요약입니다. 색인은 개행만 세므로 로그와 같고, 값을 JSON 으로 읽는 일은 **화면에 있는 행에서만** 일어납니다. 100행에 0.1ms — 한 행당 1마이크로초 남짓이니, 행마다 스캐너를 도는 비용은 파일 크기와 무관하게 한 화면치입니다. 열을 알아내는 데는 앞 1MB 만 봅니다.

xlsx 는 표 밖입니다. 바이트를 훑는 것이 아니라 **통째로 변환**하기 때문입니다 — 재는 것이 인덱싱 속도가 아니라 "한 시트가 메모리에서 얼마가 되는가"입니다. 9.7MB·25만 행 × 6열(150만 칸) 파일에서:

| 항목 | 값 |
|---|---|
| 열기 (시트 목록) | 0.28ms — **이 픽스처의 고유 문자열이 13개**라서. 25만 개짜리 같은 크기 파일은 52ms. 파일에 처음 닿는 실행은 87ms(디스크 값) |
| 시트 선택 (값 전부) | 0.36초 |
| 메모리에 올라간 값 | **51MB** |
| 100행 조회 (앞 / 중간 / 끝) | 0.14 / 0.10 / 0.08ms |
| 수식 켜기, 처음 | 0.35초 (시트를 한 번 더 읽습니다) |
| 수식 켜기, 그다음 | 0.0002ms |
| 전체 검색 (150만 칸) | 0.18초 |

**9.7MB 가 51MB 가 됩니다 — 5.3배.** 이것이 64MB 상한이 있는 이유입니다. 공유 문자열은 디스크에서 색인 하나지만 메모리에서는 문자열 하나씩이고, 같은 비율이면 상한에 걸린 64MB 워크북은 340MB 쯤의 값이 됩니다.

**여는 값은 공유 문자열 표와 스타일이고, 시트 XML 은 고를 때 풉니다.** calamine 의 `Xlsx::new` 는 열 때 `read_shared_strings` · `read_styles` · `read_workbook` 을 전부 합니다. 그래서 여는 비용이 따라가는 것은 **행 수가 아니라 고유 문자열 수**입니다 — 이 표의 0.28ms 는 `huge.xlsx` 의 고유 문자열이 13개(머리글 여섯 · 거래처 다섯 · "정상"·"보류")뿐이라서 나온 값이고, 25만 행이 이름 다섯 개를 돌려 씁니다. 같은 25만 행을 **행마다 다른 이름**으로 써서(10.0MB, 고유 문자열 25만 개) 재면 여는 데 **52ms** 입니다. 실제 업무 파일은 이쪽에 가깝습니다.

같은 표의 이 자리에 한동안 **93ms** 가 적혀 있었는데, 그것은 파일에 처음 닿는 실행의 값입니다. 이번에 고친 코드로 재면서도 첫 실행은 87ms 였고 이어지는 실행은 전부 0.28ms 였습니다 — 같은 바이너리, 같은 파일. 그러니 옛 문장("여는 시간도 파일 크기를 따라갑니다")은 절반만 맞았습니다: 잰 값 93ms 는 9.7MB 를 처음 디스크에서 끌어오는 비용이었지만, 따라가는 것이 있다는 말 자체는 맞습니다 — 다만 따라가는 것은 파일 크기가 아니라 공유 문자열 표의 크기입니다.

**바이트로 읽게 바꾼 것이 값을 나쁘게 하지 않았습니다.** 같은 기계에서 바꾸기 전 커밋과 나란히 재면(`--release` 예제, 캐시가 덥혀진 뒤 세 번씩):

| 항목 | 전 | 후 |
|---|---|---|
| xlsx 열기 (9.7MB) | 0.37~0.42ms | 0.28~0.29ms |
| xlsx 열기 (2.9KB) | 0.38~1.84ms | 0.28~0.32ms |
| xlsx 시트 선택 | 341ms | 339ms |
| xlsx 전체 검색 | 173ms | 170ms |
| parquet 열기 (52MB) | 0.18~0.19ms | 0.17~0.18ms |
| parquet 새 행 그룹 조회 | 145~198ms | 143~191ms |
| parquet 전체 검색 | 2.5초 | 2.4초 |

여는 쪽이 조금 빨라진 것은 파일을 한 번 덜 열기 때문입니다 — 예전에는 이미 mmap 해 둔 파일을 리더가 경로로 한 번 더 열었습니다. `ChunkReader::get_bytes` 가 `Bytes::copy_from_slice` 로 복사하는데도 Parquet 쪽이 나빠지지 않은 이유는, 그것이 대신한 `File` 구현도 새 버퍼로 읽어 왔기 때문입니다. 그래서 `Bytes::from_owner` 로 복사를 없애는 쪽은 하지 않았습니다.

수식은 시트를 한 번 더 읽습니다. 값을 읽는 것과 같은 값(0.35초)이고, 그래서 켤 때만 읽습니다 — 두 번째부터는 들고 있는 것을 씁니다.

Parquet 도 표 밖입니다. 바이트를 훑지 않는 것은 SQLite 와 같은데, 이유가 하나 더 있습니다 — 파일이 **행 그룹 단위로 쓰여 있어** "통째로 안 올린다"를 형식이 공짜로 줍니다. 52MB·200만 행·행 그룹 8개(그룹당 25만 행)에서:

| 항목 | 값 |
|---|---|
| 열기 (푸터만) | 0.19ms — 311MB·행 그룹 48개에서도 0.61ms |
| 100행 조회, 새 행 그룹 | 147~194ms |
| 같은 조회, 캐시된 행 그룹 | 0.10ms |
| 전체 검색 (200만 행 × 5열) | 2.5초 |

**여는 시간이 파일 크기를 따라가지 않습니다.** 푸터의 색인만 읽으므로 자라는 것은 행 그룹 수뿐입니다 — 파일이 여섯 배(311MB)가 되고 행 그룹이 여섯 배(48개)가 되었을 때 0.19ms 가 0.61ms 가 됩니다. 그래서 다른 형식들이 가진 전체 상한이 여기엔 없습니다.

값을 치르는 자리는 하나뿐입니다 — 처음 닿는 행 그룹을 통째로 푸는 순간. 행 그룹은 형식이 쓰인 단위(페이지가 함께 압축되어 있어 절반만 풀 수 없습니다)라 피할 수 없고, 대신 **그 크기에만 상한**을 둡니다. 실측이 근거입니다: 5열 파일에서 그룹당 25만 행이 148ms, 100만 행이 608ms, 200만 행이 1.2초 — 행당 약 0.6µs 로 선형입니다. 상한 400만 행은 최악의 경우 2.5초쯤 창이 멈춘다는 뜻이고, 그보다 크면 뷰어가 아닙니다. 쓰는 쪽이 보통 10만~100만 행을 한 그룹에 넣으므로, 이 상한에 걸리는 것은 일부러 이상하게 쓴 파일뿐입니다.

행 그룹은 두 개까지 들고 있습니다. 뷰포트가 경계에 걸치면 둘이 필요하고, 셋째는 스크롤 방향이 바뀔 때만 값을 하는데 그때는 어차피 다시 읽힙니다.

YAML과 TOML은 이 표에 없습니다. 파서가 값을 메모리에 만들기 때문에 64MB에서 막아 두었고, 설정 파일이 사는 크기와는 자릿수가 다릅니다.

렌더링 결과를 눈으로 확인하려면:

```bash
cd src-tauri && cargo run --release --example render -- ../fixtures/sample.md out.html
```

인코딩은 실제 바이트로 확인합니다 — `cp949.csv`, `utf16.csv`, `utf8bom.csv` 를 열면 도구 모음이 각각 EUC-KR / UTF-16 LE / UTF-8 을 표시하고 세 파일 모두 `id | 이름 | 메모` 로 읽힙니다.

Parquet 픽스처만 생성기 밖에 있습니다. thrift 로 인코딩된 푸터 위에 압축된 열 덩어리라, 손으로 쓰는 것은 읽는 쪽을 시험하려고 형식을 다시 구현하는 일이 됩니다. 읽는 크레이트가 쓰기도 하므로 example 이 만듭니다:

```bash
cd src-tauri && cargo run --release --example parquet -- write ../fixtures [--huge]
```

압축 파일 쪽은 목록 만들기와 항목 하나 꺼내기를 잽니다:

```bash
cd src-tauri && cargo run --release --example archive -- ../fixtures/archive.zip 5
```

이 저장소를 만든 기계에서 목록 만들기 — 여는 값이 파일 크기가 아니라 항목 수를 따라간다는 것이 여기서 보입니다:

| 압축 파일 | 목록 |
|---|---|
| 1GB, 5,000 항목 (로컬 헤더가 기가바이트에 흩어짐) | 8.9ms |
| 8MB, 65,000 항목 | 52ms |
| 1GB, 65,000 항목 | 110ms |

프론트의 트리와 거르기는 10만 항목에서 만들기 78ms, 거르기 12ms 입니다 (거르기 입력에는 120ms 디바운스가 있습니다).

`fixtures/` 에는 형식마다 까다로운 부분을 담은 표본이 있습니다 — `sample.csv`(값 안의 쉼표·따옴표·개행, 짧은 행), `semicolon.csv`(확장자와 다른 구분자), `sample.xml`(속성·CDATA·주석·이름공간·혼합 내용·빈 요소), `sample.yaml`(앵커·여러 문서·문자열 아닌 키), `sample.toml`(날짜·배열 테이블), `wide.json`(루트 배열 100만), `deep.json`(깊이 500), `stream.jsonl`(중첩 객체·배열·null 이 섞인 레코드), `broken.json`(중간 절단), `sample.jsonc`(주석·후행 쉼표·문자열 안의 주석 표시, 그리고 **값 뒤의 덧말** 셋 — 닫는 괄호 뒤·한 줄에 둘·쉼표 뒤)와 그 엄격한 쌍둥이 `strict.json`, 확장자만 `.json` 인 `settings.json`, `sample.sqlite`(rowid 테이블·WITHOUT ROWID·뷰·BLOB·NULL 과 빈 문자열이 나란히), `sample.xlsx`(수식·날짜·시각만 든 칸·불리언·빈 칸·개행이 든 값, 그리고 **C4 에서 시작하는 둘째 시트**), `huge.xlsx`(공유 문자열이 메모리에서 부푸는 것을 재기 위한 25만 행), `sample.parquet`(**두 행씩 세 행 그룹** — 경계를 넘는 창을 잡으려고, 열 가운데의 NULL·타임스탬프·날짜·미리보기보다 긴 바이너리), `archive.zip`(json·log·md·csv·`.log.gz`·중첩 zip·잠김 플래그), `korean-names.zip`(플래그 없는 CP949 이름), `zip64.zip`(작지만 zip64 끝 레코드), `single.zip`·`single-locked.zip`(투명 해제가 되는 쪽과 목록으로 후퇴하는 쪽), `workbook.zip`·`columnar.zip`·`database.zip`(**압축 안의 통합 문서·컬럼 파일·데이터베이스** — 항목이 하나라 투명 해제되므로 뜨는 것은 압축 목록이 아니라 그 안쪽 형식이어야 합니다), 그리고 렌더링 기능을 한 번에 훑는 `sample.md`.

`sample.xlsx` 의 바이트는 `archive.zip` 의 `data/sales.xlsx` 와 `workbook.zip` 에도 그대로 들어갑니다. 같은 워크북을 파일로 한 번, 압축 항목으로 한 번 보내는 것이 요점입니다 — 다른 길로 왔을 뿐 같은 것이어야 합니다. `columnar.zip` 은 `sample.parquet` 과 같은 이유로 Rust 예제가 씁니다(생성기가 감쌀 대상을 만들지 못합니다). `sample.sqlite` 도 같습니다 — `archive.zip` 의 `data/app.sqlite` 와 `database.zip` 이 그 바이트입니다. `node:sqlite` 는 파일에만 쓰므로 쓴 뒤 다시 읽어 넣습니다.

## M19 — 배열·맵 격자와 정렬·필터

2026-09-06, Windows / Ryzen 9 9950X3D, Rust release 예제 기준이다. 단위 테스트는 픽스처 필수 모드로 세 번 실행한다. 화면 배치·머리글 조작·배율은 아래 자동 검증과 별도로 사용자 화면 확인이 필요하다.

| 검사 | M19 이전 | M19 결과 |
| --- | ---: | ---: |
| Rust | 413 | 442, 세 번 모두 통과 |
| vitest | 97 | 100 |
| Svelte 오류 / 경고 | 0 / 0 | 0 / 0 |
| i18n | 4 × 297 | 4 × 307 |
| 스모크 | 36 + 왕복 2 | 37 + 왕복 2 |
| clippy 전체 타깃의 서로 다른 경고 | 21 | 21 (라이브러리 18 + 테스트 3) |

clippy 전 값은 기준 커밋 `55fd756`을 별도 사본에서 실제 실행했다. 신규 경고는 정리했고 기존 경고는 범위 밖에 두었다. 스모크는 프런트와 `custom-protocol` 바이너리를 빌드한 뒤 저장소 루트에서 실행했다. 이 Windows 세션의 샌드박스 안에서는 WebView 자식이 시작되지 않아 결과 파일이 비어 있었고, 밖에서는 실행됐다. 밖에서 잡힌 짧은 CSV 행의 빈 칸 오류를 고친 뒤 전수와 왕복이 통과했다.

| 깨뜨린 것 | 붉어진 검사 수 | 실제 관측 |
| --- | ---: | --- |
| 동률 원본 순서를 역순으로 | 3 | 오름·내림·같은 텍스트 접두 |
| 빈 칸을 맨 앞으로 | 2 | 숫자·글·빈 칸 순서와 내림차순 |
| 공유 중첩 렌더를 스칼라 표시로 | 2 | JSONL과 배열이 함께 실패 |
| 원본 순서의 페이지 요청 정렬 제거 | 1 | `(0,1),(2,2)` 대신 `(3,1),(2,1),(0,1)` |
| 짧은 CSV 행 수정 전 | 1 | 빈 칸 복사가 `None`, 기대는 빈 문자열 |

실측 예제는 다음처럼 실행한다. `wide.json`은 생성기에 이미 있는 백만 원소 `items` 배열이다. Parquet 대용량은 `PARQUET_ROWS=12000000`으로 기존 생성기를 실행해 행 그룹을 48개로 맞췄다(그룹당 250,000행).

```powershell
node scripts/gen-fixtures.mjs --huge
cd src-tauri
$env:PARQUET_ROWS = '12000000'
cargo run --release --example parquet -- write ../fixtures --huge
cargo run --release --example order -- ../fixtures/huge.csv
# huge.jsonl / huge.sqlite / huge.xlsx / huge.parquet / wide.json도 같은 예제
```

수 열은 첫 열 내림차순, 글 열은 두 번째 열 오름차순이다. 필터는 존재하지 않는 문자열로 전 칸을 지나가며 결과 수집 상한에 닿지 않는다. 메모리의 유지 순열과 임시 포함 값은 벡터 용량·키 아레나의 계산치이며 원본 색인·mmap·격자 캐시는 포함하지 않는다. 필터 0건의 순열은 0B다. 검색 시 역사상은 전체 원본 행당 4B를 추가한다.

| 파일 | MiB | 행 | 수 열 정렬 ms | 글 열 정렬 ms | 0건 필터 ms | 유지 순열 MiB | 글 정렬 임시 포함 MiB |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| wide.json | 29.4 | 1,000,000 | 112.8 | 181.1 | 237.1 | 3.8 | 43.6 |
| huge.csv | 298.0 | 4,000,000 | 1485.3 | 1887.6 | 17917.0 | 15.3 | 174.5 |
| huge.jsonl | 438.2 | 2,000,000 | 1473.3 | 1817.5 | 11895.4 | 7.6 | 87.3 |
| huge.sqlite | 335.7 | 3,000,000 | 873.8 | 1353.7 | 6192.3 | 11.4 | 166.9 |
| huge.xlsx | 9.7 | 250,001 | 50.7 | 65.6 | 240.4 | 1.0 | 6.9 |
| huge.parquet | 310.5 | 12,000,000 | 11823.2 | 12127.0 | 19497.1 | 45.8 | 667.6 |

| 파일 | 원본 페이지 전 ms | 원본 페이지 후 ms | 글 정렬 페이지 ms |
| --- | ---: | ---: | ---: |
| wide.json | 0.017–0.021 | 0.018–0.022 | 0.021–0.036 |
| huge.csv | 0.103–0.141 | 0.108–0.181 | 0.100–0.192 |
| huge.jsonl | 0.122–0.137 | 0.111–0.151 | 0.118–0.203 |
| huge.sqlite | 0.132–0.164 | 0.133–0.163 | 0.825–0.927 |
| huge.xlsx | 0.086–0.092 | 0.087–0.184 | 0.087–0.097 |
| huge.parquet | 0.072–0.100 | 0.070–0.078 | 0.079–548.341 |

| 프로세스 | 최고 Working Set MiB | 100ms 간격 Private 최댓값 MiB |
| --- | ---: | ---: |
| wide.json | 188.6 | 198.1 |
| huge.csv | 487.2 | 197.5 |
| huge.jsonl | 540.0 | 102.5 |
| huge.sqlite | 152.9 | 173.8 |
| huge.xlsx | 148.3 | 132.4 |
| huge.parquet | 1356.3 | 1246.9 |

백만 원소 배열의 추가 표 색인은 2.50ms, 4,000,000B였다(기존 트리 색인은 제외). 앞·중간·끝 원본 페이지는 같은 상수 시간 경로를 쓴다. Parquet은 310.5MiB·48개 행 그룹에서 앞·중간 100행이 530.1~548.3ms로 500ms를 넘었다. 최초 결정 측정에서도 556~580ms였으므로 머리글 정렬을 끄고 필터를 남겼다.

페이지 표는 앞·중간·끝의 두 번째·세 번째 조회 범위다. 원본 전·후가 대조군이며 첫 조회의 행 그룹 해제 비용은 별도 로그에 남긴다. 프로세스 실측은 열기·두 정렬·필터·페이지 조회 전체를 포함한 별도 실행이다. Working Set은 mmap과 캐시를 포함하고 Private는 100ms 간격 표본 최댓값이다. 아레나 256MiB는 전체 프로세스 메모리 상한이 아니다. Parquet 실험의 정렬 경로는 실제 UI에서 비활성화했다.


## M40 — HTML 격리 프레임

Rust 전체 538개(fixtures required)가 통과했다. 서버의 토큰/문서 수명·Host·경로 이탈·MIME·UTF-8 응답·삽입 위치·프로브 CSP·크기 상한을 검사하며, 404 정책 헤더 보완 뒤 관련 7개를 다시 통과했다. 경로 URL 정규화(퍼센트 디코딩)를 제거한 변형은 1개 실패·537개 필터 제외였고 원본 바이트로 복원했다.

vitest는 324개·28파일, check는 오류0·경고0·4로케일×438키다. 에이전트/FrameView/FrameSearchBar와 App/Toolbar를 연결했다. HTML 원문은 줄 색인으로, pendingAnchor는 ready 뒤 소비한다. 생성기의 report.html은 제목3·인라인 표식·로컬 모듈·웹폰트·차단 fetch·상대/외부 링크를 포함한다. htmlFrame 스모크는 ready/probe, 폰트/모듈, 찾기와 원문 전환을 검사한다. 추가 후 매니페스트는 46개다.

새 debug·release 바이너리로 각각 스모크 1회가 통과했다(46개 + 단일 인스턴스 전달·새 창 왕복2). 앱 sweep 시간은 debug 18,511ms, release 17,316ms였다. 두 빌드의 report.html 관측값은 동일하다: 제목3, 찾기2, 원문19줄, inline script/module/font=true, probe=`rejected:Origin header is not a valid URL`. 단순 timeout을 거부로 세지 않는다.

에이전트 삽입 제거 변형은 새 debug 빌드에서 report.html ready timeout 1개 실패·나머지45개 통과, 왕복2 통과였다. 원본을 바이트 단위로 복원하고 정상 debug·release를 다시 빌드했다. Windows 실제 실행에 한정하며 WebKit은 CI 확인이 남아 있다. clippy는 이번 단계에서 실행하지 않았다.

## M42 — 외부 자원 허용과 압축 HTML 형제 자원

M46 통합 후 프런트는 vitest 348개·32파일, check 오류·경고 0 및 4로케일×454키, FrameView Svelte 분석 issues 0이다. 토글 IPC 성공·실패, 재읽기 동안 허용 유지, 새 탭 초기값, 이전 로딩 메시지 거부를 검사한다.

Rust에는 문서별 완화 CSP와 프로브 유지, 압축 경로의 루트 이탈 거부, 같은 창의 직접 파일 ZIP 부모만 이용하는 형제 서빙, 읽는 도중의 해제 상한 검사를 추가했다. 생성기의 archive.zip에는 docs/page.html·docs/page.css·shared.css·docs/pixel.svg를 추가하고, 기존 htmlFrame에는 허용·재차단과 실제 계산된 스타일·이미지 확인을 연결했다. 매니페스트 항목은 늘리지 않는다.

M46과 v0.21.0 통합 뒤 Rust 전체 544개(fixtures required, 실패·무시 0)가 통과했다. 압축 경로의 루트 위 차단을 제거한 변형은 `../outside.css` 단언 1개 실패·543개 필터 제외였다. 완화 CSP를 무시하는 변형은 새 debug 빌드 스모크에서 report.html의 외부 허용 단언 1개만 실패했고, 나머지 45개와 왕복 2개는 통과했다. 두 변형 모두 소스를 바이트 단위로 복원했다.

픽스처 재생성 후 정상 debug·release 빌드 스모크가 각각 46개와 왕복 2개를 통과했다(앱 sweep debug 18,621ms·release 17,586ms). 두 빌드의 report.html은 허용 뒤 차단 0·재차단 1, 압축 안 같은 디렉터리 CSS·부모 디렉터리 CSS·이미지 모두 true, 제목 3·찾기 2·원문 22줄, 인라인 스크립트·모듈·폰트 모두 true, 프로브 `rejected:Origin header is not a valid URL`이었다. release는 main의 진행 로그 변경을 rebase한 뒤 실행했으며, 이 rebase는 제품·빌드 입력을 바꾸지 않았다. Windows 실제 실행에 한정하고 Linux·macOS와 사용자 화면 확인은 별도다. clippy와 성능 전후 실측은 실행하지 않았다.

## M44 — 읽던 위치 이어가기

프런트 vitest 352개·33파일과 check 오류0·경고0·4로케일×452키가 통과했다. 변경한 Svelte 컴포넌트 8개의 자동 분석 issues는 모두 0이다. 위치 왕복·손상 pos만 폐기·아직 열리지 않은 탭의 pending 유지, 원본 행 번호, 마크다운 구간 비율, 1초 디바운스와 탭 전환·닫기 즉시 저장, window load와 fonts.ready 뒤의 loaded를 검사한다.

raw 위치의 타입 검사를 제거한 변형은 vitest 2개 실패·9개 통과였다. 원본 바이트 복원 후 전체 352개가 다시 통과했다. 생성기의 session-position.md와 새 sessionPosition 검사를 추가하여 매니페스트는 47개다. 스모크는 자체 세션 스냅샷을 저장하고 실제 Session.start로 문서를 다시 열어 렌더 위치·원문 줄을 확인한 뒤 기존 저장값을 복원하도록 구성했다.

main edaa03c 통합 뒤 Rust 전체 544개(fixtures required, 실패·무시 0)가 통과했다. 정확 트리 경로의 긴/중복 키·재색인·XML 혼합 형제·사라진 경로·상한·취소를 검사한다. 완료 신호 이전에 위치를 소비하는 변형은 sessionPosition 1개 실패·나머지 46개와 왕복 2개 통과였으며 원본 바이트를 복원했다. 첫 변형 실행에서 함께 드러난 기존 목차 회귀는 저장 위치가 없는 테마 교체에도 tick을 기다려 스크롤 복원이 늦어졌기 때문이었다. pendingPosition이 있을 때만 기다리도록 좁힌 뒤 변형·정상 스모크에서 목차도 통과했다.

새 debug·release 빌드로 각각 정상 스모크 47개와 왕복 2개가 통과했다. 앱 sweep은 debug 19,831ms·release 18,594ms다. 두 빌드의 sessionPosition은 렌더 스크롤 8,584.6669921875px로 복원(검사 허용 오차 12px 미만)했고 원문 줄 인덱스 36(화면 37번째 줄)을 유지했다. 목차 200개에서 스크롤 중 제목 사각형 재측정은 0회, 정지 대조군·스크롤 프레임 간격 중앙값은 모두 5.6ms였다. 같은 실행의 대조군이며 변경 전후 성능 비교는 아니다.

HTML은 스크립트·모듈·폰트 true, 제목 3·찾기 2, 프로브 `rejected:Origin header is not a valid URL`이었다. 언마운트 직후와 약 1초 뒤 요청 카운터는 두 빌드 모두 html 1·agent 1·resource 2로 같았다. Windows 실행이며 실제 앱 재시작 후 사용자 화면 확인·다른 OS 검증·clippy는 별도로 남는다. 스모크 종료 로그의 Chrome_WidgetWin_0 등록 해제 오류 1412는 관측됐지만 검사 실패는 없었고 원인은 조사하지 않았다.


## M43 — PDF 격리 프레임

공식 PDF.js 6.3.289 ZIP의 버전·SHA-256을 고정하고 필요한 자원을 exe에 내장했다. Range는 실제 serve 경로에서 200/206/416·범위 바이트·CORS 헤더 노출을 검사한다. 매직 우선·바이너리 보존·PDF 상한·자원 경로 및 file 쿼리 경계·뷰어 주입·MIME도 검사한다. 프런트는 pages/page 메시지, file과 세대 쿼리의 공존, PDF 위치 저장과 실제 Session.start의 복원 호출을 검증한다.

| 검사 | 결과 |
| --- | --- |
| DVIEWER_FIXTURES=required cargo test | 553 통과, 실패·무시 0 |
| npm test | 359 통과, 34파일 |
| npm run check | 오류0·경고0, 4로케일×459키 |
| Windows 정상 debug 스모크 | 48개 + 왕복2 통과, 앱 sweep 20,159ms |
| Windows release 스모크 | 48개 + 왕복2 통과, 앱 sweep 19,134ms |
| 내장 PDF.js 자원 | 294파일7,926,844바이트 + manifest11,561바이트 |
| exe 크기 | debug51,595,776바이트, release31,922,688바이트 |

생성기가 scripts/pdf-fixtures.mjs를 호출해 report.pdf(두 페이지·needle·목차 세 항목 중 둘은 같은 페이지)와 image-only.pdf를 직접 쓴다. xref와 stream 길이는 실제 바이트 길이로 만든다. pdfFrame 스모크는 ready의 pages2/목차3, 고유 ID, page2 왕복, 텍스트 및 검색, 페이지 위치 보존과 탭 닫기 뒤 iframe 제거를 검사한다. 작은 PDF는 Range를 사용하지 않을 수 있으므로 범위 응답의 근거는 Rust 테스트다.

첫 debug 실행은 PDF만 실패했고 나머지47·왕복2는 통과했다. 실제 WebView2/CDP에서 opaque의 blob:null 모듈 워커 진입점이 cross-origin redirect로 거부되는 것을 확인했다. PDF.js가 처리 후 계속 진행하는 localStorage/parent.document 예외와 구분했다. Blob 클래식 워커에서 고정 ESM을 import()하도록 좁혀 수정한 뒤 PDF 단독 및 전체 debug/release가 통과했다. sandbox·CSP는 이 수정 때문에 완화하지 않았다.

| 깨뜨려 확인 | 결과 |
| --- | --- |
| Range 전달을 빈 헤더로 변경 | Rust 응답 테스트1실패(200 != 206) |
| outline 이전에 빈 목차로 ready 전송 | pdfFrame 목차 단언1실패, 다른47·왕복2통과 |

두 변형은 원본 바이트로 복원했다. 복원 뒤 Rust 전체553 통과, 정상 debug 재빌드와 release 스모크를 완료했다. PDF는 두 빌드 모두 pages2·page2·headings3·matches2이며, 프로브는 rejected:Origin header is not a valid URL이다. PDF 관측 시간은 debug508ms/release495ms, HTML 대조군은 1,254ms/1,258ms다. HTML은 inline/module/font=true·목차3·검색2, 외부 허용 뒤 차단0/재차단1, ZIP 형제 CSS·이미지 true를 유지했다. 시간은 같은 실행의 관측값이며 변경 전후 성능 실측은 아니다.

FrameView·Toolbar·docs.svelte.ts autofixer issues는 없었다. session.svelte.ts 분석은 도구 내부 Cannot read properties of undefined (reading 'text') 예외로 실패했지만 svelte-check와 세션 테스트는 통과했다. clippy·대형 PDF 메모리 실측·실제 암호 PDF·한글/다단/복사 품질 화면 확인은 실행하지 않았다. image-only.pdf 안내와 실제 재실행 페이지 복원은 사용자 확인으로 남긴다. Linux·macOS warm 스모크도 미확인이므로 Windows 결과만으로 전체 마일스톤 완료를 선언하지 않는다.

### PDF 이동 뒤 크기 변경 경합 재검증 — 2026-09-17

pagesloaded를 기다린 WIP도 release 3회 중 1회 실패했다. 기존 CDP 기록은 goto2가 실제 2쪽에 도달한 뒤 약 3ms 후 1쪽으로 돌아왔음을 보여준다. PDF.js 6.3.289의 페이지 setter는 동기 pagechanging 이후 스크롤을 옮기지만, 크기·배율 변경에 쓰는 위치 기록은 다음 update까지 갱신하지 않는다. 초기 뷰 적용 전이라는 가설과 달리 진단 시 isInitialViewSet은 true였다.

실제 release 뷰어에서 goto2의 setter 반환 직후 폭을 100px 줄이고 resize를 전달하자 같은 PDF goto timeout이 재현됐다(다른 47개 통과). 동일 조건에서 resize 전에 pdfViewer.update()만 호출한 대조 실행은 48개 모두 통과했다. 공통 goto 경로에 이 갱신을 넣어 페이지 번호·목차·세션 복원이 같은 처리를 거치게 했다. 대기 요청은 ready와 pagesloaded 뒤 적용하고 실제 목표 페이지 확인 후 응답한다. 임의 타이머는 추가하지 않았다.

| 검사 | 결과 |
| --- | --- |
| DVIEWER_FIXTURES=required cargo test | 553 통과, 실패·무시 0 |
| npm test | 363 통과, 35파일 |
| npm run check | 오류0·경고0, 4로케일×459키 |
| 회귀 테스트: 다음 스크롤 갱신 전 resize | 수정 전 1실패·기존3통과 → 수정 후4통과 |
| 새 release exe | 31,926,784바이트 |

프런트와 release 바이너리를 새로 빌드한 뒤, 진단 주입 없이 기존 스모크를 연속 실행했다. 스모크 코드와 단언은 수정하지 않았다.

| release 회차 | 픽스처 + 왕복 | PDF | HTML 대조군 | 앱 sweep |
| --- | --- | --- | --- | --- |
| 1 | 48 + 2 통과 | 474ms | 1,253ms | 19,149ms |
| 2 | 48 + 2 통과 | 555ms | 1,243ms | 19,182ms |
| 3 | 48 + 2 통과 | 559ms | 1,271ms | 19,297ms |

세 회차 모두 PDF pages2·page2·headings3·matches2와 격리 프로브 거부를 확인했다. 시간은 각 실행의 관측값이며 성능 개선 주장은 아니다. 로그는 .agent-works/m43-goto-final-*.log, 원본 결과는 로컬 임시 디렉터리 dviewer-smoke-FQmSNu·Rdu0XM·cr8Cou의 sweep.jsonl이다. 이번에는 debug 스모크·clippy·Linux/macOS·사용자 화면 및 실제 앱 재시작 복원 검증을 실행하지 않았다. 이 결과는 Windows release 검증이다.

### WebKit PDF 초기화 관측 — 2026-09-17

main a4eada5의 warm run35218242192에서 Windows는 통과했지만 Linux(WebKitGTK2.50.4)·macOS26.6.2는 모두 PDF timeout이었다. served html1/agent2/resource21·agent start yes만으로 PDF 에이전트 실행·워커 import·뷰어 초기화 완료 여부를 알 수 없었다. 이번 변경은 마지막 PDF 단계와 오류 detail을 timeout 진단에 남긴다. 워커 내부 import 거부도 별도로 전달한다. WebKit 원인을 확정하거나 워커 방식·CSP를 바꾼 것은 아니다.

| 검사 | 결과 |
| --- | --- |
| DVIEWER_FIXTURES=required cargo test | 553 통과, 실패·무시0 |
| npm test | 368 통과, 35파일 |
| npm run check | 오류0·경고0, 4로케일×459키 |
| Windows 새 custom-protocol debug 스모크 | 48개 + 왕복2 통과, PDF507ms·HTML1255ms·앱 sweep20,185ms |
| 단계 전송을 무효화한 변형 | 단계 회귀1실패, 다른6개는 필터로 제외 |
| detail을 빈 문자열로 바꾼 변형 | 오류 전달 회귀1실패, 다른6개는 필터로 제외 |
| dev 정리 | incremental 제거, cargo clean617파일6.0GiB |

회귀는 실제 에이전트를 VM에서 실행해 단계 허용 목록, 초기화·문서·워커·창 오류의 문자열 제한, 첫 실패 뒤 늦은 메시지 무시, worker bootstrap import 거부 전달을 검사한다. 파서는 발신 프레임·load 식별과 detail 최대 길이를 검증하고, timeout 문자열은 URL·토큰 가림을 전문 단언한다. 다시 열 때 frameStage 초기화도 검사한다. 두 변형은 원본 바이트로 복원했다. FrameView·docs.svelte.ts autofixer issues0. 로그는 .agent-works/m43-webkit-final-*.log 및 m43-webkit-mutation-*.log, 원본 스모크는 로컬 임시 디렉터리 dviewer-smoke-7Ceyli/sweep.jsonl이다. 이번 release 스모크·clippy·Linux/macOS 재실행은 하지 않았다. 다음 warm의 stage/detail을 보고 후속 수정을 판단하며 세 OS에서 확인한다.

### WebKit 마지막 관측: 요청 기록·스톨 스냅샷 — 2026-09-17

warm run35220937353의 두 OS는 worker-imported 뒤 준비 신호가 없었다. 고정 PDF.js 소스에서는 초기화 함수가 실패해도 initializedPromise가 미완료로 남는다. 서버의 최근 요청과 스톨 상태를 추가하고 초기화 함수 자체의 거부도 직접 수집했다. 원인은 아직 확정하지 않았으며 CSP·워커·locale·preferences 대체는 하지 않았다.

| 검사 | 결과 |
| --- | --- |
| DVIEWER_FIXTURES=required cargo test | 554 통과, 실패·무시0 |
| npm test | 373 통과, 36파일 |
| npm run check | 오류0·경고0, 4로케일×459키 |
| Windows 새 custom-protocol debug 스모크 | 48개 + 왕복2 통과 |
| PDF / HTML 대조군 / 앱 sweep | 522ms / 1,260ms / 20,446ms |
| 스톨 전송을 무효화한 변형 | 스냅샷 회귀1실패, 다른9개 필터 제외, 원본 복원 |
| dev 정리 | incremental 제거, cargo clean617파일6.0GiB |

Rust는 최근 20개 유지·순서·404/206·토큰/쿼리 제거·문서 간 격리·폐기 후 초기화를 확인한다. VM 회귀는 가상 시간으로 7,999ms에는 없고 8,000ms에 한 번 나오는 스냅샷, 최근 자원15개·상태와 경로 정리, 초기화/닫기/오류 시 취소를 확인한다. 초기화 함수가 거부됐지만 완료 약속은 계속 미완료인 경우도 직접 오류 전달로 잡는다. 파서는 자료형·목록/문자열/직렬화 길이·발신 프레임과 세대를 검증한다. FrameView·docs.svelte.ts autofixer issues0. 로그는 .agent-works/m43-webkit2-*.log, 원본 스모크는 로컬 임시 디렉터리 dviewer-smoke-MfMlkG/sweep.jsonl이다. release·clippy·Linux/macOS 재실행은 하지 않았다. 다음 warm의 last/stall/detail이 마지막 관측 근거이며 세 OS에서 확인한다.

### PDF: Windows 전용(이 판) — 2026-09-17

WebKit 관측 두 라운드 뒤 이 판의 PDF 지원을 Windows로 제한했다. 판별은 그대로 두고 열기·재읽기에서 비Windows PDF를 Unsupported로 거부한다. 공통 정책 회귀는 Windows 허용·비Windows 거부 및 다른 형식 보존을 검사한다. 직접 파일·URL(MIME 판별 포함)·붙여넣기·압축 항목·파일 재읽기 호출부에 같은 검사를 적용했다.

| 첫 커밋 검사 | 결과 |
| --- | --- |
| DVIEWER_FIXTURES=required cargo test | 555 통과 |
| npm test | 373 통과, 36파일 |
| npm run check | 오류0·경고0, 4로케일×460키 |
| 생성기·파일 선택기 조건식 모의 실행 | win32: PDF 생성·필터 포함/48항목, darwin·linux: PDF 제외/47항목 |

Windows 생성기를 다시 실행해 smoke.json48항목을 확인했다. image-only.pdf는 화면 확인용으로만 생성하며 기존 스모크 수를 늘리지 않았다. fileAssociations의 pdf는 정적으로 남는다. 로그는 .agent-works/m43-platform1-*.log. 비Windows 실제 앱 검증은 main 푸시 뒤 warm에서 확인한다. WebKit 근본 원인 조사는 후속 판으로 넘긴다.

### 스톨 진단 누락 방지와 최종 Windows 확인 — 2026-09-17

자원 엔트리의 누락 필드를 null로 정규화하고 스냅샷 상한을8192자로 올렸다. 구조 검증 실패는 raw2000자로 보존하며 프레임/load 검증·URL/토큰 가림은 유지한다. 에이전트의 개별 Performance 속성 getter가 실패해도 다른 필드는 남긴다. 기존 코드도 undefined responseStatus를 null로 바꾸고 있었으므로 WebKit 스톨 누락의 근본 원인을 확정한 것으로 보지 않는다.

| 최종 검사 | 결과 |
| --- | --- |
| Rust | 첫 커밋의555통과 재사용(백엔드 논리 변경 없음) |
| npm test | 376 통과, 36파일 |
| npm run check | 오류0·경고0, 4로케일×460키 |
| Windows 새 debug 스모크 | 48개 + 왕복2 통과 |
| PDF / HTML 대조군 / 앱 sweep | 534ms / 1,266ms / 20,216ms |
| responseStatus 누락 정규화 제거 변형 | 회귀1실패, 원본 바이트 복원 |
| dev 정리 | incremental 제거, cargo clean617파일6.0GiB |

회귀는 누락 리소스 필드·소수 duration·4096자 초과 정상 스냅샷·검증 실패 raw·순환 입력·예외를 던지는 getter를 다룬다. 로그는 .agent-works/m43-platform2-*.log 및 m43-platform-mutation.log, 원본 스모크는 로컬 임시 디렉터리 dviewer-smoke-ZSzoZs/sweep.jsonl이다. 최종 에이전트를 새 프런트와 debug 바이너리에 포함해 확인했다. release·clippy·비Windows 실제 스모크는 실행하지 않았다. Linux/macOS는 PDF 없는47항목의 후속 warm으로 확인한다.

## M43b — PDF 방향 자동 교정

앞 세 페이지의 텍스트 행렬과 /Rotate를 함께 집계하고 비공백 가로쓰기 항목 20개 이상·우세 80% 이상일 때만 문서 방향을 교정한다. 저장 회전과 명시적 0은 자동 판정보다 우선한다. VM에서 실제 에이전트를 실행해 네 방향·/Rotate 상쇄·항목 수·우세 비율 경계·세로쓰기와 잘못된 항목 제외·세 페이지 제한·추출 실패·저장값과 pagesloaded 도착 순서·동기 회전 이벤트·같은 각도의 명시적 응답·되돌리기·수동 회전·위치 캐시 유지·닫기와 입력 거부를 확인했다. 위치·메시지 파서와 세션 왕복은 회전 미저장과 0을 구분한다.

| 검사 | 결과 |
| --- | --- |
| npm test (M49 rebase 전) | 397개, 38파일 통과 |
| main 8d9718e rebase 후 npm test | 399개, 38파일 통과 |
| npm run check | 오류 0·경고 0, 4로케일 × 485키 |
| 임계 80% → 0% 변형 | 혼합 방향 표본 검사 1개 실패, 나머지 에이전트 15개 통과 |
| 원본 복원 | 14,709바이트·SHA-256 일치, 에이전트 16개 통과 |
| 새 프런트 + debug 바이너리 스모크 | 픽스처 50개 + 단일 인스턴스·새 창 왕복 2개 통과 |
| sideways.pdf / report.pdf / report.html | 486ms / 510ms / 1,365ms (기능 검증 시간, 성능 전후 비교 아님) |
| Rust 테스트·clippy·release·비Windows 실제 스모크 | 이번 프런트 과제에서 실행하지 않음 |

생성기가 쓴 sideways.pdf는 두 페이지에 회전 텍스트를 열두 항목씩 배치한다. Windows 매니페스트의 pdfOrientation 분기는 rotated 완료와 data-auto-rotation을 확인하고 실제 되돌리기 버튼을 눌러 0도·auto:false·같은 페이지 저장을 검증한다. 기존 report.pdf의 목차·이동·찾기 검사는 유지한다. 사용자 앱 종료 신호 뒤 npm run build → cargo build --features custom-protocol → debug smoke를 한 번 실행했다. 에이전트는 include_str!이므로 새 바이너리 빌드가 필수다. 로그는 .agent-works/m43b-*.log, 원본 결과 사본은 .agent-works/m43b-sweep.jsonl이다. 실제 사용자 PDF의 시각적 정확성과 재실행 화면 확인은 별도다.

debug incremental과 dviewer dev 산출물 508개·4.1GiB를 정리했다. main 8d9718e의 M49 위로 충돌 없이 rebase했으며 range-diff에서 두 구현 커밋의 패치가 동일함을 확인했다. 통합 트리의 vitest·check를 다시 통과했다. 스모크는 rebase 전 50개 결과이며 M49를 합친 트리의 스모크는 다시 실행하지 않았다.

## M43c — 이미지 PDF의 글줄 방향 추정

텍스트가 부족한 PDF는 별도의 작은 canvas로 앞 두 쪽의 행·열 잉크 프로파일을 검사한다. 단위 테스트는 정규화한 분산·희소/균일·비슷한 에너지·비율과 잉크 경계·/Rotate·두 쪽 일치·캔버스 크기·추출 실패·예산 초과·닫기 취소·저장값과 수동 회전 우선순위·반대로 후 되돌리기 유지를 검증한다. 실제 PDF.js의 렌더·메시지·DOM 연결은 아래 Windows 스모크로 확인했다.

| 검증 | 결과 |
| --- | --- |
| 전체 vitest (변형 원복 후) | 405개 / 38파일 통과 |
| 경계 보강 후 에이전트·메시지 검사 | 33개 / 2파일 통과 |
| npm run check | 오류 0·경고 0, 4로케일 × 487키 |
| Svelte autofixer | FrameView·DocTab issues 0, 기존 effect·Map 제안은 유지 |
| 이미지 판정 상한 | 앞 2쪽·긴 변 최대 256px·전체 이미지 탐색 예산 200ms |
| 픽스처 | sideways-image.pdf·upright-image.pdf 각각 2쪽, 16,176바이트 |
| 이전 PDF 픽스처 비교 | report.pdf·sideways.pdf·image-only.pdf 모두 이전 생성기와 바이트 동일 |
| 에너지 비 2 → 0 변형 debug 스모크 | upright-image.pdf만 실패, 다른 52개·왕복 2개 통과 |
| 원본 복원 | 18,163바이트·SHA-256 일치 |
| 새 프런트·원복 debug 스모크 | 53개·왕복 2개 통과, 실패 0 |
| sideways-image / upright-image | 221ms / 212ms |
| 텍스트 PDF / HTML 대조군 | report.pdf 497ms / report.html 1,289ms |
| Rust 테스트·clippy·release·비Windows 실제 스모크 | 이번 과제에서 실행하지 않음 |

생성기는 무압축 DeviceGray 비트맵의 원시 바이트와 정확한 xref를 쓴다. 새 파일은 글꼴이나 회전 텍스트가 없는 세로/가로 줄무늬 대조군이다. 스모크는 `rotated` 수신값과 알약 DOM을 확인하고 실제 반대로·되돌리기 버튼을 누른다. 정상 원본 결과는 sideways-image에서 감지 270도·반대로 90도·최종 0도·페이지 2 유지, upright-image에서 감지 0도·auto:false다. 변형은 upright-image에 기대 0도 대신 270도·auto:true가 와 실패했다. 임의 대기시간이나 픽셀 색 비교로 통과시키지 않았다.

변형과 원복 각각 npm run build → cargo build --features custom-protocol → debug smoke를 직렬로 실행했다. 원복 SHA-256은 `4a7970056aad9c6d2cf1e60cecf4b017759fbda2301bb41a7941c94b991b2766`이다. 로그·원본 결과는 `.agent-works/m43c-mutation-smoke.{log,jsonl}`, `m43c-debug-smoke.{log,jsonl}`, `m43c-mutation-restored.log`, `m43c-restored-vitest.log`, `m43c-smoke-check.log`, `m43c-fixture-check.log`에 남겼다. 표의 시간은 기능 검증 시간이며 성능 전후 비교가 아니다. 사용자 실제 Print To PDF 문서의 시각적 적합성은 계획 세션이 별도로 확인한다. 저장된 회전은 이미지 추정보다 우선한다.

종료 후 debug incremental과 dviewer dev 산출물 508개·4.1GiB를 정리했다. 이번 과제의 픽스처 비교 사본과 변형 백업도 제거했으며 검증 로그와 생성기 픽스처는 남겼다. release·의존 크레이트와 다른 작업의 자료는 보존했다.
